# 灾后临时安置点智慧水电管理与环境监测系统 — 系统文档

> 最后更新：2026-05-14
> 学校：应急管理大学
> 主控芯片：STM32F103C8T6（面包板）

---

### 最近变更

| 日期 | 内容 |
|------|------|
| 2026-05-14 | **Web大屏重构 v2**：单页设计，状态横幅+6指标卡片(带彩色顶条和图标)+全屏报警遮罩+断线检测 |
| 2026-05-14 | **Qt后台修复**：阈值设置使用成员变量缓存、离线时禁用控制按钮、图表变量加注释 |
| 2026-05-14 | **Qt源码迁移**：从 `D:\Code\QT_Code\System_UI` 迁移至 `D:\AAA\System_UI` |

---

## 一、系统概述

本系统面向灾后临时安置场景，实现对安置点的**水电计量**和**环境质量监测**，并具备自动化控制与远程管理能力。

### 1.1 三端角色定位

| 终端 | 受众 | 部署位置 | 定位 |
|------|------|----------|------|
| **Web看板** | 安置点群众、来访人员 | 公共区域大屏 | 信息公开大屏，直观展示当前环境状态，让群众看到"现在是安全的" |
| **Qt桌面端** | 管理人员、运维人员 | 管理站办公室 | 后台管理工具，查看历史数据、设置阈值、远程控制、处理报警 |
| **OLED** | 调试用途 | STM32电路板 | 开发调试时查看实时数据，部署后无实质作用，保留不删 |

### 1.2 系统架构

```
传感器采集层 (STM32F103)
    ↓ UART 115200
通信桥接层 (ESP32 + MQTT)
    ↓ WiFi
云平台层 (巴法云 bemfa.com)
    ↓ MQTT
应用层 (Web看板 + Qt桌面端)
```

---

## 二、系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        STM32F103C8T6 (主控)                        │
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │  DHT11   │  │ GP2Y1014 │  │  MQ135   │  │ ACS712   │           │
│  │ 温湿度   │  │ PM2.5粉尘│  │ 空气质量 │  │ 电流检测 │           │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘           │
│       │ PB13        │ PA2 ADC     │ PA1 ADC     │ PA0 ADC         │
│       └──────────────┴─────────────┴─────────────┘                 │
│                                                    ┌──────────┐    │
│  ┌──────────┐                                     │ YF-S401  │    │
│  │ OLED 显示│                                     │ 水流传感器│    │
│  │ PB8/PB9  │                                     │ PA5 EXTI │    │
│  └────┬─────┘                                     └────┬─────┘    │
│       │                                                │          │
│  ┌────┴─────────────────────────────────────────────────┴──────┐  │
│  │                    规则引擎 (main.c 主循环)                    │  │
│  │   温度→风扇/舵机 | 湿度→舵机 | PM2.5→蜂鸣器+风扇+关窗+LED   │  │
│  │   电流→LED | 流量→蜂鸣器 | AQ→风扇+LED                      │  │
│  └────┬─────────────────────────────────────────────────┬──────┘  │
│       │                                                │          │
│  ┌────┴──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┴──────┐   │
│  │  风扇     │  │ 舵机 │  │蜂鸣器│  │三色LED│  │  UART串口   │   │
│  │  PB6      │  │ PA6  │  │ PB14 │  │PA11/ │  │  PA9/PA10   │   │
│  │  GPIO     │  │ PWM  │  │ GPIO │  │12/15 │  │  115200 8N1  │   │
│  └───────────┘  └──────┘  └──────┘  └──────┘  └──────┬───────┘   │
│                                                       │           │
└───────────────────────────────────────────────────────┼───────────┘
                                                        │ UART
┌───────────────────────────────────────────────────────┼───────────┐
│  ESP32 (WiFi/MQTT桥接)                                │           │
│  ┌──────────────────────────────────────────────────┐ │           │
│  │  UART接收 JSON → 解析缓存 → 每5秒 MQTT上传      │ │           │
│  │  MQTT下行 → 解析阈值/指令 → UART转发给STM32     │ │           │
│  └──────────────────────────────────────────────────┘ │           │
│              │ WiFi                                   │           │
└──────────────┼────────────────────────────────────────┘           │
               ▼                                                      │
┌─────────────────────────────────────────────────────────────────┐  │
│                    巴法云 (bemfa.com:9501)                       │  │
│                    Topic: test001up                              │  │
│                    ClientID: 6525cbc0...                         │  │
└────┬────────────────────┬───────────────────────────────────────┘  │
     │ MQTT (WSS:9504)    │ MQTT (TCP:9501)                          │
     ▼                    ▼                                          │
┌──────────┐      ┌──────────────────┐                               │
│ Web看板  │      │  Qt桌面端        │                               │
│ (直连MQTT)│      │ (直连MQTT)       │                               │
│ 实时仪表盘 │      │ 历史数据 / 管理   │                               │
│ ECharts图 │      │ SQLite存储       │                               │
└──────────┘      └──────────────────┘                               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、硬件模块与引脚分配

### 3.1 MCU：STM32F103C8T6

- 内核：ARM Cortex-M3 @ 72MHz
- Flash：64KB，SRAM：20KB
- 开发板：面包板搭建

### 3.2 引脚分配总表

| 模块 | 信号 | 引脚 | 外设功能 | 备注 |
|------|------|------|----------|------|
| **传感器类** |
| ACS712 电流传感器 | ADC输入 | PA0 | ADC1_IN0 | 300次采样覆盖50Hz工频周期 |
| MQ135 空气质量 | ADC输入 | PA1 | ADC1_IN1 | ADC线性映射0~500ppm |
| GP2Y10 粉尘 | ADC输入 | PA2 | ADC1_IN2 | 厂商公式(V×11-420)×0.20 |
| GP2Y10 LED控制 | GPIO输出 | PB12 | GPIO | 低电平点亮LED |
| DHT11 温湿度 | 单总线数据 | PB13 | GPIO | 18ms起始信号，超时防卡死 |
| YF-S401 水流 | 脉冲输入 | PA5 | EXTI5 | 上升沿中断，98脉冲/L |
| **执行器类** |
| 舵机 SG90 | PWM输入 | PA6 | TIM3_CH1 | 50Hz，500~2500µs，0~180° |
| 风扇 | GPIO输出 | PB6 | GPIO | 低电平转、高电平停 |
| 蜂鸣器（有源） | GPIO输出 | PB14 | GPIO | 低电平响 |
| 红灯 | GPIO输出 | PA15 | GPIO | 高电平亮（需禁用JTAG） |
| 绿灯 | GPIO输出 | PA11 | GPIO | 高电平亮 |
| 黄灯 | GPIO输出 | PA12 | GPIO | 高电平亮 |
| **显示类** |
| OLED SCL | I2C时钟 | PB8 | 软件I2C | SSD1306 128x64 |
| OLED SDA | I2C数据 | PB9 | 软件I2C | 地址0x78 |
| **通信类** |
| USART1 TX | UART发送 | PA9 | USART1 | 接ESP32 RX |
| USART1 RX | UART接收 | PA10 | USART1 | 接ESP32 TX |

### 3.3 供电方案

```
12V适配器 → LM2596(调5V) → 面包板电源模块
  │
  ├─ 5V：MQ135(单独孔)、舵机(单独孔)、风扇、GP2Y10+ACS712+水流(共用孔)、ESP32
  ├─ 3.3V：OLED、DHT11
  └─ GND共地：LM2596 → 面包板电源GND → 所有模块GND → STM32 GND
```

---

## 四、STM32 固件详解

### 4.1 开发环境

- IDE：Keil MDK v5（ARMCC v5.06）
- 项目路径：`D:\AAA\FinalSystem\`

### 4.2 目录结构

```
FinalSystem/
 ├── Hardware/       ← 各模块驱动
 │    ├── ACS712.c/.h
 │    ├── DHT11.c/.h
 │    ├── GP2Y10.c/.h
 │    ├── MQ_135.c/.h
 │    ├── waterFlow.c/.h
 │    ├── OLED.c/.h
 │    ├── servo.c/.h
 │    ├── led.c/.h
 │    ├── buzzer.c/.h
 │    ├── motor.c/.h
 │    ├── serial.c/.h
 │    ├── pwm.c/.h
 │    └── Delay.c/.h  (实际在System/下)
 ├── User/
 │    └── main.c      ← 主循环
 ├── System/
 │    ├── Delay.c/.h
 │    └── ADCX.c/.h
 ├── Start/           ← 启动文件
 └── Library/         ← 标准外设库
```

### 4.3 各模块功能

#### 4.3.1 main.c — 主控逻辑（核心）

**流程：**

```
上电初始化（LED/蜂鸣器/DHT11/MQ135/OLED/GP2Y10/舵机/串口/ACS712/水流/风扇）
    │
    ▼
主循环（每轮约10ms）
    │
    ├── 串口下行处理：读ESP32发来的数据
    │   ├── 匹配"KKVVV"格式 → 更新阈值变量（如 tr35 → TEMP_RED=35）
    │   └── 否则 → Serial_ProcessControlDownlink() 解析数字指令0~7
    │
    ├── 传感器采集（每1秒）
    │   ├── DHT11 → 温湿度
    │   ├── GP2Y10 → PM2.5浓度
    │   ├── MQ135 → 空气质量指数
    │   ├── YF-S401 → 水流量(L/min)
    │   └── ACS712 → 电流(mA，滤除<50mA和≥5000mA噪声)
    │
    ├── 自动控制判断（每1秒） → 规则引擎
    │   ├── 风扇控制（温度/PM2.5/湿度条件）
    │   ├── 舵机控制（温度/湿度条件 → 开窗/关窗/加湿角度）
    │   ├── LED状态仲裁（红/黄/绿按优先级）
    │   └── 蜂鸣器报警（PM2.5重度污染→滴滴滴）
    │
    ├── OLED显示（每300ms，双页3秒切换）
    │
    ├── 蜂鸣器驱动（PM2.5超标时产生间歇波形）
    │
    └── JSON上报：Serial_SendSensorToESP() 每1秒发送
```

**手动覆盖机制：** 上位机远程控制（指令0~7）设置标志位 `manual_override_xx`，自动规则检测到标记后跳过该执行器的自动控制。

#### 4.3.2 serial.c — 串口通信

- 硬件：USART1，PA9(TX) + PA10(RX)，115200-8N1
- 接收：RXNE中断 + 256字节环形缓冲
- 发送：阻塞式逐字节发送

**上行协议（STM32 → ESP32）：**

```json
{"t":26,"h":60,"pm":35,"aq":52,"f":0.00,"i":0}\r\n
```

| 字段 | 含义 | 值域 | 来源 |
|------|------|------|------|
| t | 温度 | 0~50°C | DHT11 |
| h | 湿度 | 0~99%RH | DHT11 |
| pm | PM2.5浓度 | 0~500 µg/m³ | GP2Y10 |
| aq | 空气质量指数 | 0~500 ppm | MQ135 |
| f | 水流量 | 0~99.99 L/min | YF-S401 |
| i | 电流 | 0~5000 mA | ACS712 |

**下行协议（ESP32 → STM32）：**

| 格式 | 示例 | 说明 |
|------|------|------|
| 单数字 `'0'~'7'` | `"4"` | 数字指令 |
| JSON `{"code":N}` | `{"code":4,"name":"风扇开启"}` | JSON指令 |
| JSON `{"ctl":N}` | `{"ctl":4}` | 控制指令 |
| JSON `{"th":{...}}` | `{"th":{"tr":35,"tf":30}}` | 阈值批量修改 |

**数字指令映射：**

| 指令 | 功能 |
|------|------|
| 0 | 蜂鸣器关闭（远程控制关） |
| 1 | 蜂鸣器打开（远程控制开） |
| 2 | 舵机90°（开窗/WINDOW_OPEN） |
| 3 | 舵机0°（关窗/HOME） |
| 4 | 风扇开 |
| 5 | 风扇关 |
| 6 | LED报警模式开启 |
| 7 | LED报警模式关闭 |

**阈值修改命令：**

| 命令键 | 含义 | 默认值 | 对应变量 |
|--------|------|--------|----------|
| tr | 温度红灯阈值(°C) | 32 | TEMP_RED |
| tc | 温度下限(°C) | 18 | TEMP_COLD |
| hw | 湿度上限(%RH) | 70 | HUMI_WET |
| hd | 湿度下限(%RH) | 30 | HUMI_DRY |
| ph | PM2.5重度阈值(µg/m³) | 75 | PM25_HEAVY |
| aq | 空气质量阈值(ppm) | 100 | AQ_MAX |
| ci | 电流阈值(mA) | 500 | CUR_MAX |
| fl | 水流量阈值(L/min) | 5 | FLW_MAX |

格式：`tr35\n`（两字母键 + 1~5位数字 + 换行）

#### 4.3.3 ACS712.c — 电流传感器

- 型号：ACS712ELCTR-05B-T（5A量程，185mV/A灵敏度）
- 引脚：PA0 ADC1_IN0
- 采样策略：300次采样 × 68µs ≈ 覆盖完整50Hz工频周期
- 零漂校准：上电120次采样取平均，超出范围回退到标称2.5V
- 低通滤波：上升alpha=0.35，下降alpha=0.12（快速响应噪声衰减）
- 死区：100mA以下归零

#### 4.3.4 DHT11.c — 温湿度传感器

- 通信协议：单总线（PB13）
- 时序：主机发18ms低电平起始信号 → 传感器响应80µs低+80µs高 → 40bit数据
- 数据格式：8bit湿度整数 + 8bit湿度小数 + 8bit温度整数 + 8bit温度小数 + 8bit校验和
- 防卡死：两个while等待循环均设有超时计数（retry>200退出）

#### 4.3.5 GP2Y10.c — PM2.5粉尘传感器

- 型号：GP2Y1014AU0F
- 引脚控制：PB12（LED驱动，低电平点亮）
- ADC输入：PA2 ADC1_IN2
- 采样时序：LED亮 → 280µs后采样ADC → 40µs后关LED → 9680µs等待（完整10ms周期）
- 公式：`dust_density(µg/m³) = (sensor_mv - 420) × 0.20`
  - sensor_mv = ADC读数经11倍电压缩放补偿（模块分压）
- 滑动窗口滤波：10点平均
- 上限钳位：500 µg/m³
- 零点基准(NO_DUST_MV)：420mV（需微调校准）

#### 4.3.6 MQ_135.c — 空气质量传感器

- 引脚：PA1 ADC1_IN1
- 供电：5V（加热），采样电阻10kΩ
- PPM计算：ADC值(0~4095)线性映射到0~500指数
  - 注：非真实化学浓度标定，为简化线性映射
- 指数滤波：alpha=0.2

#### 4.3.7 waterFlow.c — 水流传感器

- 型号：YF-S401（霍尔效应）
- 引脚：PA5 EXTI5上升沿中断
- 标定：98脉冲 = 1升水
- 中断处理：ISR仅做 `pulse_count++`（整数累加，不涉及浮点）
- 定时器：TIM2每1秒转存脉冲计数并清零
- 流量计算：`flow = pulses_per_sec / 98 × 60` → L/min
- 累加功能：总用水量累计 `total_flow_L`

#### 4.3.8 servo.c — 舵机控制

- 型号：SG90
- PWM：TIM3_CH1 (PA6)，50Hz（周期20ms）
- 脉宽映射：`500 + angle × 2000/180` → 500~2500µs
- 角度范围：0~180°
- 预设角度：HOME=0°, WINDOW_OPEN=45°, HUMIDIFIER_OPEN=90°

#### 4.3.9 led.c — 三色LED

- 红灯 PA15（需在RCC_APB2PeriphClockCmd后调用GPIO_PinRemapConfig释放JTAG）
- 绿灯 PA11
- 黄灯 PA12
- 全部高电平点亮

#### 4.3.10 buzzer.c — 有源蜂鸣器

- 引脚：PB14
- 低电平响（GPIO_ResetBits=ON, GPIO_SetBits=OFF）

#### 4.3.11 motor.c — 风扇控制（原电机驱改）

- 风扇控制：PB6，低电平转、高电平停
- 保留原电机驱动代码（L298N接口PB0/PB1，TIM4 PWM），但当前功能由风扇替代

#### 4.3.12 OLED.c — OLED显示（调试用）

- 尺寸：0.96"，128×64，SSD1306控制器
- 接口：软件模拟I2C（PB8=SCL, PB9=SDA），地址0x78
- 字库：8×16像素（4行×16字符）
- **角色定位**：开发调试时查看实时数据，部署后无实际作用，保留不删
- 显示内容双页：
  - 第1页：传感器实时值（温度、湿度、PM2.5、AQI、流量、电流）
  - 第2页：当前阈值（tr/tc/hw/hd/ph/aq/ci/fl）
  - 每页停留3秒交替
- 切页策略：不清全屏，只覆写4行数据区（写16空格×4行防闪烁）

#### 4.3.13 pwm.c — PWM初始化

| PWM通道 | 定时器 | 引脚 | 频率 | 用途 |
|---------|--------|------|------|------|
| TIM2_CH1 | TIM2 | PA0 | 100Hz | LED调光（预留） |
| TIM3_CH1 | TIM3 | PA6 | 50Hz | 舵机 |
| TIM4_CH1 | TIM4 | PB6 | 20kHz | 电机调速（原电机预留） |

#### 4.3.14 Delay.c — 延时函数

- SysTick中断实现
- `Delay_us(us)`：72MHz写入递减循环
- `Delay_ms(ms)`：调用Delay_us(1000)
- `Delay_s(s)`：循环Delay_ms(1000)

#### 4.3.15 ADCX.c — ADC辅助

- 通用ADC1初始化，支持多通道切换
- `ADCX_Get(ch)`：指定通道单次采集
- 采样时间：55.5周期

---

## 五、联动规则引擎

所有联动逻辑实现在 `main.c` 主循环中，每1秒执行一次规则判断。

### 5.1 温度-风扇-舵机联动

| 条件 | 风扇 | 舵机 | LED | 优先级 |
|------|------|------|-----|--------|
| T ≤ 18°C（过冷） | — | 关窗(0°) | 黄灯 | 低 |
| 18 < T ≤ 30°C（舒适） | 关 | 维持 | 绿灯 | 最低 |
| 30 < T ≤ 34°C（偏热） | 开 | — | 黄灯 | 中 |
| T > 34°C（酷热） | 开 | 开窗(45°) | 红灯 | 高 |

### 5.2 湿度-舵机联动

| 条件 | 舵机 | LED |
|------|------|-----|
| H < 30%（过干） | 关窗(减少通风流失水分) | 黄灯 |
| 30 ≤ H ≤ 70%（正常） | — | — |
| H > 70%（过湿） | 开窗(排湿) | 黄灯 |

> 当温度>34°C时湿度规则优先级降低，优先散热。

### 5.3 PM2.5-蜂鸣器-风扇-舵机联动（核心安全规则）

| 条件 | 风扇 | 舵机 | LED | 蜂鸣器 |
|------|------|------|-----|--------|
| ≤35 µg/m³ | — | — | 绿灯 | — |
| 35~75 µg/m³ | 开(排风) | 关窗(防室外进入) | 黄灯 | — |
| >75 µg/m³ | 开(排风) | 关窗(防室外进入) | 红灯 | 滴滴滴(200ms响+300ms停) |
| 回落≤50 µg/m³ | — | — | — | 停止(含5µg回差) |

### 5.4 空气质量(MQ135)-风扇联动

| 条件 | 风扇 | LED |
|------|------|------|
| ≤150 ppm | — | — |
| 150~300 ppm | 开(排风) | 黄灯 |
| >300 ppm | 开(排风) | 红灯 |

### 5.5 水流量-蜂鸣器联动（漏水检测）

| 条件 | 蜂鸣器 | LED |
|------|--------|-----|
| 流量>3 L/min（预警） | — | 黄灯闪烁 |
| 流量>5 L/min 持续3秒（报警） | 持续长响 | 红灯快闪（需上位机确认解除） |

### 5.6 电流-LED联动

| 条件 | LED |
|------|-----|
| I > 500mA | 黄灯 |
| I > 800mA | 红灯 + 蜂鸣器短鸣一次 |

### 5.7 LED状态仲裁总优先级

```
优先级1  漏水报警                    → 红灯快闪
优先级2  PM2.5 > 75 或 AQ > 300    → 红灯
优先级3  电流 > 800mA               → 红灯
优先级4  温度 > 34°C                → 红灯
优先级5  PM2.5 > 35 或 温度>30°C   → 黄灯
优先级6  温度<18°C 或 湿度异常     → 黄灯
优先级7  流量 > 3 L/min             → 黄灯闪烁
优先级8  全部正常                   → 绿灯
```

### 5.8 蜂鸣器模式

| 场景 | 模式 | 波形 | 可静音 |
|------|------|------|--------|
| PM2.5>75 | 间歇报警 | 200ms响+300ms停循环 | 可（夜间模式） |
| 漏水报警 | 持续报警 | 持续响 | 不可（需手动确认关） |
| 电流>800mA | 提示 | 100ms短响一次 | 可 |
| 系统启动 | 提示 | 200ms短响一次 | 可 |

---

## 六、ESP32 固件（Arduino IDE）

- 文件：`D:\AAA\esp32\stm32_to_esp32_uart_test\stm32_to_esp32_uart_test.ino`
- 开发环境：Arduino IDE

### 6.1 功能

- **串口桥接**：UART2（RX2=GPIO16, TX2=GPIO17）接STM32 USART1，115200-8N1
- **WiFi连接**：硬编码 `vivo` / `12345678`
- **MQTT客户端**：连接 `bemfa.com:9501`，ClientID `6525cbc01d2d408eb1b28ca77a134ebc`
- **上行转发**：每5秒将STM32的JSON数据发布到 `test001up` 主题，含阈值缓存
- **下行转发**：
  - 字符 '0'~'7' → 直发STM32
  - JSON `{"code":N}` / `{"cmd":N}` / `{"ctl":N}` → 提取数字转发
  - JSON `{"th":{...}}` → 拆解为 `key+value\n` 逐条转发 + 更新本地缓存

### 6.2 上发数据格式

```json
{"t":26,"h":60,"p":35,"a":52,"f":0.00,"i":0,"th_tr":32,"th_tc":18,"th_hw":70,"th_hd":30,"th_ph":75,"th_aq":100,"th_ci":500,"th_fl":5}
```

### 6.3 断线重连

- WiFi断连：调用 `setup_wifi()` 重连
- MQTT断连：loop中循环检查 `mqttClient.connected()` 并调用 `reconnect()`

---

## 七、Web看板（信息大屏）[v2 — 单页群众大屏]

- 路径：`D:\AAA\dashboard\`
- 技术栈：纯HTML + MQTT.js 5.10.3（已移除ECharts依赖）
- **角色定位**：安置点公共大屏 → 面向群众展示当前环境状态

### 7.1 设计原则

- **3秒传达核心信息**：整屏通过颜色（绿/黄/红）直观反映系统状态
- **零学习成本**：大字数值 + Emoji图标 + 简单文字标签
- **异常全屏覆盖**：报警时遮罩覆盖整个屏幕，不依赖边栏文字
- **单页设计**：信息一屏展示，无需轮播/翻页

### 7.2 页面结构

```
┌──────────────────────────────────────────────────────────────┐
│  🏕 安置点 · 一号营地          🟢 状态正常     14:30:25     │
│     智慧水电与环境监测系统                       2026年5月14日│
├──────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐│
│  │  ✓  一切正常                                            ││
│  │      所有环境指标均在安全范围内                          ││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐│
│  │ 🌡   │  │ 💧   │  │ 🌫   │  │ 🍃   │  │ ⚡   │  │ 🚰   ││
│  │ 温度  │  │ 湿度  │  │PM2.5 │  │空气指│  │用电  │  │供水  ││
│  │ 26°C │  │ 60%  │  │ 35   │  │  52  │  │ 120  │  │ 0.5  ││
│  │ 正常  │  │ 正常  │  │ 正常 │  │ 正常 │  │ 正常 │  │ 正常 ││
│  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘│
│                                                              │
│  ℹ 系统运行正常，所有传感器在线                               │
├──────────────────────────────────────────────────────────────┤
│  ● MQTT 已连接    上次更新 14:30:25    v2.0 | 应急管理大学   │
└──────────────────────────────────────────────────────────────┘
```

### 7.3 三态安全体系

| 状态 | 顶栏圆点 | 横幅色调 | 卡片顶条 | 报警遮罩 |
|------|----------|----------|----------|----------|
| 正常 🟢 | 绿色常亮 | 绿色边框 | 绿色 | 无 |
| 注意 🟡 | 黄色脉冲 | 黄色边框 | 黄色 | 半透明黄色遮罩 |
| 危险 🔴 | 红色脉冲 | 红色闪烁边框 | 红色闪烁 | 半透明红色闪烁遮罩 |

### 7.4 断线检测

- 超过30秒未收到MQTT数据 → 状态横幅变为黄色，显示"数据异常"
- 信息条提示"长时间未收到数据，请检查系统连接"
- MQTT重连后自动恢复正常

### 7.5 阈值设定（硬编码于app.js，与STM32默认值对齐）

| 指标 | 正常 | 预警 | 危险 |
|------|------|------|------|
| 温度 | 19~29°C | 30~33°C 或 ≤18°C | ≥34°C |
| 湿度 | 31~69% | ≤30% 或 ≥70% | — |
| PM2.5 | ≤34 µg/m³ | 35~74 µg/m³ | ≥75 µg/m³ |
| 空气指数 | ≤149 ppm | 150~299 ppm | ≥300 ppm |
| 电流 | ≤499 mA | 500~799 mA | ≥800 mA |
| 水流量 | ≤2.9 L/min | 3~4.9 L/min | ≥5 L/min |

### 7.6 与旧版的区别

| 项目 | 旧版 | 新版（v2） |
|------|------|-----------|
| ECharts仪表盘 | 6个 | 全部移除 |
| 折线图 | 4个 | 全部移除 |
| 阈值栏 | 顶部显示8个阈值 | 移除 |
| 报警面板 | 右侧边栏 | 全屏遮罩覆盖 |
| 页面模式 | 单页 | 单页（无轮播） |
| 卡片设计 | 纯数字 | 图标+数值+状态顶条 |
| 信息密度 | 高（工程风格） | 中（群众风格） |
| 断线检测 | 仅MQTT状态文字 | 30秒超时→横幅变黄 |

### 7.3 配置（config.js）

```javascript
mqttUrl: 'wss://bemfa.com:9504/wss'  // WebSocket Secure
topic: 'test001up'
cacheMinutes: 1440  // 24小时缓存
```

各指标量程和告警阈值配置在 `WARN_ORANGE` / `DANGER_RED` 中。

### 7.4 依赖

- ECharts 5.5.1（CDN）
- MQTT.js 5.10.3（CDN）

---

## 八、Qt桌面端（管理后台）

- **角色定位**：管理站办公室 → 工作人员进行后台管理

- 路径：`D:\AAA\Code\QT_Code\System_UI\`
- 构建工具：qmake（.pro文件）
- 框架：Qt 5+（含 QtCharts, QtMqtt, QtSql, QtNetwork）

### 8.1 登录与认证

| 功能 | 详情 |
|------|------|
| 登录 | 用户名+密码，记住密码（加密存储） |
| 注册 | 密码强度检测，同意条款 |
| 验证码 | 模拟模式（123456有效） |
| 用户角色 | admin / user |
| 预设用户 | admin/Admin@123, test/Test@123, alice/Alice@123 |
| 数据库 | SQLite `D:/System_UI_Data/system_ui.sqlite` |

### 8.2 主界面（五页面导航）

**① 实时监控（首页）：**
- 6个指标卡片：温度、湿度、水流、空气质量、PM2.5、电流
- 每个卡片含彩色状态点（绿=正常/黄=注意/红=报警）
- 实时折线图（三条Y轴：流量/PM2.5/电流）
- 累积统计：kWh、Ah、总用水量(L)
- 模拟数据回退（断网时自动切换）

**② 历史数据：**
- 归一化堆叠柱状图（6指标百分比）
- 时间范围：1小时 / 24小时 / 7天
- 统计摘要 + 趋势文字
- Excel导出（XLSX格式，依赖PowerShell压缩）

**③ 报警管理：**
- 报警历史表格（时间、传感器、内容）
- 数据源：SQLite alarm_info表
- 30秒防抖（避免重复报警）

**④ 设备管理：**
- 设备卡片网格（悬停放大动画1.08x）
- 远程控制按钮（蜂鸣器/风扇/窗户/报警灯）→ 发MQTT指令0~7
- 远程阈值设置对话框（11个参数批量下发）
- 远程执行日志

**⑤ 系统设置：**
- 账户信息展示/编辑
- 切换账号

### 8.3 MQTT通信

- 连接：bemfa.com:9501，ClientID `6525cbc01d2d408eb1b28ca77a134ebc`
- 订阅：test001up
- 发布控制：`{"code":4,"name":"风扇开启"}`
- 发布阈值：`{"th":{"tr":35,"tf":30,...}}`
- 解析两格式：扁平JSON `{"t","h","p","a","f","i"}` 和旧嵌套格式

### 8.4 数据库（SQLite）

所有表位于 `D:/System_UI_Data/system_ui.sqlite`：

| 表名 | 用途 |
|------|------|
| users | 用户认证 |
| data | 简化核心指标 |
| device_abnormal_records | 设备离线追踪 |
| device_info | 设备注册表 |
| remote_exec_logs | 远程指令执行日志 |
| alarm_info | 报警历史记录 |

### 8.5 已知Qt端问题与修复

| 问题 | 状态 | 说明 |
|------|------|------|
| 阈值弹窗每次打开重置为默认值 | ✅ 已修复 | 改用成员变量缓存当前值，打开时恢复上一次设置 |
| 阈值下发二次确认弹窗 | ✅ 已修复 | 去掉冗余的QMessageBox确认，点确认直接下发 |
| 离线时仍可点击控制按钮 | ✅ 已修复 | publishDeviceCode和onSendRemoteControlClicked均检查MQTT状态，离线时弹出警告并取消发送 |
| 图表变量命名与实际数据不符 | ✅ 已修复 | 变量名旁加注释说明实际用途 |
| 遗留widget.cpp | ⚠️ 废弃代码 | 旧登录表单，功能被LoginDialog取代 |
| XLSX依赖PowerShell | ⚠️ 未修复 | Windows限定，跨平台需替换为QZipWriter |

- 采样写入：实时MQTT数据→SQLite `data` 表
- 累积计算：基于电流×220V×时间→kWh，电流×时间→Ah
- Excel导出：手动构建OOXML XML → PowerShell Compress-Archive → .xlsx

### 8.6 安全存储

- 凭据加密：XOR + 机器硬件ID(SHA-256)作为密钥
- 存储位置：QSettings（注册表/INI）

---

## 九、通信协议汇总

### 9.1 数据流示意图

```
采集方向（传感器→云端）：
  STM32 传感器  ──JSON──▶  ESP32  ──MQTT──▶  巴法云  ──MQTT──▶  Web看板
                                                         ──MQTT──▶  Qt桌面端

控制方向（云端→执行器）：
  Qt桌面端  ──MQTT──▶  巴法云  ──MQTT──▶  ESP32  ──UART──▶  STM32 执行器
  Web看板   ──MQTT──▶  巴法云  ──MQTT──▶  ESP32  ──UART──▶  STM32 执行器
```

### 9.2 JSON上行格式（STM32→ESP32→云端）

```json
{"t":26,"h":60,"pm":35,"aq":52,"f":0.00,"i":0}
```

ESP32在此基础上追加阈值字段后转发：

```json
{"t":26,"h":60,"p":35,"a":52,"f":0.00,"i":0,"th_tr":32,"th_tc":18,"th_hw":70,"th_hd":30,"th_ph":75,"th_aq":100,"th_ci":500,"th_fl":5}
```

### 9.3 下行命令格式（云端→ESP32→STM32）

| 格式 | 示例 | 说明 |
|------|------|------|
| 单字符 | `"4"` | 数字指令直接转发 |
| JSON指令 | `{"code":4,"name":"风扇开启"}` | 提取code字段 |
| 阈值批量 | `{"th":{"tr":35,"tf":30}}` | 拆解为逐条转发 |
| 阈值单条 | `"tr35"` | 直接在ESP32中构造 |

---

## 十、阈值系统

### 10.1 阈值列表

| 变量名 | 默认值 | 含义 | 可远程修改 | 命令键 |
|--------|--------|------|-----------|--------|
| TEMP_RED | 32 | 温度红色报警上限(°C) | ✓ | tr |
| TEMP_COLD | 18 | 过冷阈值(°C) | ✓ | tc |
| TEMP_FAN_ON | 30 | 风扇开启温度(°C) | — | — |
| TEMP_OK_LO | 22 | 温度舒适区下限(°C) | — | — |
| TEMP_OK_HI | 28 | 温度舒适区上限(°C) | — | — |
| HUMI_WET | 70 | 湿度过高阈值(%RH) | ✓ | hw |
| HUMI_DRY | 30 | 湿度过低阈值(%RH) | ✓ | hd |
| HUMI_OK_LO | 15 | 湿度正常下限(%RH) | — | — |
| HUMI_OK_HI | 75 | 湿度正常上限(%RH) | — | — |
| PM25_GOOD | 35 | PM2.5良好上限(µg/m³) | — | — |
| PM25_HEAVY | 75 | PM2.5重度污染(µg/m³) | ✓ | ph |
| AQ_MAX | 100 | 空气质量阈值(ppm) | ✓ | aq |
| CUR_MAX | 500 | 电流阈值(mA) | ✓ | ci |
| FLW_MAX | 5 | 水流量阈值(L/min) | ✓ | fl |

### 10.2 阈值存储

- **运行时**：全部在 STM32 RAM 中（`main.c` 顶部 `static uint16_t` 变量）
- **持久化**：目前无，STM32重启后恢复默认值
- **远程修改**：通过串口命令实时更新RAM变量

> **已知限制**：STM32 未使用 Flash 模拟 EEPROM 保存阈值，断电丢失自定义配置。

### 10.3 远程设置流程

```
Qt桌面端 → MQTT发布 {"th":{"tr":35,"tf":30}}
  → ESP32收到，解析th对象
  → 逐条转发 UART: "tr35\n" 和 "tf30\n"
  → STM32 serial中断接收，主循环 Serial_ParseThresholdCmd() 解析
  → 更新对应全局变量
  → 下次规则判断使用新阈值
```

---

## 十一、执行器控制总表

| 执行器 | 控制方式 | 自动规则来源 | 远程指令 | 手动覆盖 |
|--------|----------|-------------|----------|----------|
| 风扇 | PB6 GPIO | 温度>30 / PM2.5>35 / AQ>150 / 湿度>70 | 4=开, 5=关 | ✓ |
| 舵机 | PA6 PWM | 温度/湿度联动 → 0°/45°/90° | 2=开窗, 3=关窗 | ✓ |
| 红灯 | PA15 GPIO | LED仲裁 → PM2.5/温度/电流/漏水 | 6=报警开, 7=报警关 | ✓ |
| 绿灯 | PA11 GPIO | LED仲裁 → 全部正常 | — | — |
| 黄灯 | PA12 GPIO | LED仲裁 → 预警状态 | — | — |
| 蜂鸣器 | PB14 GPIO | PM2.5>75(滴滴滴) / 漏水(持续) | 0=关, 1=开 | ✓ |
| OLED | PB8/PB9 I2C | 双页循环显示 | — | — |

---

## 十二、已知问题与待办

### 12.1 硬件问题

| 问题 | 状态 | 说明 |
|------|------|------|
| GP2Y10零点校准 | ⏳ 待验证 | NO_DUST_MV=420，需实测微调 |
| 红灯微亮(PA15 JTAG) | ✅ 已修复 | `GPIO_Remap_SWJ_JTAGDisable` 释放PA15 |

### 12.2 软件问题

| 问题 | 状态 | 说明 |
|------|------|------|
| Delay.h 路径不匹配 | ⚠️ 待确认 | main.c `#include "Delay.h"` 在Hardware/下找不到，实际在System/ |
| PWM函数名误导 | ✅ 已标记 | `PWM_Servo_SetCompare2()` 实际使用 TIM3 OC1 而非 OC2 |
| ACS712漂移 | ✅ 已修复 | 300采样+68µs覆盖50Hz周期，100mA死区 |
| DHT11卡死风险 | ✅ 已修复 | while循环加超时计数(retry>200退出) |
| GP2Y10读数错误 | ✅ 已修复 | 改用厂商公式+LED极性修正 |
| waterFlow编码乱码 | ✅ 已修复 | 重写文件，ISR仅整数累加 |
| OLED切页闪烁 | ✅ 已修复 | 不清全屏，仅覆写4行数据区 |
| LED_RED_FORCE_ON宏 | ⚠️ 固定0 | 条件编译无意义，代码始终定义 |
| 阈值断电丢失 | ❌ 未解决 | 无Flash EEPROM存储 |
| 无看门狗 | ❌ 未解决 | 无独立看门狗(IWDG)或窗口看门狗(WWDG) |

### 12.3 ESP32问题

| 问题 | 状态 | 说明 |
|------|------|------|
| WiFi断线重连 | ✅ 已修复 | 增加WiFi状态检测，断WiFi后调setup_wifi() |
| 断网数据存储 | 📋 计划中 | 计划LittleFS缓存未上传数据 |
| WiFi密码硬编码 | ⚠️ 未解决 | `vivo`/`12345678`，需修改源码 |

### 12.4 Qt端问题

| 问题 | 状态 | 说明 |
|------|------|------|
| 遗留widget.cpp | ⚠️ 废弃代码 | 旧登录表单，功能被LoginDialog取代 |
| on_btn_signIn_clicked空实现 | ⚠️ 废弃 | 无功能 |
| XLSX依赖PowerShell | ⚠️ Windows限定 | 跨平台需替换 |
| SQLite连接管理 | ⚠️ 可优化 | 部分操作未使用连接池 |

### 12.5 待办清单

1. **GP2Y10零点校准** — NO_DUST_MV=420，需实测微调
2. **ESP32断网存储** — LittleFS断网写文件，联网后补传
3. **阈值掉电保存** — STM32 Flash模拟EEPROM存储阈值
4. **看门狗** — 添加IWDG防止主循环卡死
5. **情景模式** — 夜间/静音/紧急模式切换（需扩展协议）
6. **规则启停** — 每条联动规则独立开关（需扩展协议）

---

## 十三、项目目录总览

```
D:\AAA\
 ├── FinalSystem\                    ← STM32固件 (Keil MDK)
 │    ├── Hardware\                  ← 各模块驱动
 │    │    ├── ACS712.c/.h           ← 电流传感器
 │    │    ├── DHT11.c/.h            ← 温湿度传感器
 │    │    ├── GP2Y10.c/.h           ← PM2.5粉尘传感器
 │    │    ├── MQ_135.c/.h           ← 空气质量传感器
 │    │    ├── waterFlow.c/.h        ← 水流传感器
 │    │    ├── OLED.c/.h             ← OLED显示
 │    │    ├── servo.c/.h            ← 舵机控制
 │    │    ├── led.c/.h              ← 三色LED
 │    │    ├── buzzer.c/.h           ← 蜂鸣器
 │    │    ├── motor.c/.h            ← 风扇控制
 │    │    ├── serial.c/.h           ← 串口通信
 │    │    ├── pwm.c/.h              ← PWM初始化
 │    │    └── Delay.c/.h            ← 延时（实际在System/）
 │    ├── System\
 │    │    ├── Delay.c/.h            ← 实际延时函数
 │    │    └── ADCX.c/.h             ← ADC辅助
 │    ├── User\
 │    │    └── main.c                ← 主循环/规则引擎
 │    ├── Start\                     ← 启动文件
 │    └── Library\                   ← 标准外设库
 │
 ├── esp32\                          ← ESP32固件 (Arduino)
 │    └── stm32_to_esp32_uart_test\
 │         └── stm32_to_esp32_uart_test.ino
 │
 ├── dashboard\                      ← Web看板
 │    ├── index.html
 │    ├── app.js
 │    ├── config.js
 │    └── css/
 │
 └── Code\QT_Code\System_UI\         ← Qt桌面端
      ├── main.cpp
      ├── mainwindow.h/.cpp/.ui
      ├── logindialog.h/.cpp
      ├── registerdialog.h/.cpp
      ├── authservice.h/.cpp
      ├── mqtt.h/.cpp
      ├── databasemanager.h/.cpp
      ├── securestore.h/.cpp
      └── widget.h/.cpp/.ui          ← (废弃)
```
