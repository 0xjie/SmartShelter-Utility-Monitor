# 第2章 需求分析

## 2.1 系统需求分析概述

灾后临时安置点在实际运行过程中，既面临环境监测需求，也面临临时供水、供电状态管理需求。与一般室内监测系统相比，该场景对系统的要求更集中体现在三个方面：一是需要持续获取温湿度、空气质量、颗粒物、电流和水流等关键指标；二是需要在现场条件变化时及时触发联动控制，以降低环境异常和资源异常带来的风险；三是需要在不依赖复杂服务器部署的条件下，实现多端协同监测、远程干预和本地数据留存。

从本系统的实际构成看，系统并未采用传统“设备端 + 独立后端 + Web 服务端”的三层部署方式，而是形成了以 STM32 为现场控制核心、ESP32 为通信网关、Qt 客户端为管理中心、Web 看板为轻量展示终端、SQLite 为本地数据中心的实现结构。因此，本章需求分析必须立足于当前真实系统能力，对功能需求、非功能需求、数据需求、接口需求和运行约束进行归纳，为后续总体设计与详细实现奠定基础。

为了更直观地说明本系统从现场感知、状态判定、消息传输到多端展示与求助交互之间的完整业务闭环关系，本章在需求分析概述之后给出系统业务流程图，以便为后续功能需求和接口需求分析提供统一参照，如图2-1所示。

## 2.2 功能需求分析

### 2.2.1 数据采集与现场感知需求

本系统首先需要具备多源异构数据采集能力。结合现场硬件构成，系统应能够连续获取温湿度、空气质量、PM2.5、电流和水流等监测信息，并将其转换为统一的工程量表达形式，用于后续风险判定、资源统计和界面显示。由于不同传感器输出特性差异较大，系统不仅要完成原始数据读取，还应具备基本的数据预处理能力，包括异常值抑制、单位换算和资源量推导等。

除基础采样外，系统还需要对现场状态进行直接感知与表达。具体而言，STM32 端不仅要形成可上传的数据报文，还要在本地完成 OLED 信息显示和故障识别，使系统在上位端暂时不可用时仍能保留最基本的现场可观测能力。这一需求决定了系统不能将全部计算压力放到上位端，而必须在控制端完成初步状态组织。

### 2.2.2 分级预警与自动联动需求

单纯的数据展示无法满足安置点实际运行需要，系统必须具备异常识别与自动响应能力。从实际实现看，本系统需要将监测状态划分为正常、预警和告警三个等级，并依据不同等级执行差异化响应。也就是说，系统不仅要知道“是否异常”，还要进一步区分异常程度，并将其映射到提示方式与控制动作中。

在此基础上，系统还应具备多指标组合联动能力。由于环境风险往往不是由单一指标造成，例如粉尘异常与空气质量恶化可能共同指向烟雾污染风险，水流异常与电流异常可能共同指向供给或管路问题，因此系统需要在单项判定之外，识别组合异常并提升整体风险响应等级。相应地，风扇、舵机、蜂鸣器和指示灯等执行器应能够依据风险等级或组合条件进行联动控制。

### 2.2.3 远程通信与控制需求

本系统并非仅用于本地独立运行，还需要支持上位端远程感知和远程干预。因此，系统必须建立一条从现场控制端到管理终端的双向通信链路。根据当前真实实现，该链路由 STM32 与 ESP32 之间的串口通信，以及 ESP32 与 MQTT 服务之间的无线消息通信共同组成。系统需要保证现场状态能够持续上报，同时允许管理端向现场下发控制命令。

远程控制需求主要包括三类内容：一是执行器开关与动作控制，二是阈值参数下发，三是恢复默认配置与模式切换。由此可以看出，系统远程控制并非只面向单个设备操作，而是同时承担参数维护与运行模式干预功能。对于安置点管理场景而言，这种设计有助于管理人员在现场不便直接接触设备时，依然能够完成必要控制。

### 2.2.4 多端展示与交互需求

从系统当前实现看，信息展示并不局限于单一终端。Qt 客户端承担管理型应用角色，需要提供实时监控、水电数据、历史记录、告警信息、设备信息和系统设置等功能区域，使管理人员能够集中查看系统运行状态，并执行参数设置和远程控制。Web 看板则承担轻量展示角色，重点突出环境指标、资源状态、执行器状态、报警提示和历史趋势等信息，以适应浏览器端的快速访问需求。

除展示功能外，系统还需要具备基础交互能力。桌面端需要支持登录、注册、阈值修改、设备控制、历史导出和告警处理等操作；Web 端则需要支持一键求助消息上报。由此可见，多端需求并非简单复制界面，而是在统一数据来源基础上面向不同使用场景进行功能分工。

### 2.2.5 本地数据存储与事件管理需求

由于系统当前并未部署独立后端数据库服务，因此必须在本地建立完整的数据留存与业务记录机制。首先，系统需要保存连续的历史监测数据，为后续趋势分析、历史查询和数据导出提供基础。其次，系统需要记录报警事件和求助事件的发生、持续与结束过程，使告警处理不再停留于瞬时提示层面，而具备可追溯性。

此外，远程控制行为也需要被记录，以满足后续审计和问题追踪需求。对于通信中断等运行异常，系统同样需要形成独立记录，避免把“设备离线”误解释为“状态正常”。这说明本系统的数据需求不仅包括数值存储，还包括事件存储、日志存储和运行状态存储。

### 2.2.6 用户认证与基础管理需求

虽然本系统未引入独立认证服务器，但桌面端仍需具备基本的账号管理能力，以满足多人使用场景下的身份区分需求。结合当前实现，系统至少需要支持用户登录、用户注册、角色区分和账户信息修改等功能，使不同使用者能够以独立身份进入管理界面。

需要说明的是，当前系统中的权限需求主要体现在“身份识别”和“角色区分”层面，而非复杂的细粒度权限控制。也就是说，系统要求具备基本的用户边界，但不要求构建完整的企业级权限管理体系。这一需求定位与本科毕业设计系统的应用规模相一致。

## 2.3 非功能需求分析

### 2.3.1 实时性需求

灾后安置点环境和资源状态具有明显的动态性，因此系统应具备较好的实时响应能力。对于现场控制端而言，系统需要在稳定周期内完成数据采集、状态判定和本地联动，避免由于任务调度不稳定造成响应滞后。对于通信链路而言，现场节点上报的数据应在较短时间内到达 Qt 客户端和 Web 看板，使管理人员能够及时发现变化。

从真实实现出发，系统的实时性需求并不追求毫秒级控制，而是强调“秒级感知、近实时展示、及时联动”。这种需求定位与环境监测和资源管理场景相匹配，能够兼顾单片机资源限制、无线通信开销和上位端展示效率。

### 2.3.2 稳定性需求

系统在实际部署过程中可能遇到网络波动、临时断连、传感器异常和人工干预遗留等问题，因此稳定性是系统必须满足的重要非功能需求。首先，现场控制应尽量独立于上位端存在，即使网络短时中断，STM32 仍需保持本地采集与联动能力。其次，网关和客户端在通信中断后应具备自动恢复机制，避免系统因短时掉线而长期失效。

同时，人工控制模式需要具备超时回退能力，以避免执行器长期停留在非自动状态。这一需求直接对应当前系统中对手动模式超时恢复的设计，也体现了系统对长时间运行风险的防范意识。

### 2.3.3 可靠性需求

可靠性需求主要体现在数据可靠和状态可靠两个层面。数据可靠要求系统上传的监测信息应尽可能保持结构完整，避免因中间节点重组数据而引发含义偏差；状态可靠则要求系统对预警、告警、离线和求助等关键事件形成明确记录，避免事件只在瞬时显示后丢失。

从实际结构看，系统通过 STM32 统一输出结构化报文，ESP32 尽量不重构业务数据，Qt 直接对现场判定结果进行展示与存储，这一设计本身就是为提高系统一致性和可靠性服务的。因此，本系统的可靠性需求不仅是“不出错”，更重要的是“多端一致、事件可追、状态可解释”。

### 2.3.4 轻量部署需求

本课题系统面向的是临时安置场景，而非大型固定基础设施，因此部署复杂度应尽量受控。结合真实实现，系统不依赖独立应用服务器和集中式数据库服务器，桌面端即可完成业务管理和本地存储，Web 看板通过 MQTT WebSocket 直接获取实时数据，历史数据则通过导出脚本转为静态文件供浏览器加载。

这意味着系统的非功能需求中还包括“轻量部署、低维护成本和快速落地”三项要求。该需求直接影响了技术路线选择，也是系统采用 Qt + SQLite + MQTT 组合而未引入更重型后端架构的重要原因。

## 2.4 数据需求分析

### 2.4.1 监测数据需求

本系统首先需要处理环境类数据和资源类数据两大类监测信息。环境类数据主要反映临时安置点的空气和气候状态，包括温度、湿度、PM2.5 和空气质量；资源类数据主要反映临时水电使用状态，包括电流、水流以及据此推导出的累计消耗、剩余比例和预计可用时间等信息。

这类数据既要服务于实时监测，也要服务于历史统计，因此必须具备统一的时间标识和稳定的数据结构。只有在数据表达一致的前提下，Qt 客户端、Web 看板和本地数据库之间才能形成有效协同。

### 2.4.2 状态与控制数据需求

除监测值本身外，系统还需要处理状态类数据和控制类数据。状态类数据主要包括全局风险等级、分项风险等级、执行器状态、工作模式、报警信息、联动动作信息和故障信息等，用于表达“系统当前处于什么状态”。控制类数据则主要包括设备控制命令、模式切换命令、阈值设置命令和恢复类命令，用于表达“系统下一步要执行什么动作”。

这两类数据直接构成系统闭环控制的核心。没有状态数据，上位端无法正确理解现场运行情况；没有控制数据，系统又无法实现远程干预。因此，需求层面必须保证状态与控制数据在结构上可区分、在传输上可闭环。

### 2.4.3 业务记录数据需求

系统在运行过程中会产生大量业务性记录，主要包括历史监测记录、报警记录、求助记录、远程控制日志、设备离线记录和用户认证信息等。这些数据的共同特点是：它们不一定直接参与现场控制，但对于管理追溯、历史分析和运行维护具有重要意义。

其中，报警和求助信息需要具备生命周期表达能力，远程控制信息需要具备操作留痕能力，离线记录需要具备持续时间表达能力，用户信息则需要支撑基本认证流程。因此，本系统的数据需求并非只有“存值”，而是同时包括“存状态”“存事件”和“存过程”。

## 2.5 接口与通信需求分析

### 2.5.1 设备侧接口需求

从底层结构看，STM32 与 ESP32 之间需要建立稳定的串口通信接口。该接口不仅要传输周期性上报的状态数据，还要接收来自上位端转发的控制命令和阈值配置。因此，它必须同时支持上行报文传递与下行命令输入，并具备明确的消息边界，避免串口字节流环境下出现报文粘连和识别错误。

这一需求决定了底层接口设计不能仅满足单向调试输出，而必须具备双向业务通信能力。对于本系统而言，串口接口是现场控制层和网络通信层之间的唯一连接点，其稳定性直接影响整个系统链路。

### 2.5.2 MQTT消息接口需求

系统的广域通信需求通过 MQTT 机制实现。根据真实系统结构，主业务主题 `test001up` 同时承担实时状态上报与远程控制下发任务，辅助主题 `WebQT1` 承担一键求助消息传递任务。这说明系统的消息接口需求既包含数据分发，也包含事件上报和控制回传。

在消息内容组织上，系统需要用统一的 JSON 结构承载环境数据、资源数据、等级数据、控制状态、报警信息和故障信息等内容，以确保不同终端能够基于同一语义进行解析。与此同时，由于主业务主题是双向复用的，因此系统还需要具备基本的回环抑制能力，避免上行状态被误解释为下行控制。

### 2.5.3 多端接入需求

Qt 客户端和 Web 看板都需要接入同一消息体系，但二者承担的职责并不相同。Qt 端需要具备管理型接入能力，即在接收实时数据的同时，还要完成报警记录、数据库写入、远程控制和求助处理；Web 端则更强调轻量化接入，只负责实时展示和有限交互。

因此，系统接口需求不仅是“谁能接入”，还包括“不同终端如何在同一消息体系中分工协同”。这一需求也是系统能够在无独立后端服务条件下实现多端协同的重要前提。

## 2.6 约束条件分析

### 2.6.1 硬件与现场约束

本系统建立在 STM32F103 与 ESP32 的组合平台之上，现场控制资源有限，无法采用高性能嵌入式计算平台实现复杂调度和重型协议栈。因此，系统需要在有限存储空间、有限处理能力和有限通信带宽条件下完成多传感器采集、状态判定与报文组织。这决定了系统必须采用较轻量的控制逻辑和较明确的数据结构。

同时，灾后临时安置点的部署环境通常具有网络条件不稳定、供电条件受限和维护人员有限等特点，因此系统需要尽量减少对外部基础设施的依赖。这也是当前实现中保留本地自治控制、采用本地数据库存储的重要现实依据。

### 2.6.2 软件与架构约束

从软件结构看，当前系统并不存在独立后端服务程序，因此桌面端既是管理端，也是本地数据中心；Web 端也不能直接访问数据库，而需要通过 MQTT 获取实时数据，并通过导出文件方式读取历史统计结果。这一约束决定了系统在架构上必须优先考虑轻量、直接和可落地，而不是复杂的分布式服务拆分。

此外，系统当前使用单一主业务主题承载双向通信，这虽然降低了主题管理复杂度，但也要求网关侧具备必要的消息甄别能力。再加上本地用户认证采用轻量实现方式，说明本系统的软件约束更偏向于“实现完整业务闭环”，而不是“追求高复杂度平台能力”。这一定位与本科毕业设计系统的目标范围是相符的。

## 2.7 本章小结

本章基于系统实际实现结构，对灾后临时安置点智慧水电管理与环境监测系统的需求进行了分析。分析结果表明，系统需求主要集中在多源数据采集、三级预警与联动控制、双向通信、多端展示、本地数据存储和基础身份管理等方面；在非功能层面，则强调实时性、稳定性、可靠性和轻量部署能力。

与传统依赖独立服务器的物联网平台不同，本系统在真实实现中采用了“STM32 + ESP32 + MQTT + Qt + Web + SQLite”的轻量协同模式，因此其需求边界更加清晰，即在有限资源条件下完成监测、控制、展示和存储的基本闭环。上述需求分析为后续系统总体设计、详细实现与测试分析提供了明确依据。
