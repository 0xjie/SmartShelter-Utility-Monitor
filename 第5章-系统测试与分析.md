# 第5章 系统测试与分析

## 5.1 测试环境

本系统的测试围绕“现场控制端、通信网关、桌面管理端、Web 展示端和本地数据库”五个部分展开，测试对象与论文前述设计保持一致。硬件侧以 STM32F103 为主控制器，负责多类环境与资源传感数据采集、风险判定和执行器联动；ESP32 作为通信网关，与 STM32 通过 UART 串口连接，并通过 WiFi 接入 MQTT 云端消息服务。两者共同构成现场监测与控制节点。

通信侧采用巴法云 MQTT 服务作为消息中转平台，主业务主题为 `test001up`，用于承载实时监测数据与远程控制信息；辅助主题为 `WebQT1`，用于承载 Web 端一键求助消息。桌面端采用 Qt 6 框架构建，运行环境为 Windows 桌面系统，负责实时监测显示、阈值配置、远程控制、报警管理和数据存储。Web 看板通过浏览器访问，借助 MQTT WebSocket 接入同一消息体系，实现跨平台实时展示。数据库侧采用 SQLite 本地文件数据库，数据文件位于桌面端本地目录中，用于保存历史监测数据、报警信息、用户信息、设备离线记录和远程控制日志。

网络环境方面，系统测试基于局域网无线接入条件展开。STM32 本身不直接接入网络，由 ESP32 负责完成无线连接与消息转发；Qt 客户端和 Web 看板通过网络访问同一 MQTT 服务，从而构成“现场采集 - 网关转发 - 云端分发 - 多端接收”的完整测试链路。该测试环境能够较真实地反映本系统在灾后临时安置点智慧水电管理与环境监测场景下的实际运行方式。

## 5.2 功能测试

### 5.2.1 传感器采集功能测试

传感器采集测试的目标是验证系统是否能够持续、完整地获取温湿度、空气质量、PM2.5、电流和水流等关键监测数据，并将其正确上传至上位端。测试过程中，以 STM32 为核心采集节点，对各类传感器输出进行周期读取，并观察 Qt 客户端与 Web 看板是否能够同步显示对应数据。

测试现象表明，系统能够在固定采样周期内连续获取环境数据和资源数据，并通过 ESP32 上传至 MQTT 通道。Qt 客户端可实时显示各项监测值，Web 看板也能够同步刷新对应指标，说明传感器数据从采集端到展示端的链路保持畅通。对电流等容易受瞬时噪声影响的数据进行观察时，未出现连续的大幅异常跳变，说明系统在采集后进行了必要的异常值过滤与工程量处理。

测试结果表明，该系统具备稳定的多传感器采集与统一上报能力。其原因在于，STM32 端并非直接上传原始电信号，而是在本地完成单位换算、异常过滤和资源量推导之后，再输出统一结构的数据报文。因此，上位端接收到的数据已经具备直接显示与进一步分析条件。该结果验证了感知层数据标准化设计的有效性，满足了灾后临时安置点对环境监测和水电状态感知的基本应用需求。

### 5.2.2 联动控制功能测试

联动控制测试的目标是验证系统在出现预警或告警状态时，是否能够根据风险等级自动驱动风扇、舵机、蜂鸣器和指示灯等执行器，并正确处理组合异常场景。测试过程中，重点考察单一指标超限、持续超限以及多指标同时异常三类情况。

测试现象表明，当环境指标进入预警状态后，系统会触发相应提示并执行基础联动动作；当风险进一步上升至告警状态后，执行器动作明显加强，现场提示也更加明确。对于温度与粉尘、粉尘与空气质量、水流与电流等组合异常情形，系统不会简单地沿用单指标动作，而是表现出更高等级的风险响应，说明组合联动策略已经参与控制决策。

测试结果表明，系统能够根据状态等级完成差异化联动，并能对复合异常进行增强处理。其原因在于，STM32 端采用了分级状态机和组合判定机制，不仅判断单项是否超限，还进一步综合多项异常对整体风险进行修正。该结果说明系统具备从“监测”到“响应”的完整能力，验证了联动控制策略在实际场景中的有效性。

### 5.2.3 MQTT通信测试

MQTT 通信测试的重点是验证双向消息链路是否完整，即一方面确认 STM32 采集数据能否经 ESP32、MQTT 服务传递到 Qt 和 Web 端，另一方面确认 Qt 下发的控制命令能否经由同一路径反向到达 STM32。

测试过程中，上行链路以实时采样数据为对象进行观察，下行链路则以设备控制与阈值下发消息为对象进行验证。测试现象表明，上行数据能够从现场节点稳定传递至两个上位端，Qt 与 Web 均能接收到同一主题中的实时消息；下行操作发起后，现场设备状态能够及时发生变化，说明控制消息也能够顺利回到 STM32 执行端。

在消息可靠性方面，测试过程中未出现明显的主题错乱或大面积丢包现象。由于系统采用串口缓存与 MQTT 定时上传相结合的方式，现场采样节奏与云端上报节奏得以适度分离，从而避免了短时间内重复发布造成的通信拥塞。结果表明，系统具备较完整的双向通信能力，验证了“STM32 + ESP32 + MQTT + 多客户端”通信架构的有效性，满足了实时监测与远程干预并行存在的应用需求。

### 5.2.4 多端一致性测试

多端一致性测试主要验证 Qt 客户端与 Web 看板在接收同一实时消息后，是否能够在关键状态上保持一致，包括监测值、风险等级、执行器状态和报警提示等内容。

测试现象表明，在系统正常运行过程中，Qt 与 Web 端对温湿度、PM2.5、空气质量等关键指标的显示保持同步，风险等级变化后两端均会同步切换状态显示；当执行器联动动作发生时，两个终端也能够更新为相应控制状态。对于报警消息和联动提示，Qt 端表现为管理型显示，Web 端表现为轻量可视化提示，虽然界面形式不同，但状态来源一致。

测试结果表明，系统具备较好的多端一致性。其原因在于，上位端均直接消费来自现场控制端的统一 JSON 报文，而不是分别进行本地重算，从而避免了不同客户端之间出现等级判定不一致或动作解释不一致的问题。该结果验证了统一消息模型设计的合理性，也说明系统能够满足多终端协同展示的应用需求。

### 5.2.5 阈值控制与远程控制测试

阈值与远程控制测试的目标是验证桌面端发起的阈值配置、阈值恢复以及执行器控制命令能否被现场控制端正确接收和执行，并进一步反馈到系统状态中。

测试过程中，首先在 Qt 客户端修改监测阈值并下发至 MQTT 通道，然后观察现场风险判定是否按照新阈值生效；随后发送恢复默认阈值指令，检查系统是否回到默认判定区间；最后分别对风扇、舵机、蜂鸣器和指示灯进行远程控制，观察现场响应和上位端状态变化。

测试现象表明，阈值修改后，相关监测项的预警或告警触发边界随之变化；恢复默认阈值后，风险判断重新回到原始规则。对设备控制而言，下发命令后现场执行器能够做出响应，上位端对应状态信息也能同步变化。结果表明，该系统具备较完整的远程配置与人工干预能力。

从原因分析看，系统通过 MQTT 主业务主题统一承载控制消息，并由 ESP32 完成下行分类转发，使得阈值设置、恢复指令和设备控制能够在同一链路上实现。该结果验证了远程控制链路设计的有效性，说明系统能够满足管理端对现场设备进行配置和干预的实际需求。

## 5.3 性能测试与分析

### 5.3.1 实时性分析

实时性分析主要考察系统从现场采样到多端显示的响应速度，以及各节点任务节奏是否与系统设计一致。根据实际实现方式，STM32 端采用约 1 s 的数据采样周期，并以更短的基础节拍维持主循环运行；本地显示刷新周期约为 300 ms，用于保证现场状态显示的连续性。ESP32 则采用串口即时缓存与约 2 s 的网络上传节奏相结合的方式，将采样数据转发至 MQTT 服务。Qt 客户端和 Web 看板在接收到消息后立即进行界面刷新。

测试现象表明，环境数据变化后，Qt 客户端与 Web 看板均能够在较短时间内完成更新，现场联动动作与上位端显示之间不存在明显脱节。对于桌面端和浏览器端的显示而言，用户能够感受到连续的实时刷新效果，说明系统整体延迟处于可接受范围内。

结果表明，系统具备较好的实时监测能力。其原因在于，现场采样、网关上传和客户端刷新在不同节点分别采用了与各自资源条件相适应的节奏控制策略。STM32 保证了本地采集和判定的及时性，ESP32 通过缓存和定时发布降低了网络抖动影响，Qt 与 Web 则基于订阅消息实现被动刷新。该设计满足了临时安置点对环境监测与资源状态快速反馈的应用需求。

### 5.3.2 稳定性分析

稳定性测试主要关注系统在持续运行条件下是否会出现掉线、消息停滞、状态不同步或历史数据中断等问题。测试过程中，重点观察 ESP32 的无线连接恢复能力、Qt 客户端的 MQTT 自动重连能力以及多端数据连续更新情况。

测试现象表明，在网络正常的情况下，系统能够持续保持数据更新；当无线网络或 MQTT 会话短时中断后，ESP32 会优先恢复 WiFi 连接，再重新建立 MQTT 会话；Qt 客户端在连接断开后也能够执行自动重连并恢复主题订阅。恢复完成后，数据链路能够继续工作，多端显示重新进入同步状态。

测试结果表明，系统具备一定的连续运行能力。其原因在于，网关侧和桌面侧都实现了面向连接状态的自恢复机制，使得通信异常不会直接导致系统长期失效。此外，ESP32 在上传失败时保留待发数据标记，也有助于减少短时通信波动造成的数据遗漏。该结果验证了系统在实际部署环境下具备基本稳定运行能力。

为了更直观地展示系统在长时间连续运行条件下各关键监测指标的变化趋势以及整体运行波动情况，本节在稳定性分析之后给出 72 小时连续运行数据曲线，以作为系统持续运行能力的图形化证据，如图5-1所示。

### 5.3.3 并发与通信压力分析

并发与通信压力分析主要考察在多个客户端同时订阅同一主题时，系统消息分发是否仍能保持正常，以及单主题双向通信是否会引发额外负担或消息回环问题。

测试过程中，以 Qt 客户端和 Web 看板同时在线订阅 `test001up` 为基本场景，观察两端是否能够稳定接收上行数据；同时结合远程控制和阈值设置操作，分析主题中双向消息并存时的运行情况。测试现象表明，在双客户端同时在线的条件下，主业务主题仍能保持稳定分发，多端更新基本同步；在控制消息下发后，也未出现控制信息被重复回放或现场设备反复动作的异常现象。

原因分析表明，系统当前消息压力总体可控。一方面，STM32 采样频率和 ESP32 上传频率均较低，适合轻量物联网场景；另一方面，ESP32 在同主题双向通信条件下引入了回环过滤机制，能够区分状态上报与控制命令，从而避免上行消息被误判为下行控制。结果表明，系统能够满足 Qt 与 Web 双客户端并发接入条件下的通信需求，验证了当前 MQTT 架构在本课题规模下的适用性。

## 5.4 数据存储与可靠性测试

数据存储与可靠性测试的重点是验证 SQLite 本地数据库在长期运行中的写入稳定性、数据完整性以及业务记录闭环能力。测试对象主要包括历史数据表、报警信息表、远程控制日志表和设备离线记录表。

在历史数据写入方面，测试现象表明，桌面端在接收到有效实时消息后能够持续生成监测记录，数据库中的时间序列数据保持连续，未出现明显的结构性缺失。由于系统采用统一时间格式写入历史样本，并围绕时间索引进行查询，因此后续历史统计与 Web 数据导出均可正常进行。结果表明，SQLite 能够稳定支撑本系统的本地时序数据存储需求。

在报警信息管理方面，测试表明，监测项进入预警或告警状态后，数据库能够生成对应事件记录；当异常解除后，相关记录能够补写结束时间并转入已结束状态；对于 Web 一键求助事件，系统也能够将其纳入同一事件表中管理。该现象说明报警记录不仅保存了事件内容，还保存了事件生命周期。结果表明，报警信息表具备较好的闭环管理能力，验证了事件化存储设计的有效性。

在远程控制日志方面，测试表明，桌面端发起控制或阈值配置操作后，数据库中能够同步记录相应的操作时间、控制对象与结果描述。该现象说明系统已经将“控制动作”与“日志留痕”纳入同一业务链路，有助于后续操作审计与问题追溯。

在设备离线记录方面，测试表明，当桌面端在一定时间内未接收到新的实时消息时，系统能够识别通信中断状态，并将离线起点及持续时间写入数据库。对同一次离线过程，数据库不会持续生成大量重复记录，而是围绕同一离线事件更新持续时间。结果表明，系统具备对通信异常进行结构化记录的能力，说明本地数据库不仅承担历史存储任务，也承担运行可靠性追踪任务。

综合分析可见，SQLite 在本系统中不仅是简单的数据缓存工具，而是构成了从实时采集、报警闭环到历史追溯的本地数据中心。测试结果表明，该数据库方案能够满足本课题在单机部署条件下的数据可靠性需求。

## 5.5 测试结果总结

通过对传感器采集、联动控制、MQTT 通信、多端一致性、远程控制以及数据存储等方面的测试与分析，可以认为本系统已基本完成预定设计目标。测试结果表明，STM32 能够稳定完成环境与资源数据采集，并依据分级状态机和组合联动策略实现现场自动控制；ESP32 能够完成串口与 MQTT 之间的通信桥接，并具备一定的自动重连和回环抑制能力；Qt 客户端能够承担实时监测、报警管理、远程控制与本地存储等核心业务；Web 看板能够实现轻量化实时展示和一键求助交互；SQLite 能够支撑历史数据、报警事件和控制日志的本地闭环管理。

从整体表现看，系统在功能正确性、实时性、稳定性、多端协同和数据可靠性方面均达到了课题设计要求。特别是在“STM32 + ESP32 + MQTT + Qt + Web + SQLite”多端协同架构下，系统实现了从现场感知、通信传输、上位管理到历史追溯的完整链路，验证了所采用设计方案的可行性。

同时，测试过程也表明系统仍存在进一步优化空间。例如，在当前实现中，通信链路主要依赖单主题双向交互，后续可进一步细化消息主题设计；本地用户认证采用轻量方式实现，后续可根据应用需求增强安全性；Web 历史数据仍采用导出脚本更新，后续可探索更自动化的数据同步机制。总体而言，现有系统已经能够满足本科毕业设计所要求的功能实现与技术验证目标，并具备进一步完善与扩展的基础。
