# 灾后临时安置点智慧水电管理与环境监测系统

## 一、系统概述

本系统用于灾后临时安置点的环境监测与水电管理，以 STM32F103C8T6 为主控，采集温度、湿度、PM2.5、空气质量、电流、水流 6 项指标，通过 ESP32 WiFi 模块上传至巴法云 MQTT，供 Web 看板（灾民侧）和 Qt 桌面端（工作人员侧）实时查看与远程控制。

**课题**：灾后临时安置点智慧水电管理与环境监测系统设计  
**学校**：应急管理大学  
**IDE**：Keil MDK v5（ARMCC v5.06）

---

## 二、硬件架构

### 2.1 主控与通信

```
STM32F103C8T6 (面包板)
  ├── 6 路传感器采集（1s 周期）
  ├── 5 路执行器控制
  ├── OLED 0.96" 显示
  └── USART1 (115200 8N1) → ESP32 → WiFi → MQTT(巴法云) → Web/Qt
```

### 2.2 传感器引脚分配

| 模块 | 信号 | 引脚 | 接口类型 |
|------|------|------|----------|
| DHT11 温湿度 | DATA | PB13 | 单总线 |
| ACS712 电流 | ADC | PA0 | ADC1_IN0 |
| MQ135 空气质量 | ADC | PA1 | ADC1_IN1 |
| GP2Y10 粉尘浓度 | ADC | PA2 | ADC1_IN2 |
| YF-S401 水流 | 脉冲 | PA5 | EXTI5 中断 |
| GP2Y10 LED | 控制 | PB12 | GPIO 输出 |

### 2.3 执行器引脚分配

| 执行器 | 引脚 | 说明 |
|--------|------|------|
| 红灯 LED | PA15 | 需禁用 JTAG，高电平亮 |
| 绿灯 LED | PA11 | 高电平亮 |
| 黄灯 LED | PA12 | 高电平亮 |
| 蜂鸣器 | PB14 | 低电平响 |
| 风扇 | PB6 | 低电平转、高电平停 |
| 舵机(窗户) | PA6 (TIM3_CH1) | 20ms PWM，500~2500µs |

### 2.4 通信引脚

| 方向 | STM32 | ESP32 |
|------|-------|-------|
| STM32→ESP32 | PA9 (USART1_TX) | GPIO16 (RX2) |
| ESP32→STM32 | PA10 (USART1_RX) | GPIO17 (TX2) |

### 2.5 供电方案

```
12V 适配器 → LM2596（调 5V 输出）→ 面包板电源模块
  ├── 5V：MQ135、舵机、风扇、GP2Y10、ACS712、水流传感器、ESP32
  ├── 3.3V：OLED、DHT11
  └── GND 全部共地
```

---

## 三、三级联动阈值体系

系统对 6 个传感器值分别设定**正常 / 预警 / 警报**三级范围，综合判断后自动驱动执行器。

### 3.1 阈值表

| 传感器 | 正常 | 预警 | 警报 |
|--------|------|------|------|
| 温度 | 18~32℃ | 32~38℃ 或 10~18℃ | >38℃ 或 <10℃ |
| 湿度 | 30~70% | 70~85% 或 20~30% | >85% 或 <20% |
| PM2.5 | <75 μg/m³ | 75~150 μg/m³ | >150 μg/m³ |
| 空气质量 | <100 ppm | 100~200 ppm | >200 ppm |
| 电流 | <800 mA | 800~1200 mA | >1200 mA |
| 水流 | <5 L/min | 5~10 L/min | >10 L/min |

### 3.2 默认阈值变量（STM32 RAM，可远程修改）

**预警阈值：**

| 变量 | 默认值 | 远程命令 |
|------|--------|----------|
| TEMP_WARN_H | 32 | `tr` |
| TEMP_WARN_L | 18 | `tc` |
| HUMI_WARN_H | 70 | `hw` |
| HUMI_WARN_L | 30 | `hd` |
| PM25_WARN | 75 | `ph` |
| AQ_WARN | 100 | `aq` |
| CUR_WARN | 800 | `ci` |
| FLW_WARN | 5 | `fl` |

**告警阈值：**

| 变量 | 默认值 | 远程命令 |
|------|--------|----------|
| TEMP_ALARM_H | 38 | `ta` |
| TEMP_ALARM_L | 10 | `tb` |
| HUMI_ALARM_H | 85 | `ha` |
| HUMI_ALARM_L | 20 | `hb` |
| PM25_ALARM | 150 | `pa` |
| AQ_ALARM | 200 | `aa` |
| CUR_ALARM | 1200 | `ca` |
| FLW_ALARM | 10 | `fa` |

---

## 四、自动联动规则

### 4.1 系统级别判定

取 6 个传感器中**最差级别**作为系统整体级别（link_level）：

- 任一传感器 **告警** → 系统告警（level = 2）
- 无告警但任一传感器 **预警** → 系统预警（level = 1）
- 全部正常 → 系统正常（level = 0）

### 4.2 执行器联动表

| 系统级别 | 红灯 | 黄灯 | 绿灯 | 风扇 | 蜂鸣器 | 舵机(窗户) |
|----------|------|------|------|------|--------|------------|
| 正常 (0) | 灭 | 灭 | **亮** | 停 | 停 | 关 (0°) |
| 预警 (1) | 灭 | **亮** | 灭 | **转** | **慢响**（每 2s 滴一声） | 关 (0°) |
| 警报 (2) | **亮** | 灭 | 灭 | **转** | **常响** | **开 (90°)** |

### 4.3 手动/自动优先级

- 远程下发控制命令 → 对应设备进入**手动模式**，手动优先
- 手动模式关闭 → **自动联动**接管
- 每个执行器（LED/风扇/蜂鸣器/舵机）独立区分手动/自动

### 4.4 远程控制指令

| 命令 | 功能 |
|------|------|
| `0` | 蜂鸣器关（进入手动模式） |
| `1` | 蜂鸣器开（进入手动模式） |
| `2` | 窗户开 90°（进入手动模式） |
| `3` | 窗户关 0°（进入手动模式） |
| `4` | 风扇开（进入手动模式） |
| `5` | 风扇关（进入手动模式） |
| `6` | 三灯全亮（进入手动模式） |
| `7` | 恢复自动灯态 |

---

## 五、通信协议

### 5.1 上行：STM32 → ESP32 → 云端

```json
{
  "t": 26, "h": 60, "pm": 35, "aq": 52,
  "f": 1.23, "i": 450,
  "bp": 85, "wp": 72,
  "lv": 0
}
```

| 字段 | 含义 | 单位 |
|------|------|------|
| t | 温度 | ℃ |
| h | 湿度 | % |
| pm | PM2.5 | μg/m³ |
| aq | 空气质量 | ppm |
| f | 水流量 | L/min |
| i | 电流 | mA |
| bp | 剩余电量 | % |
| wp | 剩余水量 | % |
| lv | 联动级别 | 0=正常 1=预警 2=警报 |

### 5.2 下行：云端 → ESP32 → STM32

**阈值修改（KKVVV 格式）：**
```
tr35    → 温度预警上限设为 35
pa150   → PM2.5 告警设为 150
ci800   → 电流预警设为 800
```

**JSON 格式阈值修改：**
```json
{"th": {"tr": 35, "ta": 40, "ci": 800}}
```

**控制指令：**
```json
{"code": 4, "name": "风扇开启"}
```

### 5.3 水电容量远程设置

| 命令 | 示例 | 作用 |
|------|------|------|
| `bc` | `bc10000` | 设置电池容量(mAh)并重置 |
| `bp` | `bp50` | 直接设定剩余电量% |
| `wc` | `wc2000` | 设置水箱容量(L)并重置 |
| `wp` | `wp80` | 直接设定剩余水量% |

---

## 六、界面系统

### 6.1 Web 看板

**定位**：给灾后群众看（大字、直观图形、水电剩余突出）

**路径**：`dashboard/index.html`

**功能**：
- 2×2 环境指标卡片（温度/湿度/PM2.5/空气质量），根据联动级别自动变绿/黄/红
- 水电剩余电池 SVG + 水箱动画
- 未来 24 小时环境预测（温度曲线 + 天气图标）
- 右侧报警列表，显示异常传感器 + 已采取措施
- **联动弹窗 Toast**：系统告警/预警时右下角弹出通知，10 秒自动消失
- 底部一键求助按钮（身体不适/缺水/电力故障/其他）

### 6.2 Qt 桌面端

**定位**：给工作人员看（详细数据、历史曲线、控制面板）

**路径**：`System_UI/`

**功能**：
- 实时数据首页（6 指标卡片 + 累计用量）
- 环境监测页（温度/湿度/PM2.5 三线折线图，最近 20 点）
- 报警信息页（历史报警表格，按级别筛选）
- 设备管理页（设备状态表 + 远程控制）
- 历史数据页（多传感器折线图 + XLSX 导出）
- 告警弹窗：任一传感器达告警级别时弹出红白对话框，显示异常详情 + 已采取措施
- 一键求助接收（接收 Web 端求助并弹窗通知）

---

## 七、目录结构

```
D:\AAA\
 ├── FinalSystem\          ← STM32 固件 (Keil MDK)
 │    ├── User\main.c      ← 主循环 + 联动逻辑
 │    ├── Hardware\        ← 各模块驱动
 │    │    ├── ACS712.c/h  ← 电流传感器（600次采样，DC blocker）
 │    │    ├── DHT11.c/h   ← 温湿度（单总线，超时防卡死）
 │    │    ├── GP2Y10.c/h  ← 粉尘浓度（厂商公式）
 │    │    ├── MQ_135.c/h  ← 空气质量 ppm
 │    │    ├── waterFlow.c/h ← 水流传感器（98脉冲/L）
 │    │    ├── OLED.c/h    ← SSD1306 0.96" I2C
 │    │    ├── servo.c/h   ← SG90 舵机 PWM
 │    │    ├── led.c/h     ← RGB LED（红/绿/黄）
 │    │    ├── buzzer.c/h  ← 蜂鸣器
 │    │    ├── motor.c/h   ← 风扇控制 (PB6) + 电机驱动
 │    │    ├── serial.c/h  ← USART1 通信 + JSON + 阈值解析
 │    │    └── pwm.c/h     ← TIM2/3/4 PWM
 │    ├── Start\           ← 启动文件
 │    └── Library\         ← 标准外设库
 ├── esp32\                ← ESP32 固件 (Arduino)
 │    └── stm32_to_esp32_uart_test.ino  ← MQTT桥接 + 阈值缓存
 ├── dashboard\            ← Web 看板
 │    ├── index.html       ← 主页面
 │    ├── js/
 │    │    ├── app.js      ← 数据解析 + 联动弹窗 + 报警
 │    │    ├── config.js   ← MQTT 连接配置
 │    │    └── history_data.js ← 历史数据接口
 │    └── css/
 │         └── dashboard.css ← 看板样式 + Toast 动画
 └── System_UI\            ← Qt 桌面端 (Qt 6 + CMake)
      ├── mainwindow.cpp   ← 主窗口 + MQTT 处理 + 报警弹窗
      ├── mainwindow.h
      ├── mqtt.cpp/h       ← MQTT 客户端封装
      ├── databasemanager.cpp/h ← SQLite 数据管理
      └── ...
```

---

## 八、数据库

**位置**：`D:\System_UI_Data\`（Qt 桌面端 SQLite）

**核心表**：

| 表名 | 用途 |
|------|------|
| `data` | 简化核心指标（供历史查询） |
| `alarm_info` | 报警记录（含联动告警 + 求助） |
| `device_abnormal_records` | 设备离线记录 |
| `device_info` | 设备注册信息 |
| `remote_exec_logs` | 远程控制操作日志 |

---

## 九、水电剩余量模拟

| 项目 | 变量 | 默认容量 | 方法 |
|------|------|----------|------|
| 电量 | `battery_used_mAh` | 10000 mAh | 每秒积分 I/3600，用 DC blocker 去 ACS712 漂移 |
| 水量 | `total_flow_L` | 10 L | YF-S401 脉冲累加（98 脉冲/L） |

---

## 十、已解决的关键问题

1. **ACS712 空载漂移** → DC blocker 基线追踪（α=0.05），连续 3s <15mA 强制归零
2. **DHT11 卡死** → while 等待加入超时计数（retry>200 退出）
3. **GP2Y10 空读偏高** → NO_DUST_MV=420（按商家参考值），厂商公式计算
4. **LED 红灯微亮** → `GPIO_PinRemapConfig(SWJ_JTAGDisable)` 释放 PA15
5. **OLED 切页闪烁** → 仅写 4 行数据区替代全屏清除
6. **Keil 中文注释编译错误** → ARMCC v5 对 GB2312 处理不佳，统一用 `//` 行注释
