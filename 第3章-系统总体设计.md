# 第3章 系统总体设计

## 3.1 技术选型

### 3.1.1 主控与采集终端技术选型

本系统的底层控制器采用 STM32F103C8。根据 `FinalSystem/Project.uvprojx`、`FinalSystem/User/main.c` 以及各硬件驱动文件可以看出，STM32 端承担了传感器采集、执行器控制、联动判断、串口通信和本地显示等核心任务。其固件已经集成 DHT11 温湿度采集、MQ-135 空气质量采集、GP2Y10 粉尘浓度采集、ACS712 电流采集、水流脉冲计量、OLED 显示、蜂鸣器、三色指示灯、风扇和舵机等模块，因此采用 STM32 作为现场主控具有较好的统一性。

从源码实现来看，STM32 端并不是单纯的数据采集节点，而是具备本地自治能力的边缘控制节点。程序在 `main` 函数中完成各类外设初始化后，以 1 s 为周期执行传感器采样，以 300 ms 为周期刷新 OLED 显示，并在本地完成预警、告警、组合告警、自动联动和手动优先控制。这说明系统的重要控制逻辑并未依赖上位机持续在线，而是在 STM32 端闭环完成，因此该方案能够更好地满足安置点场景下的实时性要求。

### 3.1.2 网络通信技术选型

系统的联网接入部分采用 ESP32。根据 `esp32/stm32_to_esp32_uart_test/stm32_to_esp32_uart_test.ino`，ESP32 通过 WiFi 接入网络，使用 `PubSubClient` 连接 `bemfa.com:9501`，并通过 `ArduinoJson` 解析 MQTT 消息。ESP32 与 STM32 之间采用 `Serial2` 串口通信，波特率为 115200，管脚为 RX2=16、TX2=17。

从实现方式看，ESP32 在本系统中并不参与环境数据计算和控制决策，其主要作用是完成“串口 JSON 数据”与“MQTT 消息”之间的双向转发。一方面，ESP32 将 STM32 上报的完整 JSON 数据缓存后定时发布到 MQTT 主题；另一方面，ESP32 订阅同一主题的下行控制消息，并将数字命令、阈值命令或 JSON 命令继续转发给 STM32。该设计使网络接入层与控制层解耦，便于后续更换上位机或展示端。

### 3.1.3 上位机与数据管理技术选型

桌面客户端采用 Qt 6 与 C++ 实现。根据 `System_UI/CMakeLists.txt`，项目实际使用了 `Widgets`、`Charts`、`Network`、`Sql` 和 `Mqtt` 模块，其中 `Widgets` 用于界面构建，`Charts` 用于趋势曲线显示，`Sql` 用于 SQLite 数据管理，`Mqtt` 用于实时订阅与下发控制。该组合与当前源码中的设备状态展示、阈值下发、报警记录、历史查询和用户认证功能相对应。

本系统的数据持久化方式采用 SQLite。根据 `System_UI/dbconfig.h`，数据库文件固定存储在 `D:/System_UI_Data/system_ui.sqlite`。`DatabaseManager` 负责实时采样、报警记录、设备离线记录、设备信息和远程操作日志等业务数据的读写，`AuthService` 在同一数据库中维护 `users` 用户表。因此，本系统并未单独部署后端服务，而是采用“Qt 客户端 + 本地 SQLite”方式完成数据管理。

Web 看板采用 HTML、CSS 和 JavaScript 实现。根据 `dashboard/index.html`，页面引入了 MQTT.js 和 Chart.js；根据 `dashboard/js/config.js` 和 `dashboard/js/app.js`，Web 端通过 WebSocket 连接巴法云 MQTT 服务，实时订阅设备数据主题，并通过独立主题发送一键求助消息。同时，`dashboard/export_history.py` 负责将 SQLite 中的历史数据导出为 `history_data.js`，用于前端历史趋势展示。由此可见，Web 看板属于轻量级展示终端，而非独立后端系统。

## 3.2 系统总体架构

### 3.2.1 五层架构设计

结合当前源码，可以将本系统划分为五个层次。

第一层为感知采集层。该层由 DHT11、MQ-135、GP2Y10、ACS712 和水流传感器组成，负责获取温度、湿度、空气质量、粉尘浓度、电流和水流等现场数据。

第二层为嵌入控制层。该层以 STM32F103C8 为核心，负责传感器驱动、阈值比较、状态判定、组合联动、执行器控制和 OLED 本地显示，是系统的核心控制单元。

第三层为网络接入层。该层由 ESP32 与 MQTT 通信构成，负责串口与 WiFi 网络之间的协议转换，实现 STM32 与上位端之间的数据桥接。

第四层为数据管理层。该层主要由 Qt 客户端中的 `DatabaseManager`、`AuthService` 与本地 SQLite 数据库构成，负责实时数据落库、报警记录维护、设备离线记录、远程操作日志和用户信息管理。

第五层为交互展示层。该层由 Qt 桌面客户端和 Web 看板组成。Qt 客户端承担实时监测、阈值设置、设备控制、历史查询和报警管理等功能；Web 看板面向浏览器场景，提供实时监控、历史趋势和一键求助功能。

需要说明的是，以上五层架构中并不存在独立部署的服务器后端。系统的数据管理逻辑集中在 Qt 本地程序与 SQLite 数据库中，网络侧只借助 MQTT 服务完成消息中转。

为了从整体上展示系统各层之间的组织关系以及现场控制端、通信网关、桌面管理端、Web 展示端和本地数据库之间的协同方式，有必要在总体设计阶段首先给出系统总体架构图，用于概括本系统的分层结构与核心组成，如图3-1所示。

### 3.2.2 子系统职责划分

STM32 子系统负责现场数据采集与本地自治控制。其职责包括采集环境数据和水电数据、维护阈值参数、判定预警与告警级别、执行风扇与舵机联动、驱动蜂鸣器和三色指示灯、组织上行 JSON 数据包以及接收下行控制命令。

ESP32 子系统负责网络网关功能。其职责包括连接 WiFi、连接 MQTT 服务器、订阅 `test001up` 主题、转发 Qt 下发的控制命令和阈值命令、缓存 STM32 上报 JSON 并按固定周期发布到 MQTT。

Qt 客户端负责业务管理与本地数据中心功能。其职责包括订阅设备数据、解析 JSON 字段、刷新实时界面、写入 SQLite 数据库、展示历史曲线、提供阈值远程设置和设备控制界面、维护报警表与执行日志、处理 Web 看板发出的求助消息。

SQLite 子系统负责本地持久化存储。其职责包括保存传感器采样数据、报警信息、设备异常离线信息、设备信息、远程控制日志和用户认证数据。

Web 看板负责轻量级展示与求助交互。其职责包括通过 MQTT.js 订阅实时数据、根据 `lv`、`act`、`mod`、`alm`、`actn` 和 `res` 字段刷新页面状态、借助 Chart.js 展示历史趋势，并通过 `WebQT1` 主题发布求助信息。

### 3.2.3 系统工作流程

系统运行时，STM32 周期性读取各类传感器数据，并在本地完成阈值比较与联动判断。随后，STM32 将处理后的综合状态组织为 JSON 字符串，经 USART1 发送给 ESP32。

ESP32 从串口接收到完整 JSON 后，不重建数据结构，而是直接缓存原始 JSON，并按照 2 s 周期发布到 MQTT 主题 `test001up`。Qt 客户端和 Web 看板订阅该主题后，分别进行数据解析与界面更新。Qt 客户端在更新界面的同时，将实时采样写入 SQLite，并对报警状态变化进行数据库记录维护。

当用户在 Qt 客户端执行阈值下发、设备控制或阈值恢复时，Qt 将相应命令发布到 `test001up`。ESP32 接收到命令后，根据消息类型将其转换为串口命令发送给 STM32，STM32 再完成参数修改或执行器动作。

当用户在 Web 看板发起一键求助时，浏览器将求助信息发布到 `WebQT1` 主题。Qt 客户端订阅该主题后，将其写入 `alarm_info` 表，并在本地报警信息界面中显示。

为了更清晰地呈现系统在运行过程中上行监测数据、下行控制命令以及求助消息在各节点之间的流转路径，本节在文字流程描述之后补充系统数据流图，以帮助读者理解各模块之间的先后关系和信息方向，如图3-2所示。

## 3.3 硬件总体设计

### 3.3.1 采集模块总体设计

从 STM32 驱动源码可以看出，系统的环境与资源采集模块均直接接入主控。DHT11 温湿度传感器连接在 `PB13`；MQ-135 空气质量传感器使用 `PA1` 的 ADC 通道；GP2Y10 粉尘传感器的数据采样通道为 `PA2`，其发光控制引脚为 `PA7`；ACS712 电流传感器使用 `PA0` 采样；水流传感器脉冲输入连接在 `PA5`，通过外部中断和 `TIM2` 定时统计实现流量计算。

采集逻辑上，系统并未将所有数据仅作为原始值上报，而是在 STM32 端先完成必要的数据处理。例如，粉尘数据经过采样换算后得到 PM2.5 数值，电流数据通过电流传感器算法转换后再参与阈值判断，水流通过脉冲计数方式换算为瞬时流量和累计流量。该设计使上位端获得的数据已经具备直接展示和联动判断的条件。

### 3.3.2 执行与显示模块总体设计

系统执行器包括蜂鸣器、风扇、舵机和三色指示灯。根据驱动文件，蜂鸣器连接在 `PB14`；风扇控制引脚为 `PB6`，且采用低电平有效方式；舵机由 `TIM3` 输出 PWM 驱动，对应引脚为 `PA6`；三色指示灯分别使用 `PA15`、`PA11` 和 `PA12`。其中 `PA15` 在初始化时释放了默认 JTAG 复用功能，以确保红色指示灯能够正常工作。

本地显示模块采用 OLED。根据 `OLED.c`，OLED 通过软件模拟 I2C 连接在 `PB8` 和 `PB9`，用于显示温湿度、PM2.5、空气质量、水流和电流等关键数据。由此可见，系统具备脱离上位机的本地观察能力。

### 3.3.3 硬件接口与连接关系

STM32 与 ESP32 之间通过 USART1 建立串口连接，其中 STM32 发送引脚为 `PA9`，接收引脚为 `PA10`，通信波特率为 115200。该链路承担了所有上行监测数据和下行控制命令的转发任务。

各传感器与执行器的连接均由独立驱动模块封装，实现上采用“采集模块 + 判定逻辑 + 执行模块”的结构。采集端负责提供原始或换算后的数值，判定端在 `main.c` 中完成状态机式联动逻辑，执行端根据自动模式或手动模式产生最终动作。这样既有利于功能扩展，也便于后续论文中对模块化设计进行说明。

为了避免硬件设计部分停留在抽象描述层面，本节进一步给出系统硬件连接图，用于说明 STM32 主控、ESP32 网关以及各类传感器、执行器之间的实际接口关系和关键引脚分配，如图3-3所示。

此外，为了反映本课题硬件平台的真实搭建情况与模块集成效果，在硬件连接关系说明之后补充系统硬件实物图，使读者能够从实物层面对系统组成形成直观认识，如图3-4所示。

## 3.4 软件总体设计

### 3.4.1 STM32 软件总体设计

STM32 端软件是整个系统的核心控制程序。根据 `FinalSystem/User/main.c`，系统启动后依次初始化 LED、蜂鸣器、DHT11、MQ135、OLED、GP2Y10、舵机、电机、串口、ACS712、水流传感器等模块，并设置默认显示与执行器初始状态。

主循环中，程序首先处理串口下行命令。命令解析由 `Serial_ReadLine` 和 `Serial_ParseCommand` 完成，支持 `reset`、`reset_th`、`mode`、`ctrl`、`th` 等 JSON 命令，同时兼容单字节数字控制和 `kkVVV` 形式的阈值命令。对于阈值设置，固件区分告警阈值和预警阈值，其中温湿度、PM2.5、空气质量、电流和水流的预警值可以由告警值自动推导。

在采样与控制部分，STM32 以 1 s 为周期采集各类数据，并通过滞回阈值和连续计数方式判断正常、预警、告警三级状态。系统同时支持组合联动规则，例如温度与 PM2.5 同时异常、PM2.5 与空气质量同时异常、水流与电流同时异常等情形。联动结果用于控制风扇启停、舵机开闭、蜂鸣器鸣叫模式和指示灯颜色。

程序还实现了手动优先与自动恢复机制。当系统进入手动模式后，相关执行器优先响应远程命令；若 5 min 内没有新的手动指令，程序会自动回到自动控制状态。最终，STM32 将传感器数据、资源信息、级别信息、执行器状态、模式状态、报警码、动作标志和故障码统一打包为 JSON，通过串口发送给 ESP32。

### 3.4.2 ESP32 网关程序总体设计

ESP32 端程序主要由 WiFi 连接、MQTT 连接、串口接收和消息转发四部分组成。根据 `.ino` 文件实现，ESP32 启动后连接指定 WiFi，然后使用巴法云私钥作为 `clientId` 连接 `bemfa.com:9501`，并订阅 `test001up` 主题。

在上行方向，ESP32 通过 `Serial2` 读取 STM32 输出的整行 JSON。当接收到一帧完整消息后，程序先缓存原始字符串，再进行轻量级字段解析，仅用于串口日志输出和状态缓存；真正对外发布时，ESP32 直接将 STM32 原始 JSON 发布到 MQTT，避免了重复拼接数据结构。

在下行方向，ESP32 对收到的 MQTT 消息进行分类处理。若消息包含 `sen` 字段，则视为设备上报数据并直接忽略，避免自发自收形成循环；若消息为单字符数字或包含 `code`、`ctl` 字段，则转换为数字控制并发给 STM32；若消息包含 `th` 对象，则逐项拆解为阈值键值串口命令发送给 STM32，并发布 `th_sync` 消息用于前端同步；若消息是 `cmd` 型 JSON，则直接整帧透传给 STM32。由此可见，ESP32 在系统中是标准的通信网关，而不是业务处理中心。

### 3.4.3 Qt 客户端总体设计

Qt 客户端的总体结构由 MQTT 通信、界面展示、数据库管理和用户认证四部分组成。`Mqtt` 类对 `QMqttClient` 进行了封装，实现了自动重连、主题订阅和文本消息发布；`MainWindow` 负责实时监测、阈值设置、历史查询、设备管理和报警展示；`DatabaseManager` 负责 SQLite 访问；`AuthService` 负责用户登录、注册和角色查询。

在实时监测流程中，Qt 客户端连接 `bemfa.com:9501` 后，订阅 `test001up` 与 `WebQT1` 两个主题。对于 `test001up` 的消息，程序解析 `sen`、`res`、`lv`、`alm` 和 `actn` 等字段，刷新实时卡片、曲线和设备状态表，同时将采样数据写入 `data` 表，并根据报警码变化维护 `alarm_info` 表中的报警开始时间和结束时间。对于 `WebQT1` 的消息，程序将浏览器发出的求助内容写入报警数据库，并刷新界面显示。

在控制流程中，Qt 客户端提供阈值远程设置和设备控制界面。阈值界面当前实际下发的是告警阈值键，包括 `ta`、`tb`、`ha`、`hb`、`pa`、`aa`、`ca` 和 `fa`。设备控制界面当前实际发送的是 `{"code":n,"name":"..."}` 形式的消息，同时程序会将操作写入 `remote_exec_logs` 表。阈值恢复功能则通过 `{"cmd":"reset_th"}` 实现。

### 3.4.4 Web 看板总体设计

Web 看板由 `index.html`、`config.js`、`app.js` 和样式文件共同组成。根据源码实现，前端通过 MQTT.js 连接 `wss://bemfa.com:9504/wss`，订阅 `test001up` 主题以接收实时监测数据。页面读取 `sen` 字段展示温湿度、PM2.5、空气质量、电流和水流，读取 `res` 字段展示水电剩余资源，读取 `act` 与 `mod` 字段展示执行器状态，读取 `alm`、`actn` 和 `flt` 字段展示报警信息和已采取措施。

Web 端的历史数据并不是直接访问数据库获得，而是通过 `export_history.py` 脚本从 `D:/System_UI_Data/system_ui.sqlite` 中读取 `data` 表，生成 `js/history_data.js` 后再由浏览器加载。该脚本按天统计平均电流、平均水流、日耗电量和日耗水量，用于看板中的趋势图与历史信息展示。

此外，Web 看板还实现了一键求助功能。页面将求助类型、文字标签、时间和站点信息封装为 JSON，并发布到 `WebQT1` 主题，随后由 Qt 客户端接收并入库。因此，Web 看板在总体设计上定位为“实时展示 + 轻量交互”终端。

## 3.5 通信与协议总体设计

### 3.5.1 数据上行链路设计

系统的数据上行链路为“STM32 -> ESP32 -> MQTT -> Qt/Web”。在该链路中，STM32 是原始数据与联动状态的唯一生成者。它将采样值、资源信息、级别信息、执行器状态和报警信息组合为统一 JSON，再经 USART1 发送给 ESP32。

ESP32 接收到 JSON 后，不重新计算业务逻辑，而是以近似透传方式将完整字符串发布到 MQTT 主题 `test001up`。Qt 客户端与 Web 看板均订阅该主题，因此两个展示终端能够同时获得一致的数据来源。该设计减少了中间节点的二次加工，也保证了多端显示的一致性。

### 3.5.2 控制下行链路设计

系统的控制下行链路为“Qt -> MQTT -> ESP32 -> STM32”。当前源码中，Qt 客户端已经实际使用三类下行命令。

第一类为数字控制命令，如 `{"code":4,"name":"风扇开启"}`。ESP32 将其中的 `code` 解析为数字后，通过串口发送给 STM32，STM32 再映射为蜂鸣器、舵机、风扇或指示灯动作。

第二类为阈值设置命令，如 `{"th":{"ta":38,"tb":10,"ha":85,"hb":20,"pa":150,"aa":200,"ca":15,"fa":10}}`。ESP32 会将 `th` 对象拆分成多条串口阈值命令发送给 STM32。

第三类为 JSON 控制命令，如 `{"cmd":"reset_th"}`。该类命令由 ESP32 整帧透传给 STM32，STM32 串口解析器再根据 `cmd` 字段执行恢复默认等操作。

需要指出的是，STM32 的串口命令解析器还支持 `mode`、`ctrl`、`th`、`reset` 等 JSON 命令格式以及旧式兼容格式，但在当前 Qt 图形界面中已经实际使用的主要是 `code`、`th` 和 `reset_th` 三种消息类型。

### 3.5.3 MQTT 主题设计

根据 Qt、ESP32 和 Web 三端源码，系统当前实际使用两个 MQTT 主题。

`test001up` 是系统的主业务主题。STM32 上行数据经 ESP32 发布到该主题，Qt 客户端和 Web 看板从该主题接收实时状态；同时，Qt 客户端也向该主题发布阈值设置、控制命令和恢复默认命令。因此，`test001up` 同时承担了数据上行和控制下行功能。

`WebQT1` 是 Web 与 Qt 之间的交互主题。当前源码中，该主题仅用于 Web 看板向 Qt 客户端发送一键求助消息，Qt 客户端订阅该主题并将求助内容写入报警数据库。

从实现上看，主业务主题采用“同主题双向通信”方式。为了防止 STM32 上报数据被 ESP32 当作下行命令再次回发，ESP32 在 MQTT 回调中通过检查 `sen` 字段来识别设备数据，并对 `th_sync` 消息进行过滤，从而避免通信回环。

### 3.5.4 JSON 数据格式设计

当前系统的上行 JSON 由 STM32 在 `Serial_SendUploadPacket` 中统一生成，核心字段结构如下。

| 字段 | 含义 | 主要子项 |
| --- | --- | --- |
| `sen` | 传感器数据 | `t`、`h`、`pm`、`aq`、`f`、`i` |
| `res` | 水电资源信息 | `bp`、`wp`、`tu`、`wu`、`br`、`wr`、`ps`、`bc`、`tc`、`bt`、`wt` |
| `lv` | 级别信息 | `link`、`env`、`d` |
| `act` | 执行器状态 | `bz`、`fan`、`svo`、`led` |
| `mod` | 控制模式 | `gl`、`bz`、`fan`、`svo`、`led` |
| `alm` | 报警码数组 | 最多 8 个整型报警码 |
| `actn` | 已执行动作位掩码 | 蜂鸣器、风扇、窗户动作标志 |
| `flt` | 故障码数组 | 最多 8 个整型故障码 |

其中，`lv.d` 数组按顺序表示温度、湿度、PM2.5、空气质量、电流和水流的等级；`alm` 中使用 101、111、121、131、141、151 等预警码和 102、112、122、132、142、152 等告警码，200 以上编码用于组合告警，901 以上编码用于故障状态；`actn` 则使用位标志描述系统已经执行的联动动作。

下行命令方面，系统当前实际支持以下几种消息形式。

| 消息示例 | 作用 |
| --- | --- |
| `{"code":4,"name":"风扇开启"}` | 控制设备动作 |
| `{"cmd":"reset_th"}` | 恢复默认阈值 |
| `{"th":{"ta":38,"tb":10,"ha":85,"hb":20,"pa":150,"aa":200,"ca":15,"fa":10}}` | 成组下发阈值 |
| `{"cmd":"mode","val":"AUTO"}` | 串口协议支持的模式切换命令 |
| `{"cmd":"ctrl","act":"fan","val":"on"}` | 串口协议支持的通用控制命令 |

由此可见，系统协议设计体现了“上行统一打包、下行兼容多种格式”的特点，有利于不同终端共享同一通信链路。

## 3.6 数据库总体设计

### 3.6.1 数据库存储目标

本系统数据库采用 SQLite，本地数据库文件为 `D:/System_UI_Data/system_ui.sqlite`。从源码实现看，数据库设计的目标并不是构建复杂的业务后端，而是为桌面端提供轻量级持久化支撑，满足实时数据留存、历史查询、报警追踪、设备离线记录、远程操作留痕和用户认证等需求。

由于系统没有独立服务器后端，因此 SQLite 实际上承担了本地业务数据中心的作用。Qt 客户端在接收到 MQTT 数据后立即写库，并在界面查询、趋势统计和历史导出时直接读取本地数据库。

### 3.6.2 主要数据表设计

根据 `DatabaseManager` 和 `AuthService` 的建表语句，当前系统实际包含以下主要数据表。

`data` 表用于存储实时采样数据，字段包括时间戳、温度、湿度、PM2.5、空气质量、电流和水流，是历史查询和趋势分析的基础数据表。

`device_abnormal_records` 表用于记录设备离线状态，字段包括传感器名称、离线开始时间和离线持续时长。Qt 在设备离线期间会持续更新该表中的持续时长。

`device_info` 表用于维护设备标识信息，字段包括设备编号、设备名称、设备类型和安装位置，主要用于设备管理页面展示。

`remote_exec_logs` 表用于记录远程控制操作，字段包括执行时间、设备编号、命令文本和结果文本，便于追踪设备控制过程。

`alarm_info` 表用于保存报警与求助信息。该表在基础字段之外，又通过升级方式增加了 `end_time`、`level`、`status` 和 `alarm_code` 字段，用于表达报警开始时间、解除时间、等级、处理状态和报警码。

`users` 表由 `AuthService` 创建，用于本地用户认证，字段包括用户名、密码、角色和创建时间。该表与业务数据表位于同一 SQLite 数据库文件中。

为了更直观地展示本地数据库中各核心数据表的组成以及它们在业务层面的关联关系，本节在表结构文字说明之后给出数据库 ER 图，从而帮助读者理解历史数据、报警事件、用户认证、设备信息与操作日志之间的组织方式，如图3-5所示。

### 3.6.3 数据写入与查询关系

在实时数据写入方面，Qt 客户端每接收到一条有效 MQTT 数据，就会构造 `SensorData` 对象，并将时间、温度、湿度、PM2.5、空气质量、电流和水流写入 `data` 表。历史页面、统计图表和导出功能均基于该表进行查询。

在报警数据维护方面，Qt 会持续跟踪当前报警码集合与上一时刻报警码集合的差异。对于新出现的报警码，程序向 `alarm_info` 表写入新的报警记录；对于已经消失的报警码，程序调用 `updateAlarmResolved` 回填结束时间并将状态改为 `resolved`。Web 一键求助信息也写入同一张表，但等级使用“求助”。

在设备状态管理方面，Qt 会将界面中的设备元信息同步到 `device_info` 表；当系统检测到设备离线时，则将离线持续时长持续写入 `device_abnormal_records` 表。远程控制操作则统一写入 `remote_exec_logs` 表，形成完整的操作痕迹。

此外，Web 看板历史数据导出脚本会直接访问 `data` 表，并按天汇总统计平均电流、平均水流、日耗电量和日耗水量，再生成前端可直接加载的 `history_data.js` 文件。说明本系统的数据库不仅用于存储，也直接服务于展示侧的数据准备。

## 3.7 本章小结

本章结合当前真实源码，对系统的总体设计进行了梳理。系统采用“STM32 现场控制、ESP32 网络接入、MQTT 消息传输、Qt 本地管理、SQLite 数据存储、Web 轻量展示”的总体方案，其中 STM32 负责核心控制闭环，ESP32 负责协议桥接，Qt 负责管理与持久化，Web 负责展示与求助交互。系统没有独立后端服务器，整体结构围绕本地终端和消息中转服务展开。

从实现结果看，系统已经形成了较为完整的数据采集、状态判定、联动控制、远程下发、报警记录和多端展示链路。后续章节可在此基础上进一步展开系统详细实现与测试分析。
