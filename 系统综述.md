# 灾后临时安置点智慧水电管理与环境监测系统 — 系统综述

## 一、项目概述

本项目设计并实现了一套面向灾后临时安置点的智慧水电管理与环境监测系统。系统以STM32F103C8T6为主控芯片，集成6类环境与资源传感器（温度、湿度、PM2.5、空气质量、电流、水流），通过ESP32 WiFi模块经MQTT协议接入巴法云物联网平台，为Qt桌面端（工作人员侧）和Web看板（灾民侧）提供实时数据监控、远程设备控制与智能预警联动功能。

- **课题名称**：灾后临时安置点智慧水电管理与环境监测系统设计
- **学校**：应急管理大学
- **主控芯片**：STM32F103C8T6（ARM Cortex-M3, 72MHz, 64KB SRAM, 128KB Flash）
- **IDE**：Keil MDK v5（ARMCC v5.06）

---

## 二、系统架构

系统采用**"云、边、端"三层协同**的物联网架构：

```
┌─────────────────────────────────────────────────────────┐
│                    云平台应用层                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ 巴法云 MQTT   │  │ Qt 桌面端    │  │ Web 看板     │  │
│  │ (bemfa.com)  │  │ (工作人员)   │  │ (灾民)      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    边缘网关层                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │        ESP32 WiFi + MQTT 桥接 + 阈值缓存          │  │
│  └──────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    感知执行层                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │  STM32F103C8T6 — 6路传感器 + 5路执行器 + OLED    │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 数据流

1. **上行**：STM32每1秒采集传感器数据→封装JSON→UART(115200bps)→ESP32→WiFi+MQTT→巴法云→Qt/Web订阅接收
2. **下行**：Qt/Web发布MQTT控制指令→巴法云→ESP32订阅接收→解析转发→UART→STM32执行

---

## 三、硬件系统

### 3.1 主控与通信

| 组件 | 型号/参数 | 说明 |
|------|-----------|------|
| 主控 | STM32F103C8T6 | ARM Cortex-M3, 72MHz |
| 通信 | ESP32-WROOM-32 | WiFi+蓝牙, 240MHz双核 |
| 接口 | USART1 (PA9/PA10) | STM32↔ESP32, 115200 8N1 |
| 显示 | SSD1306 0.96" OLED | 128×64, I²C接口 |

### 3.2 传感器引脚分配

| 模块 | 信号 | 引脚 | 接口类型 | 核心参数 |
|------|------|------|----------|----------|
| DHT11 温湿度 | DATA | PB13 | 单总线 | 0~50℃, ±2℃; 20~90%RH, ±5%RH |
| GP2Y10 粉尘 | ADC | PA2 | ADC1_IN2 | 0~500μg/m³, 光散射原理 |
| GP2Y10 LED | 控制 | PB12 | GPIO | 10ms周期脉冲驱动 |
| MQ-135 空气质量 | ADC | PA1 | ADC1_IN1 | NH₃/CO₂/苯等综合检测 |
| ACS712 电流 | ADC | PA0 | ADC1_IN0 | ±5A量程, 185mV/A |
| YF-S401 水流 | 脉冲 | PA5 | EXTI5 | 450脉冲/L |

### 3.3 执行器引脚分配

| 执行器 | 引脚 | 说明 |
|--------|------|------|
| 红灯 LED | PA15 | 需禁用JTAG, 高电平亮 |
| 绿灯 LED | PA11 | 高电平亮 |
| 黄灯 LED | PA12 | 高电平亮 |
| 蜂鸣器 | PB14 | 低电平响 |
| 风扇 | PB6 | 低电平转 |
| 舵机(窗户) | PA6 (TIM3_CH1) | 50Hz PWM, 500~2500µs |

### 3.4 供电方案

```
12V适配器 → LM2596(调5V) → 面包板电源模块
  ├── 5V母线：MQ135、舵机、风扇、GP2Y10、ACS712、水流传感器、ESP32
  ├── 3.3V母线：OLED、DHT11
  └── GND：全部共地
```

---

## 四、三级联动阈值体系

### 4.1 阈值架构

系统为6个传感器各设定**预警阈值**和**告警阈值**两级门限：

| 传感器 | 预警阈值（默认） | 告警阈值（默认） | 推导规则 |
|--------|-----------------|-----------------|----------|
| 温度 | 18~32℃ | >38℃ 或 <10℃ | 预警=告警-6(上)/+8(下) |
| 湿度 | 30~70% | >85% 或 <20% | 预警=告警-15(上)/+10(下) |
| PM2.5 | >75μg/m³ | >150μg/m³ | 预警=告警/2 |
| 空气质量 | >100ppm | >200ppm | 预警=告警/2 |
| 电流 | >10A | >15A | 预警=告警×2/3 |
| 水流 | >5L/min | >10L/min | 预警=告警/2 |

**核心设计**：Qt桌面端仅下发告警阈值（ta/tb/ha/hb/pa/aa/ca/fa），STM32固件自动调用`derive_warn_from_alarm()`推导对应的预警阈值。这避免了Qt和STM32阈值不一致的风险。

### 4.2 三级联动规则

系统取6个传感器中**最差级别**作为系统整体联动级别：

| 级别 | 判定条件 | LED | 风扇 | 蜂鸣器 | 舵机(窗户) |
|------|----------|-----|------|--------|------------|
| 正常(0) | 全部正常 | 绿灯亮 | 停 | 停 | 关 0° |
| 预警(1) | 任一预警 | 黄灯亮 | 转 | 慢响(2s/滴) | 关 0° |
| 警报(2) | 任一告警 | 红灯亮 | 转 | 常响 | 开 90° |

### 4.3 滞回与防抖机制

为防止阈值边界处的频繁抖动，系统实现了完整的滞回控制：

- **进入预警**需连续3次（3秒）超标
- **进入告警**需连续5次（5秒）超标
- **退出预警**需连续3次低于（阈值-滞回偏移量）
- **退出告警**需连续5次低于（阈值-滞回偏移量）
- 每种传感器有独立的滞回偏移量（如温度预警滞回2℃、告警滞回3℃）

### 4.4 组合联动规则

系统实现了三项组合联动判断：

| 组合 | 触发条件 | 效果 |
|------|----------|------|
| 温度+PM2.5 | 两者同时达预警以上 | 级别强制提升至告警（火灾风险） |
| PM2.5+AQ | 两者同时达预警以上 | 级别强制提升至告警（烟雾污染） |
| 水流+电流 | 两者同时达预警以上 | 风扇强制关闭（管道异常保护） |

### 4.5 手动/自动优先级

- 每个执行器（蜂鸣器/风扇/舵机/LED）独立维护手动/自动模式标志
- **手动优先**：远程下发控制指令后，对应执行器进入手动模式
- **自动接管**：手动模式关闭后，自动联动逻辑重新接管
- **看门狗保护**：手动模式下5分钟无新指令，自动切回AUTO模式（防止误操作导致永久手动）
- 支持全局MANUAL/AUTO模式切换和全局RESET恢复

---

## 五、水电剩余量模拟

### 5.1 电量模拟

- **原理**：每秒用ACS712采集瞬时电流I(mA)，积分累加消耗 `battery_used_mAh += I/3600`
- **默认容量**：10000 mAh（模拟应急电源/充电宝）
- **剩余百分比**：`(1 - battery_used/capacity) × 100`，下限0%
- **漂移处理**：DC blocker基线追踪（α=0.05, τ≈20s）+ 死区15mA + 连续3秒<死区强制归零
- **工频抑制**：600次×68µs = 40.8ms采样窗口，恰好覆盖2个50Hz周期

### 5.2 水量模拟

- **原理**：YF-S401水流传感器脉冲累加（450脉冲/L），直接取累计用量
- **默认容量**：10L（便于吹气演示验证）
- **剩余百分比**：`(1 - total_flow/capacity) × 100`，下限0%
- **原子保护**：ISR只做整数脉冲累加，浮点转换在取值函数中进行，32位读操作关中断防撕裂

### 5.3 远程校准命令

| 命令 | 示例 | 作用 |
|------|------|------|
| `bc` | `bc10000` | 设置电池总容量(mAh)并重置已用量 |
| `bp` | `bp50` | 直接设定剩余电量百分比（人工校准） |
| `wc` | `wc10` | 设置水箱总容量(L)并重置已用量 |
| `wp` | `wp80` | 直接设定剩余水量百分比（人工校准） |

---

## 六、通信协议

### 6.1 上行数据（STM32→ESP32→云端）

JSON格式，每1秒发送，ESP32每2秒转发至MQTT：

```json
{
  "sen": {"t":26,"h":60,"pm":35,"aq":52,"f":1.23,"i":450},
  "res": {"bp":85,"wp":72,"tu":1500,"wu":20,"br":8500,"wr":80,
          "ps":0,"bc":10000,"tc":1000,"bt":5760,"wt":1440},
  "lv": {"link":0,"env":0,"d":[0,0,0,0,0,0]},
  "act": {"bz":0,"fan":0,"svo":0,"led":[0,1,0]},
  "mod": {"gl":0,"bz":0,"fan":0,"svo":0,"led":0},
  "alm": [],
  "actn": 0,
  "flt": []
}
```

| 分组 | 主要字段 | 含义 |
|------|---------|------|
| sen | t,h,pm,aq,f,i | 温/湿/PM2.5/AQ/水流(L/min)/电流(mA) |
| res | bp,wp,br,wr,bt,wt | 电量%/水量%/剩余mAh/剩余cL/电池可用分钟/水量可用分钟 |
| lv | link,env,d[6] | 系统级别/环境级别/6传感器独立级别 |
| act | bz,fan,svo,led[3] | 执行器当前状态 |
| mod | gl,bz,fan,svo,led | 各执行器手动/自动模式标志 |
| alm | [101,121,...] | 活跃告警码数组 |
| actn | 位掩码 | 自动采取的措施标志 |
| flt | [901,...] | 传感器故障码数组 |

### 6.2 下行控制（云端→ESP32→STM32）

支持多种格式兼容：

- **数字指令（旧工具兼容）**：`0`~`8`（单字符）
- **JSON控制指令**：`{"code":4,"name":"风扇开启"}`
- **JSON阈值修改**：`{"th":{"ta":40,"pa":160}}`
- **新协议JSON**：`{"cmd":"mode","val":"AUTO"}` / `{"cmd":"reset"}` / `{"cmd":"reset_th"}`

---

## 七、各子系统功能摘要

### 7.1 STM32嵌入式固件
- C语言，Keil MDK开发，前后台系统架构
- 6路传感器采集（1s周期）+ 5路执行器控制
- 三级联动逻辑 + 滞回防抖 + 组合联动规则
- OLED本地四行显示（温/湿/PM2.5&/AQ/水流/电流）
- 水电剩余量积分模拟
- USART1中断接收+环形缓冲+非阻塞行读取
- 兼容JSON/旧数字/旧KKVVV三种下行格式

### 7.2 ESP32边缘网关
- Arduino框架，C/C++开发
- UART(Serial2)接收STM32数据 + WiFi + MQTT桥接
- 2秒定时上传，直接转发完整JSON（不做重建）
- 下行指令解析转发（数字指令/JSON阈值/新协议命令）
- 阈值本地缓存（16个参数）
- th_sync防循环机制
- WiFi断线重连 + MQTT自动重连（级联恢复）
- MQTT缓冲区1024字节（适配大数据包）

### 7.3 Qt桌面端
- Qt 6 + C++17，CMake构建
- 深色无边框主题（暗蓝色调）
- 6大功能页面：实时监控/水电管理/历史数据/告警管理/设备管理/系统设置
- QtCharts多传感器趋势图+历史折线图
- SQLite 3数据库（7张表），D:/System_UI_Data/
- 用户认证系统（管理员/普通用户+密码加密）
- MQTT通信（QMqttClient）+ 自动重连（指数退避，上限60秒）
- 远程设备控制（阈值下发+执行器控制+控制日志）
- Excel导出（原生XLSX格式）
- Web端一键求助实时接收与弹窗通知
- 传感器级别直接从STM32 lv.d数组读取，不在Qt端重复计算

### 7.4 Web看板
- 纯前端技术栈（HTML5+CSS3+原生JS）
- MQTT.js直连巴法云（MQTT over WebSocket）
- Chart.js水电趋势图 + SVG电池/水箱图标 + SVG温度曲线
- 三栏布局：环境指标(左) + 水电管理(中) + 报警预警(右)
- 联动Toast弹窗（右下角，10秒自动消失）
- 一键求助功能（4种求助类型→独立MQTT主题→Qt接收）
- 24小时环境预测（天气图标+温度曲线+风力/UV/空气质量）
- 颜色语义化：绿/黄/红三色系统贯穿全部界面
- 适配1920×1080，向下兼容1366×768

---

## 八、数据库设计

**位置**：`D:\System_UI_Data\system_ui.sqlite`

| 表名 | 用途 | 核心字段 |
|------|------|----------|
| data | 简化核心指标（历史查询） | ts, temperature, humidity, pm25, air_index, current_a, flow_l_min |
| alarm_info | 报警记录（含求助） | alarm_time, end_time, sensor_name, alarm_content, level, status, alarm_code |
| device_info | 设备元数据 | device_id, device_name, device_type, location, firmware_version |
| device_abnormal_records | 设备离线记录 | sensor_name, offline_time, offline_duration_sec |
| remote_exec_logs | 远程控制日志 | execute_time, device_id, command_text, result_text |
| users | 用户账号 | username, password, role, phone, email |

---

## 九、已解决的关键技术问题

| 问题 | 解决方案 |
|------|----------|
| ACS712空载漂移 | DC blocker基线追踪(α=0.05)，死区15mA，连续3秒<死区强制归零 |
| DHT11可能卡死MCU | 所有while等待循环加入超时计数(retry>200退出) |
| GP2Y10空读偏高 | NO_DUST_MV=420（商家参考值校正），厂商公式(V×11-420)×0.20 |
| LED红灯微亮（PA15 JTAG冲突） | `GPIO_PinRemapConfig(SWJ_JTAGDisable)`释放PA15 |
| OLED切页闪烁 | 仅写4行数据区（16空格×4行）替代全屏Clear |
| Keil中文注释编译错误 | ARMCC v5对GB2312处理不佳，统一改用`//`行注释 |
| 水流传感器ISR浮点运算 | ISR中只做整数脉冲累加，浮点转换移至取值函数 |
| 32位累加器读撕裂 | `__disable_irq()`/`__enable_irq()`原子保护 |
| ESP32 WiFi断线不重连 | 级联恢复：先detect→reconnect WiFi→reconnect MQTT |
| MQTT阈值消息循环 | th_sync标记+过滤：收到th_sync消息跳过处理 |
| GPIO PA15 JTAG冲突 | JTAG禁用+SWD保留，确保调试与LED同时可用 |
| 50Hz工频干扰 | 600次×68µs=40.8ms覆盖2个工频周期，算术平均消除 |
| 阈值频繁抖动 | 滞回偏移量+连续触发确认计数（进入3~5次/退出3~5次） |

---

## 十、系统运行指标

| 指标 | 数值 |
|------|------|
| 传感器采样周期 | 1秒 |
| MQTT上传间隔 | 2秒（ESP32） |
| 端到端延迟 | 1~5秒 |
| 报警响应时间 | ≤3秒 |
| OLED刷新周期 | 300ms |
| 传感器精度 | 温度±1.5℃, 湿度±4%RH, 电流R²=0.997 |
| 连续运行测试 | 72小时稳定运行，数据完整率100% |
| 数据库查询性能 | 支持最多10000条历史记录查询 |
| 水电积分误差 | 电量<2%, 水量<1L |

---

## 十一、目录结构

```
D:\AAA\
├── FinalSystem\               ← STM32固件 (Keil MDK v5)
│   ├── User\main.c            ← 主循环 + 联动逻辑 + 水电积分
│   ├── Hardware\              ← 传感器/执行器驱动模块
│   │   ├── ACS712.c/h        ← 电流传感器（600次采样+DC blocker）
│   │   ├── DHT11.c/h         ← 温湿度（单总线+超时防卡死）
│   │   ├── GP2Y10.c/h        ← 粉尘浓度（厂商公式+滑动滤波）
│   │   ├── MQ_135.c/h        ← 空气质量（EMA低通滤波）
│   │   ├── waterFlow.c/h     ← 水流传感器（ISR脉冲累加+原子保护）
│   │   ├── OLED.c/h          ← SSD1306 0.96"（软件I²C）
│   │   ├── servo.c/h         ← SG90舵机（50Hz PWM）
│   │   ├── led.c/h           ← RGB三色LED（JTAG禁用）
│   │   ├── buzzer.c/h        ← 蜂鸣器（预警/警报差异化节奏）
│   │   ├── motor.c/h         ← 风扇控制 + 电机驱动
│   │   ├── serial.c/h        ← USART1通信 + JSON解析 + 环形缓冲
│   │   └── pwm.c/h           ← TIM2/3/4 PWM（LED/舵机/电机）
│   ├── System\               ← 系统模块（Delay/ADCX）
│   ├── Start\                ← 启动文件
│   └── Library\              ← STM32标准外设库
├── esp32\                     ← ESP32固件 (Arduino IDE)
│   └── stm32_to_esp32_uart_test.ino ← MQTT桥接 + 阈值缓存 + 断线重连
├── dashboard\                 ← Web看板（纯前端）
│   ├── index.html            ← 三栏主页面
│   ├── js\
│   │   ├── app.js            ← 数据解析 + Toast弹窗 + 报警 + 求助
│   │   ├── config.js         ← MQTT连接配置
│   │   └── history_data.js   ← 历史数据接口
│   └── css\
│       └── dashboard.css     ← 看板样式 + 动画
├── System_UI\                 ← Qt桌面端 (Qt 6 + CMake)
│   ├── main.cpp              ← 应用入口 + 登录流程
│   ├── mainwindow.cpp/h      ← 主窗口 + 6页面 + MQTT处理
│   ├── mqtt.cpp/h            ← MQTT客户端封装（自动重连）
│   ├── databasemanager.cpp/h ← SQLite数据库管理（7表）
│   ├── authservice.cpp/h     ← 用户认证服务
│   ├── logindialog.cpp/h     ← 登录对话框
│   └── dbconfig.h            ← 数据库路径配置
├── CLAUDE.md                  ← 项目开发文档
├── 系统说明文档.md            ← 完整系统说明
└── 系统综述.md                ← 本文档
```
