# 第4章 详细设计与实现

## 4.1 STM32控制程序详细设计与实现

### 4.1.1 主程序结构

【设计目标】  
STM32F103 是本系统的现场主控单元，其设计目标是在无操作系统条件下同时完成多源传感采集、执行器联动控制、状态显示以及与网关模块的数据交换。该部分需要优先保证本地控制闭环的实时性与确定性，使系统在上位端断连时仍能维持基本监测与控制功能。

【实现方案】  
控制程序采用裸机循环结构组织全部任务。系统上电后依次完成传感器、执行器、显示接口和串口通信接口初始化，并将风扇、舵机、蜂鸣器和指示灯置于默认安全状态。随后以固定短周期作为基础节拍，在同一主循环内分层安排串口命令处理、周期采样、联动控制、OLED 刷新以及模式维护等任务，其中环境采集周期约为 1 s，显示刷新周期约为 300 ms。

【核心机制】  
该部分采用“统一节拍驱动 + 多周期分发”的任务组织方式，将不同频率的工作纳入同一控制框架，从而避免了复杂的多线程调度开销。与此同时，系统同时维护自动模式与人工干预模式。在人工模式下，执行器可按下行指令直接控制；当人工控制持续一定时间后，系统会自动回到自动模式，防止现场长期停留在人工接管状态。

【作用与意义】  
该设计实现了在资源受限单片机平台上的稳定任务调度，使监测、控制和通信能够在统一时序下协同运行，从而提高了系统现场控制的实时性与独立运行能力，满足了灾后临时安置场景对本地自治控制的实际需求。

为了使主程序结构不只停留在文字层面的任务说明，本节在总体机制分析之后给出 STM32 主程序流程图，用于展示系统上电初始化、周期采样、状态判定、联动执行、数据打包与显示刷新之间的运行顺序，如图4-1所示。

### 4.1.2 传感器采集实现

【设计目标】  
系统需要同时获取环境状态与资源使用状态，因此传感器采集模块的目标不仅是读取原始测量值，还要将不同类型的电信号统一转换为可比较、可显示、可存储的工程量，为后续判定与联动提供一致的数据基础。

【实现方案】  
STM32 端接入了 DHT11、MQ-135、GP2Y10、ACS712 以及水流传感器，分别完成温湿度、空气质量、颗粒物浓度、电流和水流信息采集。采样结果在本地完成单位换算与异常值处理后，再进一步计算资源消耗量、剩余量及剩余比例等派生数据，使上层节点接收到的已是可直接用于业务处理的数据。

【核心机制】  
该模块的关键在于“原始量采集 + 工程量归一化 + 资源量推导”三级处理过程。环境类数据以当前状态表达现场变化，资源类数据则通过累计用电和累计用水形成消耗统计。对于明显超出合理范围的采样值，系统会进行过滤处理，以避免瞬时异常干扰后续状态判断。

【作用与意义】  
该设计实现了对环境数据和资源数据的统一采集与标准化处理，从而提高了上层展示与分析的一致性，有效解决了不同传感器量纲不统一、直接上报难以业务化使用的问题。

在多类传感器中，GP2Y10 粉尘传感器的采样精度与 LED 驱动时序密切相关，因此有必要单独给出其关键脉冲驱动与采样时序关系，以说明系统如何在固定周期内完成发光、延时采样与熄灭控制，如图4-2所示。

### 4.1.3 阈值判断与三级状态

【设计目标】  
现场监测系统不仅需要识别异常，更需要区分异常程度。因此，本系统将状态判定目标定义为三级状态识别，即正常、预警和告警，并要求状态切换具有抗抖动能力，避免测量值在临界点附近波动时频繁误报。

【实现方案】  
系统针对温度、湿度、PM2.5、空气质量、电流和水流分别设置状态判定规则。阈值体系采用“告警阈值为主、预警阈值派生”的设计方式，恢复默认配置时由系统自动推导预警区间。在判定结果上，各监测项均输出 `NORMAL`、`WARN`、`ALARM` 三类状态，为后续全局联动提供统一表达。

【核心机制】  
状态切换采用“阈值比较 + 连续确认 + 滞回恢复”的状态机策略。也就是说，系统不会因为单次超限立即提升等级，而是要求连续多次触发后才进入预警或告警；当数据恢复正常时，也需要经过连续恢复确认后才回落等级。该机制有效抑制了传感器瞬时抖动带来的误判问题。

【作用与意义】  
该设计实现了稳定的三级风险判定机制，从而提高了告警识别的可信度，并为后续执行器联动、上位机报警记录和界面分级显示提供了可靠依据。

为了更清楚地表达各监测指标从原始采样值到正常、预警和告警三级状态的判定过程，以及全局风险等级与联动输出之间的关系，本节在阈值机制说明之后给出三级状态判定示意图，如图4-3所示。

同时，考虑到本系统状态切换并非简单阈值越界即立刻升级，而是引入了连续确认和滞回恢复机制，因此有必要进一步补充滞回防抖示意图，用于解释系统如何抑制临界值附近的频繁误触发，如图4-4所示。

### 4.1.4 组合联动逻辑

【设计目标】  
单一指标超限并不能完全反映现场风险，因此系统需要在单项判定基础上进一步识别多指标同时异常的情形，并据此形成更符合实际场景的联动控制策略。

【实现方案】  
系统在单项状态基础上同时计算全局状态和环境状态。全局状态综合六类监测指标中的最高风险等级，环境状态重点关注温湿度、PM2.5 和空气质量等环境相关指标。在此基础上，系统进一步识别“温度与粉尘同时异常”“粉尘与空气质量同时异常”“水流与电流同时异常”等组合场景，并根据组合结果调整系统风险等级和执行器动作。

【核心机制】  
联动逻辑采用“单项状态汇总 + 组合条件增强 + 执行策略映射”的控制方法。环境相关异常主要驱动风扇和舵机动作，而资源类组合异常则优先表达为风险提升与自动动作限制，避免在疑似故障场景下继续执行不合适的自动控制。由此，执行器动作不再仅由单个阈值决定，而是由整体状态共同决定。

【作用与意义】  
该设计实现了由单点监测向场景识别的提升，使系统能够对复合异常作出更合理的控制响应，从而提高了环境调节和风险预警的有效性，满足了临时安置点复杂工况下的实际需求。

### 4.1.5 JSON数据打包与串口发送

【设计目标】  
为了使 ESP32、Qt 客户端和 Web 看板获得一致的数据视图，STM32 需要将采集结果、状态结果和控制结果组织为统一的数据报文，并通过串口向网关侧稳定输出。

【实现方案】  
系统采用 JSON 作为上行数据载体，并通过 UART 以 115200 波特率发送。报文整体由传感信息、资源信息、等级信息、执行器状态、工作模式、报警信息、联动动作信息和故障信息等若干区段组成，对应结构保持稳定，便于网关和上位端按统一协议解析。

【核心机制】  
该部分的关键不在于简单上传采样值，而在于输出“完整状态快照”。其中，环境数据与资源数据用于描述现场状态，等级信息用于描述风险判定结果，执行器状态与工作模式用于描述控制状态，报警与故障信息用于描述异常事件，联动动作信息则用于表明系统已采取的自动措施。实际上传的数据结构由 `sen`、`res`、`lv`、`act`、`mod`、`alm`、`actn` 和 `flt` 等区段构成，形成统一的消息模型。

【作用与意义】  
该设计实现了感知层状态向通信层的标准化映射，从而提高了上位端解析的一致性，减少了多终端重复定义数据格式的问题，为系统后续的可视化、存储和控制闭环提供了统一数据基础。

### 4.1.6 下行命令解析

【设计目标】  
系统需要支持远程模式切换、执行器控制和阈值调整，因此 STM32 必须具备稳定的下行命令接收与解析能力，同时兼容当前图形界面所使用的命令格式和早期控制格式。

【实现方案】  
下行命令通过串口按行接收，并统一纳入结构化解析流程。当前控制链路主要支持三类信息：设备控制命令、阈值设置命令以及模式或恢复类命令。其中，设备控制可用于蜂鸣器、风扇、舵机和指示灯的远程干预；阈值设置用于参数更新；模式和恢复命令用于自动模式切换和默认配置恢复。

【核心机制】  
命令处理采用“分帧接收 + 类型识别 + 分类执行”的策略。对于当前系统实际使用的下行协议，ESP32 和 Qt 主要围绕 `code`、`th` 和 `cmd` 三类结构进行交互；STM32 在解析时则分别映射到控制、阈值和模式处理逻辑。对于告警阈值更新，系统会同步重建相应的预警范围，从而保证本地判定机制的一致性。

【作用与意义】  
该设计实现了统一下行控制入口，有效解决了多控制端命令格式并存的问题，从而增强了远程调控的稳定性和兼容性，为人工干预和参数维护提供了可靠支撑。

## 4.2 ESP32通信网关实现

### 4.2.1 WiFi连接与重连机制

【设计目标】  
ESP32 在系统中承担现场控制端与云端消息服务之间的连接任务，因此首先需要保证无线网络接入的连续性，使数据能够稳定进入 MQTT 通信链路。

【实现方案】  
系统上电后由 ESP32 主动连接预设 WiFi 网络，并设置有限次数的重试机制。运行过程中一旦检测到网络断开，网关不会直接重复尝试 MQTT 会话，而是优先恢复无线网络连接，再继续后续消息服务重连。

【核心机制】  
该部分采用“链路分层恢复”策略，即先恢复网络层，再恢复应用层。通过这种顺序控制，可以避免在 WiFi 不可用时反复尝试消息服务连接，减少无效重试和阻塞现象。该机制特别适合部署环境中无线网络波动较大的实际场景。

【作用与意义】  
该设计实现了网关节点在网络异常后的自动恢复，从而提高了系统云端连接的可持续性，降低了现场维护频率，满足了安置点长期运行场景对通信自恢复能力的要求。

### 4.2.2 MQTT连接与订阅发布

【设计目标】  
在网络接入建立后，ESP32 需要完成与消息服务器的稳定会话维护，并承担上行发布与下行订阅两项任务，使现场节点与上位端形成双向通信通道。

【实现方案】  
系统采用 PubSubClient 构建 MQTT 客户端，与 `bemfa.com:9501` 建立连接，并将 `test001up` 作为主业务主题。连接建立后，网关立即完成主题订阅，从而同时具备接收下行控制和上传现场数据的能力。对于上行数据，网关并非串口一到即发，而是按固定时间窗口进行发布，从而在实时性与消息频率之间取得平衡。

【核心机制】  
该部分采用“连接建立后立即订阅 + 周期性保持会话 + 按更新触发发布”的通信机制。网关仅在串口侧产生新数据时进入发布流程；若本次上行失败，则保留待发标记并在后续时机重试。与此同时，较大的 MQTT 缓冲区保证了完整 JSON 报文能够一次性传输。

【作用与意义】  
该设计实现了串口采样节奏与网络上传节奏的适度解耦，从而提高了 MQTT 通信的稳定性，降低了重复上报压力，并满足了主业务主题双向复用的通信需求。

为了更直观地说明 STM32、ESP32、MQTT 服务以及 Qt 与 Web 端之间的消息交互顺序，本节在连接与发布订阅机制分析之后给出 MQTT 通信时序图，以展示系统在上行监测、下行控制和求助消息传递中的完整链路，如图4-5所示。

### 4.2.3 串口数据接收与缓存

【设计目标】  
ESP32 与 STM32 之间的数据交换需要具有明确帧边界和缓存保护能力，以确保来自现场控制端的 JSON 报文能够完整、连续地进入云端通信流程。

【实现方案】  
网关通过独立串口与 STM32 保持 115200 波特率通信，采用换行符作为帧结束标志，对字节流进行逐步拼接。当接收到完整一帧后，系统先完成文本清理与缓存，再将该帧整体保存为最近一次有效数据，供后续 MQTT 发布使用。

【核心机制】  
该部分采用“流式接收 + 分帧缓存 + 上限保护”的实现思路。换行符用于界定完整消息边界，缓存上限用于防止异常数据持续堆积造成内存膨胀。此外，网关对已缓存数据只进行轻量级状态读取，不对其业务结构做重建，以保持报文原貌。

【作用与意义】  
该设计实现了串口文本协议的稳定承载，有效解决了字节流环境下报文边界不清和异常数据膨胀的问题，为后续 MQTT 透传提供了可靠前提。

### 4.2.4 JSON透传策略

【设计目标】  
网关层的主要职责应是桥接而非重算，因此 ESP32 需要避免对 STM32 已完成判定的数据进行二次加工，以减少中间层与控制层之间的语义偏差。

【实现方案】  
系统采用 JSON 原样透传策略。即 STM32 将完整状态报文发送至 ESP32 后，网关仅负责缓存、判断是否可发布并将原始字符串上送 MQTT，不在网关端重新组织字段，也不重新计算等级、报警或联动结果。

【核心机制】  
该策略的核心是将 STM32 视为现场业务语义的唯一输出端，而将 ESP32 定位为协议适配和链路桥接节点。这样一来，Qt 客户端、Web 看板以及串口原始数据之间保持同构关系，上位端可基于统一报文结构进行解析与展示。

【作用与意义】  
该设计实现了控制层与通信层的清晰解耦，从而降低了网关实现复杂度，避免了中间层字段重组引起的数据不一致问题，增强了系统跨终端展示的一致性。

### 4.2.5 下行控制转发逻辑

【设计目标】  
系统的下行控制既来自桌面客户端，也可能来自兼容旧格式的控制端，因此网关需要将不同形式的控制消息统一转发给 STM32，保证远程控制链路可用。

【实现方案】  
ESP32 将接收到的 MQTT 消息划分为数字型控制、阈值设置和结构化命令三类处理。对于简化控制消息，网关可直接转换为控制编号下发；对于阈值设置消息，网关按照阈值项逐项转交给 STM32；对于包含模式切换、复位或结构化控制意图的消息，则以整帧方式继续向下传递。

【核心机制】  
该部分采用“消息分类适配”策略，以兼容系统当前真实存在的多种控制表达方式。其中，Qt 客户端主要围绕 `code`、`th` 和 `cmd` 三类结构进行下发，网关负责将这些结构映射为 STM32 可识别的串口命令形态。该机制既保留了结构化命令的表达能力，又兼顾了简化控制路径的兼容需求。

【作用与意义】  
该设计实现了上位控制接口与底层串口协议之间的平滑衔接，有效解决了控制格式差异带来的适配问题，从而提高了远程操作链路的可用性和系统集成度。

### 4.2.6 防回环机制

【设计目标】  
由于系统将上行数据与下行控制同时置于 `test001up` 主题中，若不进行甄别，网关可能会把自身刚上传的状态报文误认为新的控制命令，从而引发消息回环。

【实现方案】  
系统在网关接收端增加了结构识别过滤。对于包含上行感知区段特征的数据，ESP32 不再继续下发；对于阈值同步类回传消息，也在接收阶段直接丢弃，以阻断重复传播。

【核心机制】  
该机制采用“基于消息结构特征的快速判别”方法，而不是新增独立主题。系统利用上行报文与下行控制在字段组织上的天然差异，对包含 `sen` 的数据视为状态上报，对 `th_sync` 视为同步回传，从而避免同主题双向通信造成的闭环传播。

【作用与意义】  
该设计有效解决了单主题双向复用条件下的回环问题，保证了通信链路的稳定性，也使系统在不增加主题复杂度的前提下维持了双向交互能力。

## 4.3 Qt客户端实现

### 4.3.1 MQTT连接与订阅

【设计目标】  
Qt 客户端在系统中承担数据中心和业务管理中心角色，因此需要持续接收现场状态消息与求助消息，并在网络中断后具备自动恢复能力。

【实现方案】  
桌面端基于 `QMqttClient` 建立消息连接，与 `bemfa.com:9501` 保持会话，并订阅 `test001up` 与 `WebQT1` 两个主题。前者用于接收主业务数据与控制反馈，后者用于接收 Web 看板发起的一键求助信息。连接中断后，客户端并不依赖人工重新登录，而是通过自动重连与重订阅机制恢复工作。

【核心机制】  
该部分采用“连接状态驱动”的管理策略。系统持续监听 MQTT 会话状态，当连接成功时立即恢复订阅；当连接断开时按递增间隔发起重连，避免频繁短周期尝试造成额外负载。这样，通信稳定性被提升为客户端运行的基础能力，而非附属功能。

【作用与意义】  
该设计实现了桌面端对现场数据和求助事件的持续接入，从而提高了客户端的在线可用性，并保证了系统作为管理节点的业务连续性。

### 4.3.2 数据解析流程

【设计目标】  
桌面端接收到 MQTT 消息后，需要将原始报文转化为界面显示、报警处理、历史存储和远程控制反馈所需的业务信息，因此必须建立清晰、稳定的数据解析流程。

【实现方案】  
客户端首先对 MQTT 负载进行 JSON 合法性校验，然后优先按照当前系统使用的结构化报文进行解析。解析内容主要包括环境数据、资源数据、等级数据、报警数据、动作数据和模式数据等几类信息。对于早期兼容格式，系统保留回退解析能力，但不再作为主路径使用。

【核心机制】  
该流程的关键在于“以现场控制端判定结果为准”。也就是说，Qt 不再本地重复计算各传感器风险等级，而是直接消费 STM32 输出的等级与报警结果，并将动作信息和报警集合规范化处理后交给界面刷新和数据库模块。这样可以避免桌面端与现场控制端分别判定而出现结果不一致的问题。

【作用与意义】  
该设计实现了通信数据向业务状态的高效转换，从而增强了多终端状态一致性，有效解决了重复计算导致的逻辑分叉问题，使 Qt 客户端真正成为系统的数据汇聚与业务承载节点。

### 4.3.3 实时UI刷新逻辑

【设计目标】  
管理界面既要准确反映当前环境状态，又要让使用者迅速区分正常、预警、告警和离线等不同运行状态，因此界面刷新不仅是数据显示问题，更是状态表达问题。

【实现方案】  
客户端在收到有效实时数据后，同步更新总体状态、各监测指标卡片、设备状态表和趋势图。各项指标按照风险等级切换颜色、文字和标识；趋势图仅保留最近一段时间的数据点，以突出实时变化特征。当系统在一定时间窗口内未收到新的 MQTT 数据时，界面则转入离线显示模式，并将实时值降级展示。

【核心机制】  
该部分采用“等级驱动显示 + 时间驱动离线判定”的双机制设计。一方面，所有界面样式尽量直接映射来自现场控制端的等级结果；另一方面，客户端依据最近一次有效消息时间判断在线状态，从而把“设备异常”和“通信中断”区分为两类不同的界面反馈。

【作用与意义】  
该设计实现了对实时状态的分层可视化表达，从而提高了值班人员的判读效率，也增强了系统对通信异常的可识别性，满足了答辩和实际演示场景中对系统可观察性的要求。

为了使界面实现效果与前述刷新逻辑形成对应关系，本节在实时 UI 机制说明之后给出 Qt 实时监控页面截图，用于展示系统对环境状态、风险等级、趋势曲线和总体运行状态的综合呈现效果，如图4-6所示。

除环境监测外，客户端还需要对水电资源状态进行实时表达，因此在实时监控页面之后补充 Qt 水电数据页面截图，以进一步说明系统在资源剩余量、当前负载与水流趋势展示方面的实现效果，如图4-7所示。

### 4.3.4 SQLite写入实现

【设计目标】  
桌面端需要为历史查询、趋势统计和 Web 数据导出提供稳定的数据来源，因此必须在接收到有效实时消息后及时完成本地持久化。

【实现方案】  
Qt 客户端在完成实时数据解析与界面更新后，将当前采样组织为标准化记录写入 SQLite 本地数据库。写入对象主要包括时间信息以及环境与资源监测值，数据库连接、建库和建表工作由统一的数据管理模块负责维护。

【核心机制】  
该部分采用“有效消息即落库”的写入策略，即每接收到一条满足格式要求的实时消息，就生成一条对应历史记录。时间信息统一采用毫秒级时间格式，从而为后续按时间段统计、曲线绘制和导出聚合提供统一基准。

【作用与意义】  
该设计实现了实时监测与历史存储之间的直接衔接，从而形成稳定的数据积累链路，为后续报警追溯、历史分析和 Web 端展示提供了可靠的数据支撑。

### 4.3.5 报警记录生成与更新逻辑

【设计目标】  
监测系统中的报警若仅停留在瞬时提示层面，难以满足后续追踪与处理需求。因此，Qt 客户端需要将实时报警转化为具有生命周期的信息记录，实现从发生到结束的完整管理。

【实现方案】  
客户端持续比较当前报警集合与上一时刻报警集合。对于新出现的报警事件，系统立即写入报警记录；对于已恢复的报警事件，系统补写结束时间并更新状态。与此同时，来自 `WebQT1` 主题的一键求助事件也被纳入同一报警记录体系，作为特殊事件统一管理。

【核心机制】  
该模块采用“集合差分驱动的生命周期管理”机制。报警首次出现时建立活动记录，报警消失时转为已结束状态，人工处理时进一步完成闭环标记。这样，报警表不仅保存了事件内容，还保存了发生时刻、持续状态和结束状态，使报警记录从单点提示转变为可追踪事件流。

【作用与意义】  
该设计实现了报警信息的过程化管理，从而提高了系统事件追溯能力，也为后续统计分析、值班记录整理和求助事件处理提供了统一依据。

### 4.3.6 远程控制与阈值下发实现

【设计目标】  
桌面端不仅承担监视职责，还需要在必要时向现场设备发起干预，因此必须提供远程控制和阈值调整能力，并同步保留操作记录。

【实现方案】  
Qt 客户端通过 MQTT 向 `test001up` 主题发送控制消息，当前实际支持设备控制、阈值下发以及阈值恢复三类操作。其中，设备控制用于执行器开关和动作调整；阈值下发采用结构化配置方式发送；默认阈值恢复则通过专门命令触发现场控制端重置参数。所有远程操作均同步写入日志表。

【核心机制】  
该部分采用“控制指令发送与操作记录同步”的业务策略。桌面端关注的是控制意图表达与日志留痕，而具体告警与预警边界的最终形成由 STM32 根据本地阈值策略完成。也就是说，Qt 负责下发控制与配置请求，STM32 负责在现场执行并维持判定一致性。

【作用与意义】  
该设计实现了从监测到干预的业务闭环，有效解决了上位端只能看不能控的问题，并通过日志记录增强了系统操作的可追踪性和管理规范性。

为了使远程控制与阈值下发部分的实现结果更加直观，本节在业务逻辑说明之后给出 Qt 设备管理页面截图，用于展示阈值远程设置、执行器控制以及远程操作日志三部分功能在同一管理界面中的组织方式，如图4-8所示。

### 4.3.7 用户登录与权限控制

【设计目标】  
在无独立后端服务条件下，系统仍需具备基本账号识别能力，以满足不同使用者登录、注册和身份区分的实际需求。

【实现方案】  
Qt 客户端将用户认证信息保存在本地 `users` 表中，支持默认账户初始化、用户登录、用户注册和账户基本信息修改。当前系统设置了管理员和普通用户两类角色，并在主界面中持续保留当前登录身份信息。

【核心机制】  
该部分采用“本地认证 + 角色标识”的轻量实现方式。登录时通过本地用户表完成用户名与口令校验，注册时进行用户名可用性检查，登录成功后再将用户角色传递给主界面。现阶段角色作用主要体现在身份区分和界面信息显示层面，而未扩展为细粒度功能隔离。

【作用与意义】  
该设计实现了桌面端的基本身份管理能力，使系统在不依赖独立服务器的前提下仍具备用户使用边界，满足了课程设计和本科毕业论文系统对账号区分的基本要求。

## 4.4 Web看板实现

### 4.4.1 MQTT WebSocket连接

【设计目标】  
Web 看板的主要任务是以较低部署成本实现跨平台实时展示，因此其通信方式需要避免额外后端中转，并直接接入现有 MQTT 消息体系。

【实现方案】  
前端基于 MQTT.js 通过 WebSocket 连接 `wss://bemfa.com:9504/wss`，订阅主业务主题 `test001up`。页面同时维护连接状态显示，当消息链路建立或断开时，界面会同步更新在线标识，使浏览器端用户能够直观判断数据是否仍在实时刷新。

【核心机制】  
该部分采用“浏览器直连消息总线”的实现方式。通过将 MQTT 与 WebSocket 结合，前端无需自建 API 服务即可接收现场状态数据；结合自动重连配置，页面能够在短时网络波动后继续恢复消息接收。

【作用与意义】  
该设计实现了轻量级实时看板功能，从而降低了 Web 侧部署复杂度，增强了系统在不同终端上的展示灵活性，适合答辩展示和现场演示。

### 4.4.2 实时数据展示逻辑

【设计目标】  
Web 看板的实时展示目标并不是复制桌面端全部业务，而是以更轻量的方式突出环境、资源、设备和报警四类核心信息，形成面向浏览器场景的直观展示界面。

【实现方案】  
浏览器接收到 MQTT 消息后，先完成结构归一化处理，再将结果分发至指标卡片、资源卡片、执行器状态区和报警提示区等不同展示模块。环境类数据显示当前监测值及其状态等级，资源类数据显示剩余比例和消耗情况，控制类区域显示当前执行器状态和运行模式，报警区则给出异常与联动提示。

【核心机制】  
该部分采用“统一报文归一化 + 多区域同步刷新”的前端组织方式。看板不重新推导风险等级，而是直接消费来自现场控制端的等级、报警和动作结果；当系统等级变化时，再通过提示信息强化联动感知。由此，浏览器端与桌面端在状态来源上保持一致，在表达方式上保持轻量。

【作用与意义】  
该设计实现了面向 Web 场景的实时可视化展示，从而提高了系统信息发布效率，也避免了浏览器端重复承担业务判定职责，增强了多终端展示的一致性。

为了说明浏览器端在轻量展示场景下的整体界面布局和信息组织方式，本节在实时展示逻辑分析之后给出 Web 看板页面截图，使读者能够直观了解环境指标、资源状态、执行器状态和报警区域的可视化呈现效果，如图4-9所示。

### 4.4.3 Chart.js历史曲线实现

【设计目标】  
为了在浏览器端展示近阶段资源变化趋势，系统需要提供一种无需直接访问数据库、又便于图形化呈现的历史曲线方案。

【实现方案】  
Web 看板采用 Chart.js 绘制历史趋势图，数据来源于预先生成的 `history_data.js` 文件。当前图表重点展示近 7 天的用电量和用水量变化，并采用双纵轴方式分别表达电力消耗与水资源消耗，从而兼顾两类资源的量纲差异。

【核心机制】  
该部分采用“离线聚合数据 + 前端直接渲染”的方法。也就是说，历史统计并不在浏览器端实时计算，而是先由桌面端关联脚本完成聚合，再由 Web 页面直接读取结果并绘制图表。若历史数据文件暂未生成，页面则退回示例数据，保证界面结构完整。

【作用与意义】  
该设计实现了历史资源变化的轻量可视化表达，从而提高了浏览器端对中短期趋势的展示能力，也避免了前端直接连接数据库带来的依赖和复杂性。

### 4.4.4 一键求助功能

【设计目标】  
灾后临时安置点场景下，除常规监测外，还需要提供简洁直接的人机交互入口，使现场人员能够快速向管理端发起求助请求。

【实现方案】  
Web 看板在界面中设置浮动式求助入口，用户选择求助类型后，前端将求助类别、说明文字、时间和地点等信息封装为消息，并发布到 `WebQT1` 主题。浏览器端仅负责消息发送和交互提示，不直接参与数据库写入与事件落库。

【核心机制】  
该部分采用“轻量消息上报”思路，将求助行为视为系统内的一类事件消息。Web 端负责发起，Qt 客户端负责订阅、提醒和持久化，从而使求助功能能够在既有 MQTT 链路内完成闭环，而不引入新的服务端模块。

【作用与意义】  
该设计实现了面向现场使用者的低门槛交互能力，有效扩展了系统的人机协同范围，使 Web 看板不仅能展示状态，也能承担简易事件上报功能。

为了补充说明一键求助功能在前端交互层面的具体表现形式，本节在功能机制说明之后给出 Web 端提示界面截图，用于展示求助或告警触发后页面向用户反馈的可视化效果，如图4-10所示。

### 4.4.5 history_data.js导出机制

【设计目标】  
由于 Web 看板不直接访问 SQLite 数据库，因此需要建立一条从本地数据库到前端静态数据文件的转换路径，以支撑历史趋势图展示。

【实现方案】  
系统通过 Python 脚本读取桌面端 SQLite 数据库中的 `data` 表，对按日粒度的监测数据进行统计，生成包含历史全集、最近 10 天和最近 7 天三个层次的数据对象，并写入 `dashboard/js/history_data.js`，供浏览器端直接加载。

【核心机制】  
该部分采用“数据库聚合导出”机制，将原始监测记录转换为每日样本数、平均资源指标以及累计水电消耗等统计结果，再以静态 JavaScript 对象方式发布给前端。这样既保留了 SQLite 历史数据的真实来源，又避免了浏览器实时执行复杂查询。

【作用与意义】  
该设计实现了桌面数据库与 Web 历史展示之间的数据桥接，从而形成了“本地采集存储 - 统计导出 - 浏览器展示”的闭环链路，满足了轻量展示系统对历史信息的访问需求。

## 4.5 SQLite数据库实现

### 4.5.1 数据库结构说明

【设计目标】  
本系统没有独立后端数据库服务，因此需要依托本地 SQLite 建立完整的数据持久化体系，以承载实时监测、报警事件、设备信息、操作日志、离线记录和用户认证等核心业务。

【实现方案】  
系统将全部业务数据统一保存在本地数据库文件 `D:/System_UI_Data/system_ui.sqlite` 中，当前实际使用的数据表包括 `data`、`alarm_info`、`users`、`device_info`、`remote_exec_logs` 和 `device_abnormal_records`。从业务分类看，这些表分别对应历史监测数据、事件记录、用户认证、设备基础信息、远程控制日志和离线异常记录。

【核心机制】  
数据库设计采用“单库多表、按业务分层”的组织方式。实时监测值进入历史数据表，报警与求助进入事件表，控制行为进入日志表，设备属性进入基础信息表，用户信息进入认证表，通信异常进入离线记录表。通过这种分类，系统在不引入独立数据库服务器的情况下完成了完整的数据闭环。

【作用与意义】  
该设计实现了桌面端本地化数据管理，从而降低了系统部署门槛，并为上层历史查询、事件追踪和用户管理提供了统一支撑。

### 4.5.2 data表写入流程

【设计目标】  
`data` 表承担原始历史样本存储职责，因此其设计重点在于保证实时消息能够稳定沉淀为可追溯的时序数据。

【实现方案】  
每当 Qt 客户端接收到一条有效实时消息后，就将当前时间与主要监测值组合为一条历史记录写入 `data` 表。写入内容覆盖环境类指标和资源类指标，形成后续统计分析所需的基础样本。为便于时间序列查询，记录时间采用统一格式保存，并配套建立时间索引。

【核心机制】  
该表的核心机制是“消息驱动写入”。也就是说，监测数据的持久化并不依赖人工操作，而是与实时消息接收过程同步发生。由于桌面端与 Web 历史展示均以该表为原始来源，因此 `data` 表在系统中承担事实数据源角色。

【作用与意义】  
该设计实现了实时采样向历史样本的稳定转化，从而提高了系统历史分析能力，也为后续按时间聚合、趋势绘制和数据导出提供了统一基础。

### 4.5.3 alarm_info报警生命周期管理

【设计目标】  
报警记录不仅要说明“发生了什么”，还应说明“从何时开始、是否结束、是否处理”，因此系统需要为报警与求助事件建立完整的生命周期管理机制。

【实现方案】  
`alarm_info` 表统一保存传感器预警、传感器告警以及 Web 一键求助事件。传感器类事件由实时报警集合变化触发生成，事件消失后补写结束时间并更新状态；求助类事件则在消息抵达桌面端后直接写入该表，由工作人员后续人工标记处理结果。

【核心机制】  
该表采用“事件起点记录 + 结束状态回填 + 人工处理闭环”的生命周期设计。系统通过状态字段区分活动中与已解决事件，通过结束时间记录事件终止点，从而把原本瞬时的报警提示扩展为具有持续过程的信息对象。

【作用与意义】  
该设计实现了报警和求助信息的统一事件化管理，从而增强了系统的追踪能力和管理规范性，也为后续统计不同类型事件的发生频率和处理情况提供了可能。

### 4.5.4 remote_exec_logs记录机制

【设计目标】  
远程控制功能一旦投入使用，就需要能够回答“何时发出、发给什么、结果如何”等管理问题，因此数据库必须保存完整的操作日志。

【实现方案】  
系统在桌面端发起远程控制或快捷控制操作时，同步向 `remote_exec_logs` 表写入操作时间、控制对象、命令文本和结果描述。界面侧可进一步读取最近一段时间的日志记录，用于值班查看和操作追溯。

【核心机制】  
该表采用“控制动作与日志同步落库”的记录机制。也就是说，只要系统认定某次远程操作已经被发起，就立即写入操作留痕，而不是依赖用户后续补录。这种设计将控制链路与审计链路保持在同一事务路径上。

【作用与意义】  
该设计实现了远程控制过程的可追踪管理，从而增强了系统运维规范性，也为分析误操作、重复操作和控制时序问题提供了基础依据。

### 4.5.5 device_abnormal_records离线检测逻辑

【设计目标】  
在实际部署中，设备异常不仅包括测量值超限，也包括通信中断。因此，系统需要将离线状态单独记录下来，避免把“没有数据”误判为“数据正常”。

【实现方案】  
离线检测由 Qt 客户端依据实时消息更新时间完成。当桌面端在约 8 s 内未收到新的 MQTT 数据时，系统判定设备进入离线状态，并将离线起点和持续时长写入 `device_abnormal_records` 表。对同一次离线过程，系统不是不断新建记录，而是围绕同一离线起点持续更新持续时间。

【核心机制】  
该表采用“离线会话式记录”策略。系统以监测项名称和离线起始时间作为识别依据，在同一次中断过程中不断刷新离线持续时长，从而准确表达故障过程。该机制避免了长时间离线造成的重复记录堆积问题。

【作用与意义】  
该设计实现了通信异常的独立持久化表达，使系统能够区分“数值异常”和“链路异常”两类风险，从而提高了运维排查效率，也增强了监测数据解释的准确性。

### 4.5.6 users用户认证逻辑

【设计目标】  
在无服务器认证体系的前提下，系统仍需满足基本的登录、注册和身份区分要求，因此需要通过本地数据库完成用户认证信息管理。

【实现方案】  
`users` 表保存用户名、口令、角色和创建时间等基本信息，系统初始化时提供默认账户，日常使用中支持用户登录、注册和账户信息修改。当前角色类型主要包括管理员和普通用户两类，用于标识不同身份。

【核心机制】  
该表采用“本地存储、本地校验、本地维护”的轻量认证方式。登录阶段通过本地用户表完成用户名与口令比对，注册阶段检查用户名唯一性，账户维护阶段支持用户更新基本信息。客户端在登录成功后再将角色信息传递给主界面，用于后续身份展示和基础管理。

【作用与意义】  
该设计实现了在单机部署条件下的基本认证闭环，使系统在不依赖外部服务的前提下具备了用户区分能力，满足了桌面管理软件的基本使用要求。
