# STM32F103C8T6 嵌入式固件技术文档

## 一、概述

STM32固件是整个系统的核心终端，运行于STM32F103C8T6微控制器（ARM Cortex-M3, 72MHz, 64KB SRAM, 128KB Flash），负责6路传感器数据采集、5路执行器控制、OLED本地显示、三级联动阈值判断、水电剩余量积分模拟，以及通过USART1串口与ESP32网关通信。

- **开发环境**：Keil MDK v5，编译器 ARMCC v5.06
- **编程语言**：C语言
- **架构**：前后台系统（主循环轮询 + SysTick时基 + 中断服务）
- **主循环周期**：10ms（通过SysTick延时累加实现1s传感器采集周期和300ms OLED刷新周期）

---

## 二、传感器驱动模块

### 2.1 DHT11 温湿度传感器（PB13）

- **通信协议**：单总线（One-Wire），软件模拟时序
- **读取流程**：
  1. 主机发送启动信号：拉低18ms→释放→切换为输入模式
  2. 传感器响应：80μs低电平 + 80μs高电平
  3. 接收40bit数据：湿度整数8bit + 湿度小数8bit(恒为0) + 温度整数8bit + 温度小数8bit(恒为0) + 校验和8bit
  4. 校验：前4字节之和等于第5字节
  5. 每位数据以50μs低电平起始，高电平持续约70μs表示"1"，约26~28μs表示"0"
- **关键问题解决**：所有while等待循环加入超时计数（retry>200退出），防止传感器离线时MCU陷入死循环
- **读取频率**：每1秒调用一次

### 2.2 GP2Y10 粉尘浓度传感器（PA2 ADC + PB12 LED控制）

- **原理**：红外光散射法，红外LED照射空气样本，散射光强度与颗粒物浓度成正比
- **驱动时序**：10ms周期脉冲 — LED亮0.32ms（在0.28ms处采样ADC）→ LED灭9.68ms
- **ADC处理**：10点滑动平均滤波
- **电压补偿**：转接板内置1K+10K分压（分压比11:1），代码中乘以11还原实际传感器输出电压
- **浓度计算**：`dustDensity = (Vsensor_mV - NO_DUST_MV) × 0.20`
  - NO_DUST_MV = 420（经商家参考值校正，原值200偏小导致空读100~200μg/m³）
  - 换算系数K = 0.20 μg/m³/mV
- **输出限幅**：上限500μg/m³
- **演示模式**：洁净空气下自动生成20~50μg/m³三角波模拟数据

### 2.3 MQ-135 空气质量传感器（PA1 ADC）

- **原理**：半导体气体传感器，对NH₃、CO₂、苯、酒精等多种有害气体敏感
- **ADC采样**：16次采样取平均（100μs间隔）
- **信号滤波**：指数移动平均（EMA）低通滤波，α=0.2
- **输出映射**：ADC值线性映射至0~500空气质量指数
- **预留功能**：Rs/Ro电阻计算公式已实现（Rs=RL×(Vcc-Vout)/Vout），可升级为精确ppm计算

### 2.4 ACS712 电流传感器（PA0 ADC）

- **原理**：霍尔效应电流传感器，量程±5A，灵敏度185mV/A，零点电压2.5V
- **工频抑制**：600次采样×68μs间隔=40.8ms，恰好覆盖2个50Hz工频周期，算术平均消除周期性噪声
- **电流计算**：`I = |Vout - 2.50| / 0.185`，取绝对值测量大小
- **漂移抑制**：
  - DC blocker基线追踪（α=0.05，时间常数τ≈20s）
  - 15mA死区：连续3秒低于死区→强制归零
  - main.c中额外过滤≥5A异常值
- **上限**：5A
- **DEMO缩放**：CUR_SCALE=10（400mA→显示4A），真实场景可设为1

### 2.5 YF-S401 水流传感器（PA5 EXTI）

- **原理**：霍尔效应水流传感器，450脉冲/L
- **中断采集**：EXTI5上升沿中断，ISR中只做整数脉冲累加（pulse_count++）
- **定时统计**：TIM2每1秒中断，将pulse_count移入s_total_pulses累加器
- **流量计算**：`flow_rate(L/min) = pulse_diff × 60 / 450`
- **累计流量**：`total_flow_L += flow_rate / 60`（按调用间隔累加）
- **原子保护**：读32位s_total_pulses时__disable_irq()/__enable_irq()防撕裂
- **DEMO缩放**：FLW_SCALE=5

### 2.6 OLED 显示屏（PB8 SCL, PB9 SDA）

- **型号**：SSD1306 0.96英寸 128×64 OLED
- **驱动方式**：软件模拟I²C协议（开漏输出+上拉），不使用硬件I²C
- **从机地址**：0x78（SA0=0, 7位地址0x3C左移）
- **字体**：8×16像素（OLED_F8x16字库），每行16字符
- **四行显示内容**：
  - 第1行：温度(℃) + 湿度(%)
  - 第2行：PM2.5(μg/m³) + 空气质量(ppm)
  - 第3行：水流(L/min)
  - 第4行：电流(A)
- **刷新周期**：300ms
- **防闪烁**：OLED_Clear()替换为仅写4行数据区（16空格×4行），避免全屏清除导致的闪烁

---

## 三、执行器控制模块

### 3.1 LED 指示灯（PA15红 / PA11绿 / PA12黄）

- 高电平亮，低电平灭
- **PA15 JTAG问题**：PA15默认为JTDI功能，复位后JTAG占用导致驱动能力微弱（红灯微亮）
- **解决方案**：`GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE)` 释放PA15为正常GPIO，同时保留SWD（PA13/PA14）用于调试

### 3.2 蜂鸣器（PB14）

- 有源蜂鸣器（DC），低电平响、高电平停
- **预警节奏**：响100ms → 停1900ms（2秒周期慢滴声）
- **警报节奏**：响10ms→停10ms→响10ms→停70ms（100ms周期滴滴滴声）

### 3.3 风扇（PB6）

- GPIO直接控制：低电平转，高电平停
- 与电机PWM共享PB6引脚（互斥使用）

### 3.4 舵机 SG90（PA6, TIM3_CH1）

- 50Hz PWM（20ms周期），1MHz定时器时钟
- 脉宽500~2500μs 对应 0°~180°角度
- 角度计算公式：`pulse = 500 + Angle × 2000 / 180`
- 联动控制：正常/预警→0°（关窗），警报→90°（开窗通风）

---

## 四、三级联动阈值体系

### 4.1 阈值系统

系统为每类传感器维护两组阈值（预警、告警），均存储在RAM中（static变量），可通过远程命令动态修改。

**默认阈值**（代码中`static uint16_t`变量）：

| 参数 | 预警阈值 | 告警阈值 | 告警→预警推导 |
|------|---------|---------|--------------|
| 温度上限 | 32℃ | 38℃ | warn = alarm - 6 |
| 温度下限 | 18℃ | 10℃ | warn = alarm + 8 |
| 湿度上限 | 70% | 85% | warn = alarm - 15 |
| 湿度下限 | 30% | 20% | warn = alarm + 10 |
| PM2.5 | 75μg/m³ | 150μg/m³ | warn = alarm / 2 |
| 空气质量 | 100 | 200 | warn = alarm / 2 |
| 电流 | 10A | 15A | warn = alarm × 2/3 |
| 水流 | 5L/min | 10L/min | warn = alarm / 2 |

**核心设计原则**：Qt桌面端只需下发6个告警阈值（ta/tb/ha/hb/pa/aa/ca/fa），STM32自动调用`derive_warn_from_alarm()`推导8个预警阈值。系统同时兼容旧的预警阈值直接下发命令（tr/tc/hw/hd/ph/aq/ci/fl）。

### 4.2 滞回机制

**进入/退出计数**：

| 状态转换 | 需连续确认次数 |
|----------|--------------|
| 正常→预警 | 3次（3秒） |
| 预警→告警 | 5次（5秒） |
| 预警→正常 | 3次（恢复条件=阈值-滞回量） |
| 告警→预警 | 5次（恢复条件=阈值-滞回量） |

**滞回偏移量**（独立配置）：

| 传感器 | 预警滞回 | 告警滞回 |
|--------|---------|---------|
| 温度 | 2℃ | 3℃ |
| 湿度 | 5% | 5% |
| PM2.5 | 10μg/m³ | 20μg/m³ |
| AQ | 15 | 30 |
| 电流 | 1A | 2A |
| 水流 | 2L/min | 3L/min |

### 4.3 系统级别判定

取6传感器中最差级别作为系统整体联动级别(link_level)：
- 任一传感器告警(2) → 系统告警(2)
- 无告警但任一传感器预警(1) → 系统预警(1)
- 全部正常(0) → 系统正常(0)

环境级别(env_level)只取温湿PM2.5/AQ四类（不含水电）。

### 4.4 组合联动规则

| 组合名称 | 触发条件 | 联动效果 | 告警码 |
|----------|----------|----------|--------|
| 温度+PM2.5组合 | 两者同时≥预警 | 级别强制升至告警 | 200 |
| PM2.5+AQ组合 | 两者同时≥预警 | 级别强制升至告警 | 201 |
| 水流+电流组合 | 两者同时≥预警 | 风扇强制关闭(管道异常) | 202 |

### 4.5 执行器联动表

| 系统级别 | LED | 风扇 | 蜂鸣器 | 舵机(窗户) |
|----------|-----|------|--------|------------|
| 正常(0) | 绿灯 | 停 | 停 | 0° |
| 预警(1) | 黄灯 | 转 | 慢响(2s/滴) | 0° |
| 警报(2) | 红灯 | 转 | 常响(滴滴滴) | 90° |

---

## 五、手动/自动模式控制

- 每个执行器（蜂鸣器/风扇/舵机/LED）独立维护手动/自动标志位
- **手动优先**：远程指令下发后对应执行器进入手动模式，以远程控制为准
- **自动接管**：手动模式关闭后自动联动逻辑重新接管
- **手动看门狗**：手动模式下5分钟无新指令（MANUAL_WDOG_TICKS=30000×10ms），自动切回AUTO
- 支持全局模式切换：`{"cmd":"mode","val":"MANUAL"}` / `{"cmd":"mode","val":"AUTO"}`
- 支持全局复位：`{"cmd":"reset"}` 恢复全部自动状态

---

## 六、水电剩余量模拟

### 6.1 电量

- 默认电池容量：10000mAh
- 每秒积分：`battery_used_mAh += I(mA) / 3600`
- 剩余百分比：`(1 - used/capacity) × 100`，下限0%
- 预估剩余时间：`remain_min = (remain_mAh / I) × 60`
- 电流缩放系数CUR_SCALE=10（演示模式放大读数）

### 6.2 水量

- 默认水箱容量：10L
- 水流脉冲累加：450脉冲/L
- 剩余百分比：`(1 - total_flow/capacity) × 100`，下限0%
- 预估剩余时间：`remain_min = (remain_L / flow_rate) × 60`
- 水流缩放系数FLW_SCALE=5

### 6.3 远程校准

| 命令 | 作用 |
|------|------|
| bcNNNN | 设置电池容量(mAh) + 重置已用量为0 |
| bpNN | 直接设定电量百分比（工作人员现场校准） |
| wcNNNN | 设置水箱容量(L) + 重置已用量为0 |
| wpNN | 直接设定水量百分比 |

---

## 七、通信模块（serial.c）

### 7.1 硬件配置

- USART1: PA9(TX), PA10(RX)
- 波特率：115200bps, 8N1，无流控
- 接收：中断方式(RXNE) + 256字节环形缓冲区

### 7.2 上行数据

每1秒通过`Serial_SendUploadPacket()`发送综合JSON（约400~500字节），包含：

- **sen**：传感器原始值（温/湿/PM2.5/AQ/水流/电流）
- **res**：水电资源（电量%/水量%/已用/剩余/容量mAh和cL/预估分钟/负载状态）
- **lv**：联动级别（系统级别/环境级别/6传感器独立级别数组）
- **act**：执行器当前状态
- **mod**：各执行器手动/自动模式标志
- **alm**：活跃告警码数组（最多8个）
- **actn**：动作标志位掩码
- **flt**：传感器故障码数组（如DHT11离线=901）

### 7.3 下行命令解析

`Serial_ParseCommand()`支持三种格式：

1. **JSON新协议**：`{"cmd":"ctrl","act":"fan","val":"on"}`, `{"cmd":"mode","val":"MANUAL"}`, `{"cmd":"reset"}`, `{"cmd":"reset_th"}`, `{"cmd":"th","key":"ta","val":40}`
2. **JSON旧协议**：`{"code":0~8}`, `{"ctl":0~8}`（0蜂关/1蜂开/2窗开/3窗关/4扇开/5扇关/6灯开/7灯关/8复位）
3. **旧数字**：单个字符`0`~`8`
4. **旧KKVVV**：如`tr35`（温度预警上限35）、`ta40`（温度告警上限40）

---

## 八、告警码体系（serial.h）

### 8.1 传感器告警码（100~159）

| 范围 | 码值 | 含义 |
|------|------|------|
| 温度 100-109 | 101/102/103/104 | 偏高预警/过高告警/偏低预警/过低告警 |
| 湿度 110-119 | 111/112/113/114 | 偏高预警/过高告警/偏低预警/过低告警 |
| PM2.5 120-129 | 121/122 | PM2.5偏高/PM2.5超标告警 |
| AQ 130-139 | 131/132 | 空气质量下降/恶化告警 |
| 电流 140-149 | 141/142 | 电流偏高/过载告警 |
| 水流 150-159 | 151/152 | 水流偏高/异常告警 |

### 8.2 组合告警码（200~209）

| 码值 | 含义 |
|------|------|
| 200 | 温度+PM2.5组合告警（火灾风险） |
| 201 | PM2.5+AQ组合告警（烟雾污染） |
| 202 | 水流+电流组合预警（管道异常） |

### 8.3 故障码（900~909）

| 码值 | 含义 |
|------|------|
| 901 | DHT11离线 |
| 902 | MQ135离线 |
| 903 | GP2Y10离线 |
| 904 | ACS712离线 |
| 905 | 水流传感器离线 |

### 8.4 动作标志位

| 位 | 含义 |
|----|------|
| bit0 | 蜂鸣器已开启 |
| bit1 | 蜂鸣器已关闭 |
| bit2 | 风扇已开启 |
| bit3 | 风扇已关闭 |
| bit4 | 窗户已打开 |
| bit5 | 窗户已关闭 |

---

## 九、PWM配置（pwm.c）

| 定时器 | 通道 | 引脚 | 频率 | 用途 |
|--------|------|------|------|------|
| TIM2 CH1 | PA0 | LED PWM | 1kHz | LED亮度调节（预留） |
| TIM3 CH1 | PA6 | 舵机 | 50Hz | SG90舵机角度控制 |
| TIM4 CH1 | PB6 | 电机PWM | 20kHz | 直流电机调速（预留） |

---

## 十、故障检测与处理

- DHT11通信失败（校验错误或超时）→ 标记`dht_ok=0` → 上报故障码901
- DHT11失败时该周期测温/湿度不参与阈值判断
- DHT11失败时温湿度保留上一周期有效值
- 故障码随上行JSON的flt数组发送至云端

---

## 十一、已知遗留注意事项

1. **ARMCC v5编码限制**：ARMCC v5对GB2312/UTF-8编码处理不佳，中文注释中的某些字节可能被识别为`/*`或`*/`导致编译错误。当前所有含中文注释的源文件已统一使用`//`行注释。
2. **PB6引脚共享**：风扇GPIO控制和电机PWM输出共享PB6，两者互斥使用。
3. **ADC饱和**：ACS712在5V供电时最大输出可达~3.425V(C=5A时)，而STM32 ADC参考电压为3.3V，实际可测最大电流约4.32A。
4. **SysTick占用**：SysTick被Delay模块以轮询方式使用（非中断模式），如需使用RTOS需重新设计时基。
