# Web看板技术文档

## 一、概述

Web看板是面向灾民群体的数据可视化界面，设计遵循"大字、直观、关键信息突出"的原则。看板通过MQTT over WebSocket直连巴法云物联网平台，无需后端服务器，纯前端技术栈实现。与Qt桌面应用共享同一数据源。

- **文件位置**：`D:\AAA\dashboard\`
- **技术栈**：HTML5 + CSS3 + 原生JavaScript（无框架依赖）
- **核心库**：MQTT.js v5.10.3（MQTT over WebSocket）、Chart.js v4.4.7（趋势图）
- **适配分辨率**：1920×1080（满屏展示），向下兼容1366×768

---

## 二、文件结构

| 文件 | 功能 |
|------|------|
| `index.html` | 三栏主页面结构 + DOM元素 |
| `js/config.js` | MQTT连接配置（服务器地址、端口、客户端ID、主题） |
| `js/app.js` | 核心逻辑：MQTT连接、数据解析、UI更新、告警/Toast、求助功能 |
| `js/history_data.js` | 历史数据接口（可选：真实7天数据或演示数据） |
| `css/dashboard.css` | 看板全面样式 + 动画效果（Toast弹窗、卡片变换、水箱波浪等） |

---

## 三、页面布局

### 3.1 顶部状态栏

| 区域 | 内容 |
|------|------|
| 左侧 | 系统图标🏠 + 系统名称 + 监测站点名称 |
| 中央 | 系统状态标签（正常✓绿/预警!黄/异常✗红）+ 在线设备数 |
| 右侧 | 实时时钟（年-月-日 时:分:秒）+ MQTT连接状态指示灯 |

系统状态标签的边框颜色随STM32上发的link_level动态变化。

### 3.2 左栏 — 环境监测中心

#### 2×2 指标卡片
| 卡片 | 显示内容 | 单位 |
|------|----------|------|
| 温度 🌡️ | 当前温度值 | ℃ |
| 湿度 💧 | 当前湿度值 | % |
| PM2.5 💨 | 当前PM2.5浓度 | μg/m³ |
| 空气质量 🍃 | 当前空气质量指数 | ppm |

**卡片颜色自动变化**（根据STM32上发的`lv.d[]`数组）：
- 绿色：正常（lv=0）
- 黄色：预警（lv=1）
- 红色：告警（lv=2）

每张卡片包含：图标+名称、大字体数值+单位、级别标签（正常/预警/告警）、阈值参考说明文字。

#### 24小时环境预测
- **温度曲线SVG**：25个时间点（当前+未来24小时），贝塞尔曲线绘制，曲线颜色从蓝（低温）到红（高温）渐变
- **天气图标列**：每列显示天气图标（☀️🌤️⛅☁️🌧️🌙）、风力SVG（微风/强风+风向标）、空气等级（优绿/良黄/差红）、紫外线（弱/中/强）、时间标签（现在/h:00/日期）
- **交互功能**：鼠标拖拽滑动 + 惯性滚动 + 吸附对齐到最近列 + hover高亮
- **天气提示语**：预测文字轮播（每6.5秒切换一句）
- **自适应背景**：根据前8小时天气分布自动切换背景主题（晴天/多云/雨天/夜间）

### 3.3 中栏 — 智慧水电管理

#### 电力监控卡片
- **电池SVG图标**：fill高度随电量%变化，绿(>60%)/黄(>30%)/红(≤30%)
- **大字体百分比** + 剩余mAh
- **预计剩余时间**：自动切换显示单位（分钟/小时/天）
- **今日已用电**：从STM32 res.tu直接显示(Ah)
- **电力线路状态**：正常(绿)/偏高(黄)/过载(红)，从res.ps读取
- **状态标签**：电力供应充足/电力不足/电力告急

#### 供水监控卡片
- **水箱动画**：fill高度+CSS波浪效果随水量%变化，蓝(>60%)/黄(>30%)/红(≤30%)
- **大字体百分比** + 剩余升数
- **预计剩余时间** + 今日已用水(L)
- **水质状态**显示

#### 水电消耗趋势图
- Chart.js双Y轴折线图
- 左侧Y轴：用电量（橙色，单位mAh）
- 右侧Y轴：用水量（蓝色，单位L）
- X轴：过去7天日期
- 支持真实历史数据或演示数据

### 3.4 右栏 — 报警预警中心

#### 执行器状态面板
显示4个执行器的实时状态：
| 执行器 | 状态显示 | 模式显示 |
|--------|----------|----------|
| 蜂鸣器 | 开/关 | 手动/自动 |
| 风扇 | 开/关 | 手动/自动 |
| 窗户 | 关/开(N°) | 手动/自动 |
| 指示灯 | 亮/灭 | 手动/自动 |

模式和状态从STM32上发的act和mod字段读取。

#### 实时报警列表
- 红色标题 + 按时间倒序排列
- 每条显示：时间 + 告警类型中文 + 故障码
- 告警码→中文映射表（101=温度偏高, 102=温度过高告警, 200=温度+PM2.5组合告警(火灾风险), 901=DHT11离线...）
- 无报警时显示"✅ 当前无实时报警"
- 显示已采取措施列表（从actn位掩码解码）

#### 预警信息列表
- 黄色标题 + 同样格式显示
- 无预警时显示"✅ 当前无预警信息"

---

## 四、联动弹窗Toast

当系统进入预警或告警状态时，右下角弹出半透明通知面板：

- **触发条件**：link_level变化（由0→1或0→2）
- **防抖**：同一级别不重复弹出（lastToastLevel比较）
- **内容**：图标(⚠️/🚨) + 标题(系统预警/系统告警) + 异常传感器列表(含故障码) + 已采取措施
- **自动消失**：10秒后自动隐藏
- **样式**：预警黄色边框，告警红色边框，半透明深色背景

---

## 五、一键求助功能

固定在页面底部右下角的浮动按钮：

- **按钮外观**：🆘 求助（圆形浮动按钮）
- **展开面板**：点击展开4种求助类型：
  - 🏥 身体不适
  - 💧 生活缺水
  - 🔌 电力故障
  - 📢 其他求助
- **发送机制**：点击后通过MQTT发布JSON到独立主题WebQT1
  ```json
  {"type": "medical", "label": "身体不适", "time": "2026-05-19T...", "site": "一号营地"}
  ```
- **反馈提示**：发送后显示"求助已发送，工作人员将尽快处理"（3秒后消失）
- **接收端**：Qt桌面应用通过订阅WebQT1主题实时接收并弹窗通知

---

## 六、数据协议解析

### 6.1 新协议（当前主要使用）

来源于STM32上传的综合JSON：

```javascript
{
  sen: { t, h, pm, aq, f, i },          // 传感器原始值
  res: { bp, wp, tu, wu, br, wr, ps, bc, tc, bt, wt },  // 水电资源
  lv:  { link, env, d: [6] },            // 联动级别
  act: { bz, fan, svo, led: [3] },       // 执行器状态
  mod: { gl, bz, fan, svo, led },        // 手动/自动模式
  alm: [101, 121, ...],                  // 活跃告警码数组
  actn: 位掩码,                           // 自动采取的措施
  flt: [901, ...]                         // 传感器故障码数组
}
```

解析函数`normalizePayload()`将MQTT消息标准化为以上结构。

### 6.2 旧协议兼容

```javascript
{ t, h, p/pm, a/aq, f, i, bp, wp, lv }
```
旧协议中所有传感器使用同一个lv级别。

---

## 七、核心函数说明

### updateMetrics(data)
- 从lv.d数组读取6传感器独立级别
- 更新4张指标卡片的数值、级别标签、卡片颜色类

### updateResources(data)
- 更新电池SVG fill高度和颜色
- 更新水箱CSS fill高度和波浪颜色
- 更新剩余时间（分钟/小时/天自动切换）
- 更新今日已用统计
- 更新电力线路状态

### updateActuators(data)
- 更新4个执行器状态行（设备名/状态/手动-自动模式）

### updateAlarms(data)
- 从alm数组分类告警（danger=2级）和预警（warn=1级）
- 从actn位掩码解码已采取的措施文本
- 更新右侧报警列表和预警列表

### showLinkageToast(data)
- link_level变化时触发
- 组装Toast HTML内容并显示
- 10秒自动隐藏

### normalizePayload(raw)
- 新协议/旧协议/其他格式统一标准化

---

## 八、MQTT连接

配置从`config.js`读取：
```javascript
const CFG = {
  mqttUrl: 'wss://bemfa.com:9504/mqtt',  // WebSocket
  clientId: 'webdash_' + randomHex(8),
  topic: 'test001up',
  helpTopic: 'WebQT1',
  siteName: '一号营地监测站'
};
```

- 连接：`mqtt.connect(url, { clientId, clean: true, reconnectPeriod: 3000 })`
- 订阅：连接成功后自动订阅test001up主题
- 状态显示：绿色圆点=已连接"数据实时更新中"，灰色圆点=已断开
- 断线检测：30秒周期检测MQTT连接状态

---

## 九、24小时环境预测

- **数据生成**：初始化时在客户端生成模拟预测数据（25个时间点）
- **天气图标**：☀️🌤️⛅☁️🌧️🌙 按时间段随机分配
- **温度曲线**：SVG绘制，贝塞尔曲线（Catmull-Rom样条），颜色渐变
- **自适应背景**：根据前8小时天气分布自动选择背景主题CSS类
- **滚动交互**：鼠标拖拽+惯性+吸附对齐

---

## 十、设计特点

1. **面向灾民**：大字体、直观图形、水电剩余信息最突出
2. **颜色语义化**：绿(正常)/黄(预警)/红(告警)三色系统贯穿全部界面
3. **纯前端无后端**：MQTT over WebSocket直连云平台，零服务器依赖
4. **执行器透明化**：灾民可看到系统自动措施（风扇开了没？窗户开了没？）
5. **Toast防抖**：同一级别不重复弹窗
6. **一键求助**：简单快捷，自动通知工作人员
7. **SVG图表**：电池、水箱、温度曲线均为纯SVG/CSS实现，无需额外图表库
8. **多浏览器兼容**：Chrome 120+, Edge 120+, Firefox 121+
