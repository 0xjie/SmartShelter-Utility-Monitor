# 灾后临时安置点智慧水电管理与环境监测系统

## 项目信息

- **课题**：灾后临时安置点智慧水电管理与环境监测系统设计
- **学校**：应急管理大学
- **主控**：STM32F103C8T6（面包板）
- **通信链路**：STM32 UART(115200 8N1) ↔ ESP32 ↔ WiFi(MQTT) ↔ 巴法云(bemfa.com:9501) ↔ Qt桌面端 / Web看板
- **IDE**：Keil MDK v5（ARMCC v5.06）

## 外部资源路径

- **数据库位置**：`D:\System_UI_Data`（Qt桌面端 SQLite 数据库文件）
- **论文位置**：`D:\论文`（课题论文/毕设文档）
- **系统文档**：`D:\AAA\系统说明文档.md`（完整系统说明，供他人快速了解）

## 界面定位

- **Web 看板** → 给灾后群众看（大字、直观图形、水电剩余突出）
- **Qt 桌面端** → 给工作人员看（详细数据、历史曲线、控制面板）

## 目录结构

```
D:\AAA\
 ├── FinalSystem\          ← STM32固件 (Keil MDK项目)
 │    ├── Hardware\        ← 各模块驱动
 │    ├── User\            ← main.c
 │    ├── Start\           ← 启动文件
 │    ├── Library\         ← 标准外设库
 │    └── ...
 ├── esp32\                ← ESP32固件 (Arduino)
 │    └── stm32_to_esp32_uart_test\
 ├── dashboard\            ← Web看板 (MQTT直连)
 │    ├── index.html
 │    ├── app.js
 │    ├── config.js
 │    └── css/
 └── System_UI\  ← Qt桌面端
```

## 硬件模块与引脚分配（STM32F103C8T6 面包板）

C8T6 引脚数少，共用 ADC1 多通道，需注意每次读取前显式切换通道。

### ADC1 输入（共用 ADC1）
| 模块 | 信号 | 引脚 | ADC通道 |
|------|------|------|---------|
| ACS712 电流 | ADC | PA0 | ADC1_IN0 |
| MQ135 空气质量 | ADC | PA1 | ADC1_IN1 |
| GP2Y10 粉尘浓度 | ADC | PA2 | ADC1_IN2 |

### GPIO / 外设
| 模块 | 引脚 | 说明 |
|------|------|------|
| DHT11 温湿度 | PB13 | 单总线数据 |
| GP2Y10 LED控制 | PB12 | GPIO输出，低电平点亮LED |
| 蜂鸣器 | PB14 | GPIO输出，低电平响 |
| 风扇控制 | PB6 | GPIO输出，低电平转、高电平停 |
| OLED SCL | PB8 | 软件I2C时钟 |
| OLED SDA | PB9 | 软件I2C数据 |
| 水流脉冲输入 | PA5 | EXTI5 上升沿中断 |
| 舵机PWM | PA6 (TIM3_CH1) | 20ms周期PWM，500~2500µs |
| USART1 TX (STM32→ESP32) | PA9 | 接ESP32 RX |
| USART1 RX (ESP32→STM32) | PA10 | 接ESP32 TX |

### LED指示灯（高电平亮）
| 颜色 | 引脚 | 备注 |
|------|------|------|
| 红灯 | PA15 | 需禁用JTAG才能正常驱动 |
| 绿灯 | PA11 | 正常GPIO |
| 黄灯 | PA12 | 正常GPIO |

### 预留/空闲引脚
- PA3, PA4, PA5, PA7
- PA3, PA4, PA7, PA8
- PB0, PB1, PB2, PB3, PB4, PB5, PB7, PB10, PB11, PB15
- PC0, PC1, PC2, PC3, PC4, PC5, PC6, PC7, PC13(板载LED), PC14, PC15

## 供电方案

12V适配器 → LM2596（调5V输出）→ 面包板电源模块

**5V供电**：MQ135（单独孔）、舵机（单独孔）、风扇、GP2Y10+ACS712+水流（共用孔）、ESP32

**3.3V供电**：OLED、DHT11

**GND共地**：LM2596 GND → 面包板电源GND → 所有模块GND → STM32 GND（全部连在一起）

## 系统架构

```
STM32F103C8T6 (面包板)
  ├── 传感器采集 (1s周期)
  │   ├── DHT11 温湿度 (PB13)
  │   ├── MQ135 空气质量 (PA1 ADC)
  │   ├── GP2Y10 粉尘浓度 (PA2 ADC)
  │   ├── ACS712 电流 (PA0 ADC)
  │   └── YF-S401 水流 (PA5 EXTI)
  ├── 执行器控制
  │   ├── 舵机 (PA6 PWM) — 开窗/加湿角度
  │   ├── 风扇 (PB6 GPIO)
  │   ├── 蜂鸣器 (PB14 GPIO) — PM2.5超标滴-滴-滴报警
  │   └── LED (PA15/11/12) — 红/绿/黄状态指示
  ├── 显示
  │   └── OLED 0.96" (PB8/PB9 I2C模拟) — 双页切换显示
  ├── 通信
  │   └── USART1 (PA9 TX, PA10 RX) → ESP32
  └── 控制逻辑 (阈值自动判断)
```

## 阈值系统（三级联动）

所有阈值存储在 RAM 中（`FinalSystem/User/main.c` 顶部 `static uint16_t` 变量）。

### 预警阈值（可通过远程命令修改）
| 变量 | 默认 | 命令 | 含义 |
|------|------|------|------|
| TEMP_WARN_H | 32 | tr | 温度预警上限 |
| TEMP_WARN_L | 18 | tc | 温度预警下限 |
| HUMI_WARN_H | 70 | hw | 湿度预警上限 |
| HUMI_WARN_L | 30 | hd | 湿度预警下限 |
| PM25_WARN | 75 | ph | PM2.5 预警 |
| AQ_WARN | 100 | aq | 空气质量预警 |
| CUR_WARN | 800 | ci | 电流预警 (mA) |
| FLW_WARN | 5 | fl | 水流预警 (L/min) |

### 告警阈值
| 变量 | 默认 | 命令 | 含义 |
|------|------|------|------|
| TEMP_ALARM_H | 38 | ta | 温度告警上限 |
| TEMP_ALARM_L | 10 | tb | 温度告警下限 |
| HUMI_ALARM_H | 85 | ha | 湿度告警上限 |
| HUMI_ALARM_L | 20 | hb | 湿度告警下限 |
| PM25_ALARM | 150 | pa | PM2.5 告警 |
| AQ_ALARM | 200 | aa | 空气质量告警 |
| CUR_ALARM | 1200 | ca | 电流告警 (mA) |
| FLW_ALARM | 10 | fa | 水流告警 (L/min) |

### 三级联动规则

系统取 6 传感器**最差级别**为整体级别，自动控制执行器：

| 级别 | LED | 风扇 | 蜂鸣器 | 舵机(窗户) |
|------|-----|------|--------|------------|
| 正常(0) | 绿灯 | 停 | 停 | 关 0° |
| 预警(1) | 黄灯 | 转 | 慢响(2s/滴) | 关 0° |
| 警报(2) | 红灯 | 转 | 常响 | 开 90° |

- 远程下发指令 → 手动模式优先
- 手动关闭 → 自动联动接管
- 每个执行器独立区分手动/自动

## 代码文件说明

### STM32固件（FinalSystem/）
**Hardware/**
| 文件 | 功能 |
|------|------|
| `ACS712.c/.h` | 电流采样，300次×68µs覆盖50Hz工频周期，0.35/0.12低通滤波 |
| `DHT11.c/.h` | 温湿度，单总线协议，已加入超时防卡死 |
| `GP2Y10.c/.h` | 粉尘浓度，厂商公式 `(V×11-420)×0.20`，LED控制PB12 |
| `MQ_135.c/.h` | 空气质量ppm |
| `waterFlow.c/.h` | 水流传感器，98脉冲/L，ISR只做整数累加 |
| `OLED.c/.h` | SSD1306 0.96寸，I2C模拟 |
| `servo.c/.h` | SG90 舵机PWM |
| `led.c/.h` | LED控制，PA15/PA11/PA12高电平亮 |
| `buzzer.c/.h` | 蜂鸣器 |
| `motor.c/.h` | 原电机驱动代码保留，新增Fan_Init()风扇控制(PB6) |
| `serial.c/.h` | USART1通信，中断接收+环形缓冲，JSON发送，阈值/控制命令解析 |
| `Delay.c/.h` | 微秒/毫秒延时 |
| `pwm.c/.h` | PWM初始化（TIM2 LED/ TIM3 舵机/ TIM4 电机） |

**User/**
| 文件 | 功能 |
|------|------|
| `main.c` | 主循环：传感器采集、阈值判断、LED状态、OLED显示、蜂鸣器报警、执行器控制 |

### ESP32固件
`D:\AAA\esp32\stm32_to_esp32_uart_test\stm32_to_esp32_uart_test.ino`
- MQTT桥接，巴法云test001up主题
- 串口接收STM32 JSON数据，定时上传（5s）
- MQTT下行转发数字指令(0~7)和阈值命令(tr/tc/hd/hw/etc)

### Web看板
`D:\AAA\dashboard\` — MQTT直连巴法云，显示实时数据和控制

### Qt桌面端
`D:\AAA\System_UI\` — 折线图历史记录

## 通信协议

### STM32 → ESP32 → 云端 (JSON)
```json
{"t":26,"h":60,"pm":35,"aq":52,"f":0.00,"i":0,"bp":85,"wp":72,"lv":0}
```
每 1 秒发送一次。

| 字段 | 含义 | 备注 |
|------|------|------|
| t/h/pm/aq/f/i | 温/湿/PM2.5/AQ/水流/电流 | 传感器原始值 |
| bp/wp | 电池%/水量% | 水电剩余模拟 |
| lv | 联动级别 | 0=正常 1=预警 2=警报 |

### 云端 → ESP32 → STM32 (下行)
- **数字指令**：`"4"` → STM32 解析执行（0蜂关/1蜂开/2窗开/3窗关/4扇开/5扇关/6灯开/7灯关）
- **JSON格式**：`{"code":4,"name":"风扇开启"}`
- **阈值修改**：`{"th":{"tr":35,"tf":30}}` → ESP32转发`tr35\n`

## 已解决的遗留问题

### ACS712空载漂移
- 采样数64→300，延迟50µs→68µs（覆盖完整50Hz周期），加入100mA死区

### DHT11可能卡死MCU
- 两个while等待循环加入超时计数（retry>200退出）

### GP2Y10 PM2.5读数错误
- 旧代码差值算法导致空读300µg/m³ → 改用厂商公式`(V×11-420)×0.20`
- LED极性修正：转接板有1K+10K分压(×11)，LED控制高电平点亮（参考商家原厂例程）
- 注意：2026-05-14之前误记录为"低电平点亮"，实际模块使用NPN三极管驱动，PB12=高电平时LED亮
- 2026-05-15：NO_DUST_MV 从 200 改为 420（原值偏小导致空读 100-200，按商家参考值修正）

### waterFlow.c编码乱码+ISR浮点运算
- 重写文件，ISR只做整数累加，浮点移到取值函数

### Motor_DEInit命名误导
- 重命名为Fan_Init（实际是风扇初始化）

### ESP32 WiFi断线不重连
- reconnect()增加WiFi状态检测，断WiFi后先调setup_wifi()

### OLED切页闪烁
- OLED_Clear()替换为仅清除4行数据区（写16空格×4行）

### Keil v5中文注释编译报错
- ARMCC v5对GB2312编码处理不佳，`/* */`中的中文字节可能含`*/`或`/*`序列
- 当前状态：GP2Y10.h第22行注释中含`*/`导致编译错误

### LED红灯微亮（PA15 JTAG冲突）
- PA15默认用作JTDI，GPIO驱动很弱导致红灯微亮
- 代码已加入 `GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE)` 释放PA15

## Keil编译注意事项

- ARMCC v5对UTF-8/GB2312编码处理不佳
- 中文字符在`/* */`注释中可能导致"nested comment"警告并使宏定义失效
- **解决方案**：含中文的注释统一用`//`行注释
- 当前已使用`//`行注释的文件：GP2Y10.h, GP2Y10.c, ACS712.h, ACS712.c, waterFlow.c

## 水电剩余量模拟（2026-05-15 已实施）

> 方案D：积分衰减 + 远程校准 + 剩余可用时间。
> 水箱默认 10L（方便吹气演示），电池默认 10000mAh。

### ACS712 空载漂移（2026-05-15 补充）
- 采样从 300 次×2 周期 → **600 次×2 周期**（40ms，覆盖两次工频过零）
- 改用 DC blocker 基线追踪（α=0.05，τ≈20s）替代固定零点校准
- 死区 15mA，连续 3 秒低于死区 → 强制归零
- main.c 过滤 10mA

### 背景
现有 ACS712 电流传感器和 YF-S401 水流传感器。灾后场景下，**还剩多少水电** 是关键信息。通过积分/累加方式模拟剩余量（无需额外硬件）。

### 方案

#### 1. 剩余电量模拟
- **原理**：每1s用 ACS712 测得瞬时电流 `I(mA)`，积分累加消耗 `battery_used_mAh += I / 3600`
- **默认容量**：`BATTERY_CAPACITY = 10000 mAh`（模拟一个应急电源/充电宝）
- **剩余%**：`(1 - used / capacity) × 100`，下限0%

#### 2. 剩余水量模拟
- **原理**：YF-S401 已有 `total_flow_L` 累计用量，直接取用
- **默认容量**：`TANK_CAPACITY = 2000 L`（模拟一个水箱）
- **剩余%**：`(1 - total / capacity) × 100`，下限0%

#### 3. 远程命令（复用现有 `KKNNN` 阈值解析）
| 命令 | 示例 | 作用 |
|------|------|------|
| `bc` | `bc10000` | 设置电池总容量(mAh) **并** 重置已用为0（换新电池） |
| `bp` | `bp50` | 直接设定剩余电量百分比（人工查看后校准） |
| `wc` | `wc2000` | 设置水箱总容量(L) **并** 重置已用为0（换新水箱） |
| `wp` | `wp80` | 直接设定剩余水量百分比（人工查看后校准） |

`bp`/`wp` 用于"我刚才看了眼实际电池/水箱，还剩XX%"的校准场景。

#### 4. 通信协议变更

```json
// 当前
{"t":26,"h":60,"pm":35,"aq":52,"f":0.00,"i":0}
// 新增 bp/wp
{"t":26,"h":60,"pm":35,"aq":52,"f":0.00,"i":0,"bp":85,"wp":72}
```

- `bp` = battery_pct (0~100)
- `wp` = water_pct (0~100)

#### 5. OLED显示变更

**当前第2页**（阈值页）→ **改为水电剩余量页**：

```
PWR:85%[########]
WTR:72%[####### ]
P:1500m/10000m
W:560L/2000L
```

阈值页不再需要常看（阈值由远程设定），水电剩余量对灾民更关键。

#### 6. Web看板变更

在 utilities 栏新增两个进度条：

```
[电量剩余 ████████░░ 85%] [水量剩余 ██████░░░░ 72%]
```

保留瞬时电流/流量显示在进度条副标题中。

### 涉及改动的文件

| 文件 | 改动 |
|------|------|
| `FinalSystem/User/main.c` | 积分变量、累加逻辑、OLED第2页替换、新命令处理 |
| `FinalSystem/Hardware/serial.c` | JSON 新增 `bp`/`wp` 字段 |
| `FinalSystem/Hardware/serial.h` | `Serial_SendSensorToESP` 签名增加参 |
| `dashboard/js/app.js` | 解析 `bp`/`wp`，渲染资源进度条 |
| `dashboard/index.html` | 新增资源进度条 DOM |
| `dashboard/css/dashboard.css` | 进度条样式 |

## 开发约定

- **必须先出方案，同意后再执行** — 涉及多文件改动、新增功能、架构调整的，先把方案写入 CLAUDE.md 或直接说明，等我说"可以"再动手写代码。
- **禁止修改 ESP32、Web、Qt 三端的 MQTT 连接代码**（包括 clientId 生成方式、连接参数、重连逻辑、订阅/发布流程）。MQTT 通信链路已调通，任何改动可能破坏现有连接稳定性。
- Web 看板只改 UI 表现层（HTML/CSS/数据渲染逻辑），不改底层 MQTT 连接。
- Qt 桌面端同理，UI 和功能可以改，MQTT 连接代码不动。
