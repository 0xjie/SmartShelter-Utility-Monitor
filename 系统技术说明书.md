# 系统技术说明书

## 文档说明

本文档依据当前项目实际源码编写，覆盖以下真实子系统：

- `D:/AAA/FinalSystem`：STM32F103 固件
- `D:/AAA/esp32/stm32_to_esp32_uart_test/stm32_to_esp32_uart_test.ino`：ESP32 通信网关程序
- `D:/AAA/System_UI`：Qt 桌面客户端
- `D:/AAA/dashboard`：Web 看板与历史数据导出脚本

本文档不引入源码中不存在的模块。根据当前代码实现，系统不包含独立自建后端服务进程，云侧消息中转由巴法云 MQTT 提供，业务数据管理与落库由 Qt 客户端和本地 SQLite 完成。

---

## 1. 系统总体功能

本系统面向安置点环境监测与水电资源管理场景，围绕“采集、判断、联动、上报、展示、记录”六个环节运行，主要功能如下：

- 采集温度、湿度、PM2.5、空气质量、电流、水流等现场参数。
- 在 STM32 本地完成阈值判断、预警判定、告警判定和组合联动判定。
- 按环境等级自动控制风扇、窗户舵机、蜂鸣器和指示灯。
- 通过 ESP32 将串口数据转发到 MQTT 平台，实现远程数据上报与控制下发。
- 通过 Qt 客户端实现实时监测、阈值配置、设备控制、报警记录、求助处理和运行状态展示。
- 通过 SQLite 持久化保存历史监测数据、报警记录、设备信息、远程控制日志和用户信息。
- 通过 Web 看板实现实时可视化展示，并提供一键求助入口。

---

## 2. 系统五层架构

系统实际可归纳为五层结构。

| 层次 | 组成 | 真实实现 | 主要职责 |
| --- | --- | --- | --- |
| 第1层：感知执行层 | 传感器与执行器 | DHT11、GP2Y10、MQ135、ACS712、流量传感器、风扇、舵机、蜂鸣器、三色灯、OLED | 完成现场数据采集与执行动作输出 |
| 第2层：嵌入式控制层 | STM32 主控 | `FinalSystem/User/main.c` 及 `Hardware` 目录 | 完成阈值判断、联动控制、数据打包和串口收发 |
| 第3层：边缘通信层 | ESP32 网关 | `stm32_to_esp32_uart_test.ino` | 完成 WiFi 接入、MQTT 连接、UART 与 MQTT 桥接 |
| 第4层：消息与应用层 | MQTT 平台、Qt 客户端 | 巴法云 MQTT、`System_UI` | 完成消息中转、实时监视、阈值下发、报警管理、远程控制 |
| 第5层：存储与展示层 | SQLite、Web 看板 | `system_ui.sqlite`、`dashboard` | 完成数据持久化、历史导出、实时可视化和求助展示 |

架构特征如下：

- STM32 是本系统的本地控制核心，不依赖云端完成阈值判断。
- ESP32 不承担核心业务判断，只承担边缘通信网关职责。
- MQTT 在当前系统中既承载上行数据，又承载下行控制。
- Qt 客户端同时承担业务应用层和本地数据管理层职责。
- Web 看板不直接连接 SQLite，只读取导出的历史 JS 数据并订阅 MQTT 实时消息。

---

## 3. 数据流

系统数据流按方向可分为实时采集数据流、控制数据流、报警数据流和历史存储数据流。

### 3.1 实时采集数据流

1. STM32 周期性采集 DHT11、GP2Y10、MQ135、ACS712 和流量传感器数据。
2. STM32 在本地完成阈值判断、等级计算、联动控制和资源剩余量计算。
3. STM32 将监测结果封装为 JSON，通过 USART1 发送给 ESP32。
4. ESP32 从 `Serial2` 接收完整 JSON，并按周期发布到 MQTT 主题 `test001up`。
5. Qt 客户端订阅该主题，解析消息后刷新界面，并写入 SQLite。
6. Web 看板通过 MQTT WebSocket 订阅同一主题，实现实时展示。

### 3.2 控制数据流

1. Qt 客户端构造控制命令或阈值命令并发布到 `test001up`。
2. ESP32 接收消息后判断消息类型。
3. 数字控制码直接经串口转发给 STM32。
4. 阈值配置命令由 ESP32 转换为串口阈值命令后发送给 STM32。
5. STM32 解析命令并更新控制状态或阈值参数。

### 3.3 报警数据流

1. STM32 根据单项阈值与组合条件生成报警码数组。
2. 报警码随上行 JSON 一并发送至 Qt 和 Web。
3. Qt 对报警码集合进行增量比较，识别新增报警和已恢复报警。
4. 新增报警写入 `alarm_info` 表，恢复报警补写结束时间并更新状态。
5. Web 看板仅负责实时展示，不承担报警持久化。

### 3.4 历史存储数据流

1. Qt 客户端将每次接收到的实时样本写入 `data` 表。
2. `dashboard/export_history.py` 读取 `system_ui.sqlite` 的 `data` 表。
3. 脚本按日期聚合日用电量和日用水量估算值，输出到 `dashboard/js/history_data.js`。
4. Web 看板加载该 JS 文件展示历史趋势图。

---

## 4. 通信流

### 4.1 STM32 与 ESP32 通信流

- 物理接口：USART1 对接 ESP32 串口。
- 速率配置：`115200, 8N1`。
- 上行形式：一行一帧的 JSON 文本，以 `\r\n` 结束。
- 下行形式：
  - 单字符控制码 `0` 到 `8`
  - 阈值命令字符串，如 `ta38`
  - JSON 命令，如 `{"cmd":"mode","val":"MANUAL"}`

### 4.2 ESP32 与 MQTT 通信流

- ESP32 连接 WiFi 后连接 `bemfa.com:9501`。
- 主题 `test001up` 同时承担订阅和发布功能。
- ESP32 为避免回环，会忽略包含 `"sen"` 字段的消息，将其视为设备上行数据。

### 4.3 Qt 与 MQTT 通信流

- Qt 连接巴法云 MQTT 后订阅 `test001up` 和 `WebQT1`。
- `test001up` 用于接收设备实时数据，也用于向设备发送控制指令。
- `WebQT1` 用于接收 Web 看板发起的一键求助消息。

### 4.4 Web 与 MQTT 通信流

- Web 看板通过 MQTT over WebSocket 连接 `wss://bemfa.com:9504/wss`。
- Web 订阅 `test001up` 获取实时监测数据。
- Web 向 `WebQT1` 发布求助消息。

---

## 5. STM32 职责

STM32 在当前系统中承担以下职责。

### 5.1 传感器采集

- 读取 DHT11 温湿度数据。
- 读取 GP2Y10 粉尘浓度数据。
- 读取 MQ135 空气质量数据。
- 读取 ACS712 电流数据。
- 通过外部中断和定时器统计流量脉冲，计算水流速率和累计流量。

### 5.2 阈值管理

- 维护温度、湿度、PM2.5、空气质量、电流、水流的告警阈值。
- 根据告警阈值自动推导预警阈值。
- 支持远程恢复默认阈值。
- 支持远程修改单项阈值、蓄电池容量、水箱容量和资源百分比。

### 5.3 预警与告警判断

- 对六类监测量分别进行正常、预警、告警三级判断。
- 采用滞回恢复和连续计数机制，减少误触发。
- 分别计算单项等级、环境等级和全局联动等级。

### 5.4 自动联动控制

- 根据环境等级自动控制风扇启停和窗户开闭角度。
- 根据联动等级自动控制蜂鸣器提示方式。
- 根据联动等级自动控制红、绿、黄指示灯。

### 5.5 手动控制管理

- 支持全局 `AUTO` 与 `MANUAL` 模式切换。
- 支持蜂鸣器、风扇、舵机、警报灯单独手动接管。
- 手动模式下设置 5 分钟超时回退机制，超时自动恢复自动模式。

### 5.6 数据封装与串口通信

- 将监测值、资源值、等级值、执行器状态、模式状态、报警码、动作标志和故障码封装为 JSON。
- 通过 USART1 发送上行数据。
- 通过环形缓冲区接收下行命令。

---

## 6. ESP32 职责

ESP32 在当前系统中承担边缘网关职责，不承担本地控制决策职责。

### 6.1 网络接入

- 连接指定 WiFi。
- 维护 MQTT 长连接。
- 在断网或掉线时自动重连。

### 6.2 串口桥接

- 从 `Serial2` 读取 STM32 上报的完整 JSON 数据帧。
- 缓存 STM32 最近一次完整 JSON。
- 按固定周期将 JSON 原样发布到 MQTT。

### 6.3 下行命令转发

- 订阅 `test001up`。
- 识别数字控制码命令并转发给 STM32。
- 识别阈值对象 `{"th":{...}}` 并转换为串口阈值命令。
- 识别新 JSON 控制协议并原样转发给 STM32。

### 6.4 阈值同步

- 在处理 `{"th":{...}}` 后，向同一主题回发 `th_sync` 形式消息。
- 该逻辑用于云端或 Web 端阈值同步，并避免重复循环。

---

## 7. Qt 职责

Qt 桌面客户端是系统的业务管理中心，主要职责如下。

### 7.1 实时监测与可视化

- 连接 MQTT 并订阅实时主题。
- 解析 STM32 新协议、旧扁平协议和旧云端 `params` 协议。
- 展示实时温湿度、PM2.5、空气质量、电流、水流和资源状态。
- 绘制实时折线图和状态卡片。

### 7.2 阈值配置

- 提供温度、湿度、PM2.5、空气质量、电流、水流八项告警阈值输入界面。
- 通过 MQTT 下发阈值配置。
- 提供恢复默认阈值按钮，下发 `reset_th` 命令。

### 7.3 远程控制

- 提供蜂鸣器、风扇、窗户、警报灯控制按钮。
- 将控制命令编码为 `{"code":x,"name":"..."}` 并发布到 `test001up`。
- 记录远程控制执行日志。

### 7.4 报警管理

- 根据当前报警码集合与上一周期报警码集合做差集。
- 将新增报警写入数据库。
- 将已恢复报警更新为已解决状态。
- 对 Web 求助消息执行入库和弹窗提醒。

### 7.5 设备信息管理

- 维护本地设备信息列表。
- 将设备信息同步写入 `device_info` 表。
- 更新设备运行状态、搜索与筛选结果。

### 7.6 本地数据持久化

- 将实时样本写入 `data` 表。
- 将报警、求助、控制日志、离线记录写入 SQLite。

### 7.7 认证与基础账户管理

- 提供登录、注册、切换账号和修改账号功能。
- 用户信息保存在同一 SQLite 数据库中。

---

## 8. SQLite 职责

SQLite 在当前系统中的职责不是云端共享数据库，而是 Qt 客户端的本地业务数据库。

### 8.1 本地历史数据存储

- 保存实时监测数据样本。
- 支持时间序列查询和均值统计。

### 8.2 报警事件存储

- 保存报警起始时间、结束时间、报警内容、级别和状态。
- 保存 Web 求助记录。

### 8.3 设备与日志存储

- 保存设备信息。
- 保存远程控制执行日志。
- 保存传感器离线时长记录。

### 8.4 用户认证数据存储

- 保存用户账号、密码、角色和创建时间。

---

## 9. Web 看板职责

Web 看板在当前系统中的职责如下。

### 9.1 实时展示

- 通过 MQTT WebSocket 订阅 `test001up`。
- 解析实时上行 JSON。
- 展示温度、湿度、PM2.5、空气质量、水电资源、执行器状态和报警信息。

### 9.2 历史趋势展示

- 读取 `history_data.js` 中的历史汇总结果。
- 展示按天聚合的用电与用水趋势图。

### 9.3 一键求助

- 向 `WebQT1` 发布求助消息。
- 求助类型包括身体不适、生活缺水、电力故障和其他求助。

### 9.4 展示边界

- Web 看板不直接写入 SQLite。
- Web 看板不直接控制 STM32。
- Web 看板不承担报警持久化，只承担实时展示与求助发起功能。

---

## 10. MQTT 主题设计

当前系统只使用两个业务主题。

| 主题名 | 发布方 | 订阅方 | 用途 |
| --- | --- | --- | --- |
| `test001up` | ESP32、Qt | ESP32、Qt、Web | 设备实时数据上行、Qt 控制指令下行 |
| `WebQT1` | Web | Qt | 一键求助通知 |

主题设计特点如下：

- `test001up` 为单主题双向复用设计。
- ESP32 通过过滤 `"sen"` 字段识别设备上行消息，避免将上行数据再次当作下行命令处理。
- `WebQT1` 为单向通知主题，仅用于 Web 到 Qt 的求助消息传递。

---

## 11. JSON 协议设计

### 11.1 STM32 上行 JSON 结构

STM32 当前上行消息为一行完整 JSON，核心字段如下。

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `sen.t` | 整数 | 温度 |
| `sen.h` | 整数 | 湿度 |
| `sen.pm` | 整数 | PM2.5 |
| `sen.aq` | 整数 | 空气质量指标 |
| `sen.f` | 浮点数 | 水流量 |
| `sen.i` | 整数 | 电流上报值 |
| `res.bp` | 整数 | 电量百分比 |
| `res.wp` | 整数 | 水量百分比 |
| `res.tu` | 整数 | 已用电量，单位 mAh |
| `res.wu` | 整数 | 已用水量，单位 cL |
| `res.br` | 整数 | 剩余电量，单位 mAh |
| `res.wr` | 整数 | 剩余水量，单位 cL |
| `res.ps` | 整数 | 电力状态，0 正常，1 预警，2 告警 |
| `res.bc` | 整数 | 电池总容量，单位 mAh |
| `res.tc` | 整数 | 水箱总容量，单位 cL |
| `res.bt` | 整数 | 预计剩余供电时间，单位 min |
| `res.wt` | 整数 | 预计剩余供水时间，单位 min |
| `lv.link` | 整数 | 全局联动等级 |
| `lv.env` | 整数 | 环境联动等级 |
| `lv.d` | 数组 | 六项监测量等级，顺序为温、湿、PM2.5、AQ、电流、水流 |
| `act.bz` | 整数 | 蜂鸣器状态 |
| `act.fan` | 整数 | 风扇状态 |
| `act.svo` | 整数 | 舵机角度 |
| `act.led` | 数组 | 红、绿、黄三色灯状态 |
| `mod.gl` | 整数 | 全局模式，0 自动，1 手动 |
| `mod.bz` | 整数 | 蜂鸣器是否手动 |
| `mod.fan` | 整数 | 风扇是否手动 |
| `mod.svo` | 整数 | 舵机是否手动 |
| `mod.led` | 整数 | 警报灯是否手动 |
| `alm` | 数组 | 报警码数组 |
| `actn` | 整数 | 自动动作标志位 |
| `flt` | 数组 | 故障码数组 |

### 11.2 STM32 上行示例

```json
{
  "sen": {"t": 26, "h": 55, "pm": 42, "aq": 38, "f": 2.50, "i": 2200},
  "res": {"bp": 80, "wp": 75, "tu": 120, "wu": 35, "br": 8000, "wr": 750, "ps": 0, "bc": 10000, "tc": 1000, "bt": 180, "wt": 240},
  "lv": {"link": 1, "env": 1, "d": [1, 0, 1, 0, 0, 0]},
  "act": {"bz": 1, "fan": 1, "svo": 90, "led": [0, 0, 1]},
  "mod": {"gl": 0, "bz": 0, "fan": 0, "svo": 0, "led": 0},
  "alm": [101, 121],
  "actn": 20,
  "flt": []
}
```

### 11.3 下行控制协议

当前系统实际支持以下下行形式。

#### 1. 数字控制码

| 控制码 | 含义 |
| --- | --- |
| `0` | 蜂鸣器关闭 |
| `1` | 蜂鸣器开启 |
| `2` | 窗户打开，舵机转到 90 度 |
| `3` | 窗户关闭，舵机转到 0 度 |
| `4` | 风扇开启 |
| `5` | 风扇关闭 |
| `6` | 警报灯手动开启 |
| `7` | 警报灯恢复自动 |
| `8` | 控制状态复位，恢复自动模式 |

对应 Qt 下发示例：

```json
{"code":4,"name":"风扇开启"}
```

#### 2. JSON 控制命令

STM32 串口解析器支持以下 JSON 形式：

```json
{"cmd":"ctrl","act":"fan","val":"on"}
{"cmd":"ctrl","act":"servo","val":90}
{"cmd":"mode","val":"MANUAL"}
{"cmd":"reset"}
{"cmd":"reset_th"}
{"cmd":"th","key":"ta","val":38}
```

说明如下：

- `ctrl` 表示单设备控制。
- `mode` 表示全局模式切换。
- `reset` 表示控制状态复位。
- `reset_th` 表示恢复默认阈值。
- `th` 表示单项阈值配置。

#### 3. 阈值批量配置命令

Qt 当前实际使用的阈值下发形式为：

```json
{
  "th": {
    "ta": 38,
    "tb": 10,
    "ha": 85,
    "hb": 20,
    "pa": 150,
    "aa": 200,
    "ca": 15,
    "fa": 10
  }
}
```

ESP32 收到该命令后，不直接原样发送给 STM32，而是将每个键值拆分为串口阈值命令，如：

```text
ta38
tb10
ha85
```

### 11.4 Web 求助消息协议

Web 看板当前求助消息格式如下：

```json
{
  "type":"medical",
  "label":"身体不适",
  "time":"2026-05-22T10:00:00.000Z",
  "site":"一号营地"
}
```

字段含义如下：

- `type`：求助类型编码
- `label`：求助中文名称
- `time`：发起时间
- `site`：站点名称

---

## 12. 自动控制逻辑

自动控制逻辑在 STM32 本地执行，主要规则如下。

### 12.1 阈值推导

Qt 界面仅配置告警阈值，STM32 自动推导预警阈值：

| 监测量 | 预警阈值推导方式 |
| --- | --- |
| 温度上限 | `告警上限 - 6` |
| 温度下限 | `告警下限 + 8` |
| 湿度上限 | `告警上限 - 15` |
| 湿度下限 | `告警下限 + 10` |
| PM2.5 | `告警值 / 2` |
| 空气质量 | `告警值 / 2` |
| 电流 | `告警值 × 2 / 3` |
| 水流 | `告警值 / 2` |

### 12.2 单项等级判定

- 每项监测值都可被判定为正常、预警、告警三种状态。
- 进入预警或告警前，需达到连续计数条件。
- 退出预警或告警时，需满足滞回恢复条件和退出计数条件。

### 12.3 组合联动规则

系统定义了三组组合联动：

- 温度与 PM2.5 同时达到预警及以上，触发组合报警码 `200`。
- PM2.5 与空气质量同时达到预警及以上，触发组合报警码 `201`。
- 水流与电流同时达到预警及以上，触发组合报警码 `202`。

### 12.4 风扇与窗户自动控制

- 当环境等级为告警时，自动开启风扇，窗户打开到 90 度。
- 当环境等级为预警时，自动开启风扇，窗户保持关闭。
- 当环境等级为正常时，自动关闭风扇，窗户关闭。
- 当水流和电流同时达到预警及以上时，风扇自动关闭。

### 12.5 蜂鸣器自动控制

- 告警状态下采用双段鸣叫节奏。
- 预警状态下采用周期短鸣方式。
- 正常状态下蜂鸣器关闭。
- 若进入手动模式，则手动控制优先。

### 12.6 指示灯自动控制

- 告警时亮红灯。
- 预警时亮黄灯。
- 正常时亮绿灯。
- 若警报灯进入手动模式，则三色灯按手动逻辑控制。

### 12.7 手动优先与超时回退

- 蜂鸣器、风扇、舵机、警报灯均支持手动优先。
- 全局手动模式下，自动联动逻辑不覆盖手动输出。
- 手动持续超过 5 分钟后，系统自动复位到自动模式。

---

## 13. 告警机制

### 13.1 告警来源

告警来源分为三类：

- 单项阈值告警
- 组合联动告警
- 设备故障告警

### 13.2 报警码设计

当前代码中的报警码分布如下：

| 范围 | 含义 |
| --- | --- |
| `101` 到 `104` | 温度类 |
| `111` 到 `114` | 湿度类 |
| `121` 到 `122` | PM2.5 类 |
| `131` 到 `132` | 空气质量类 |
| `141` 到 `142` | 电流类 |
| `151` 到 `152` | 水流类 |
| `200` 到 `202` | 组合联动类 |
| `901` 到 `905` | 传感器故障类 |

### 13.3 STM32 端告警生成

- STM32 将当前命中的告警码写入 `alm` 数组。
- 将动作结果写入 `actn` 位标志。
- 将故障码写入 `flt` 数组。

### 13.4 Qt 端告警处理

- Qt 维护上一周期报警码集合。
- 当前周期与上一周期差集中的新增报警写入数据库。
- 已消失报警更新结束时间并标记为已解决。
- 当新报警出现时，弹出实时告警对话框。

### 13.5 Web 求助处理

- Web 求助不通过 `alm` 上报，而通过 `WebQT1` 主题单独发送。
- Qt 收到求助后写入 `alarm_info`，级别记为“求助”。
- 操作员在 Qt 端手动点击“解决”后，求助状态更新为已解决。

---

## 14. 数据库存储逻辑

### 14.1 数据库文件

- 目录：`D:/System_UI_Data`
- 文件名：`system_ui.sqlite`

### 14.2 表结构与用途

| 表名 | 用途 |
| --- | --- |
| `data` | 监测样本历史表 |
| `device_abnormal_records` | 设备离线记录表 |
| `device_info` | 设备基础信息表 |
| `remote_exec_logs` | 远程控制日志表 |
| `alarm_info` | 报警与求助信息表 |
| `users` | 用户认证信息表 |

### 14.3 实时样本存储逻辑

- Qt 每接收一次有效实时样本，就向 `data` 表插入一条记录。
- 存储字段包括时间、温度、湿度、PM2.5、空气质量、电流和水流。
- `data.current_a` 虽字段名为电流安培值，但历史导出脚本按 mA 进行日汇总估算。

### 14.4 报警存储逻辑

- 新增报警调用 `insertAlarmInfo` 写入 `alarm_info`。
- 报警恢复时调用 `updateAlarmResolved` 更新结束时间和状态。
- Web 求助同样写入 `alarm_info`，但级别为“求助”。

### 14.5 设备离线记录逻辑

- Qt 在 MQTT 离线期间，按传感器字段更新 `device_abnormal_records`。
- 当前记录的传感器字段包括：
  - `temperature`
  - `humidity`
  - `water_flow`
  - `current`
  - `air_quality`
  - `pm25`

### 14.6 设备信息存储逻辑

- Qt 启动后将本地设备列表写入 `device_info`。
- 同一 `device_id` 存在时执行更新，不重复插入。

### 14.7 远程控制日志逻辑

- 每次 Qt 发出控制命令时，向 `remote_exec_logs` 表写入一条执行日志。
- 记录内容包括执行时间、设备标识、命令文本和结果文本。

### 14.8 历史导出逻辑

- `export_history.py` 直接读取 `data` 表。
- 该脚本按日期进行聚合。
- 用电量估算公式为 `SUM(current_a * 5 / 3600)`。
- 用水量估算公式为 `SUM(flow_l_min * 5 / 60)`。
- 聚合结果输出到 `dashboard/js/history_data.js`，供 Web 看板加载。

---

## 结论

从源码实现看，当前系统是一套由 STM32 本地控制、ESP32 边缘桥接、MQTT 云端中转、Qt 客户端管理、SQLite 本地落库、Web 看板展示共同组成的多端协同系统。其核心特点不是传统的“传感器 + 自建后端服务器”模式，而是“本地控制优先、消息中转上云、桌面端承载业务管理”的实现方式。后续若用于毕业论文、开题报告或系统设计说明，应以此实际结构为准。
